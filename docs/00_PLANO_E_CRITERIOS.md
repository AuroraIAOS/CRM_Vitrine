# PLANO_E_CRITERIOS — CRM Vitrine

## Princípio-guia
Entregar um MVP em uma semana é sempre melhor que passar uma eternidade construindo algo surreal.
Foco: fatia vertical funcional (100% verde) antes de qualquer sofisticação.

## Idea lock (fechado no estágio criativo)
- Problema/persona: Max precisa vender CRMs configurados por cliente rápido, sem repetir a dificuldade operacional (VPS/Docker/Evolution GO) que travou o CRM Maximus.
- Escopo do MVP (dentro): núcleo de conta/RBAC; `aba_people` (pessoas unificadas); `aba_catalog`; `aba_scheduling`; `aba_finance`; `aba_health`; `aba_messaging` (Meta Cloud API); `aba_sales`; `aba_automations`; `aba_ai` (bring-your-own-key); deploy estático + FTP.
- Fora do MVP (depois): Evolution GO; RAG versionado em arquivo; CLI de clonagem automatizada; `HaveIBeenPwned`.
- Stack essencial v01: Vite + React 18 + TS + PWA; Supabase (Postgres/Auth/RLS/Storage/Edge Functions); `pg_cron`; deploy build+FTP em hospedagem compartilhada.
- Coração do modelo de dados: `aba_people.pessoas` (tabela-mãe, chave compartilhada com `leads`/`clientes`/`funcionarios`/`fornecedores`); padrão RLS `is_account_member() + access.can()` em todo schema `aba_*`.
- Restrições inegociáveis (compliance/ética/segurança): RLS ativa e testada em toda tabela; prontuário só via concessão explícita + log de acesso; segredo de conta sempre criptografado em repouso; nenhuma credencial commitada.
- Decisões pendentes p/ Etapa 01:
  - Tradução coluna a coluna de `aba_catalog`, `aba_scheduling`, `aba_finance`, `aba_health`, `aba_messaging` a partir do mapa em `docs/02_MODELO_DE_DADOS.md` §7 (nomes de tabela já decididos, DDL completo é execução).
  - RLS detalhada de `aba_sales`, `aba_automations`, `aba_ai` (padrão já definido, política por tabela é execução).
  - Ciclo de estados completo de `automacoes`/`fluxos` e `faturas`/`pagamentos` — herdar das migrations originais do Maximus.
  - Política de retenção de `log_acesso` (prontuário) — prazo mínimo legal a confirmar por jurisdição do cliente.

---

## Convenção `0X.0` — revisão de plano no início de toda Etapa

**Regra permanente, válida para a Etapa 01 e para toda Etapa futura (02, 03, e as etapas de versionamento que vierem depois dela):** a primeira subetapa de cada Etapa é sempre uma subetapa `0X.0 — Leitura de Referências e Revisão do Plano`, executada em `[Plan Mode]`, antes de qualquer subetapa de execução daquela Etapa. Ela relê os repositórios de referência relevantes ao escopo que se abre, confere se as decisões registradas no plano ainda batem com a realidade do repositório e do código, e revisa `docs/00_PLANO_E_CRITERIOS.md` à luz do que a Etapa anterior deixou pronto. Isso vale mesmo quando a Etapa nasceu de um documento já revisado no estágio criativo — o plano pode ter ficado desatualizado entre o fechamento do documento e o início da execução.

Instância desta regra na Etapa 01: **Subetapa 01.0**, abaixo. As instâncias nas Etapas 02 e 03 (**Subetapa 02.0**, **Subetapa 03.0**) seguem o mesmo objetivo, adaptado ao escopo de cada uma — ver cada Etapa.

---

# ESTÁGIO PRÁTICO (executado no Claude CODE)

## ETAPA 01 — PLANEJAMENTO E ESTRUTURAS
Objetivo geral: portar e reorganizar o schema do CRM Maximus, deixar toda a fundação desenhada, conectada, testada, aprovada e versionada. Gerar HANDOFF_BUILD ao final.
Modo predominante: [Plan Mode]
Portão de entrada: repositório com fundação documental completa (7 docs + 3 handoffs + `CLAUDE.md` + `CHANGELOG.md`), sem subetapa de código iniciada. Prova: `git log` mostra só o commit inicial da fundação.
Portão de saída: todos os schemas do MVP aplicados no Supabase do projeto, RLS testada 100% verde em cada um, varredura de segredos zerada, portão de segurança adversarial executado com parecer registrado, e `HANDOFF_BUILD.md` preenchido. Enquanto vermelho, nenhuma subetapa da Etapa 02 pode abrir.
Observações: nada destrutivo sem aprovação; commit com prefixo padronizado (feat/fix/docs/chore/refactor/test) + push ao fim de cada subetapa; sessão separada das demais etapas.

### Subetapa 01.0 — Leitura de Referências e Planejamento da Etapa 01 [Plan] [LLM: Opus]
Objetivo: Ler repositórios de referência (CRM Maximus = https://github.com/AuroraIAOS/CRM_Maximus.git; CRM Sindcom = https://github.com/AuroraIAOS/CRM-Sindcom.git); construir arquivo `handoffs/instrucoes.md` conforme modelo do CRM Maximus; rever/atualizar este arquivo `docs/00_PLANO_E_CRITERIOS` e; elaborar plano de ação da Etapa 01.
Conclusão: repositórios avaliados; arquivo `handoffs/instrucoes.md` criado e arquivo `docs/00_PLANO_E_CRITERIOS` conferido; plano de ação apresentado.
Qualidade: nenhuma inconscistência ou divergência entre arquivos; `handoffs/instrucoes.md` seguindo modelo padrão do CRM Maximus; `docs/00_PLANO_E_CRITERIOS` apresentando número de tentativas do [goal] e indicação de escalagem de LLM (Haiku >> Sonnet >> Opus).
Evidência: relatório de informações útes dos repo de referência + `handoffs/instrucoes.md` e `docs/00_PLANO_E_CRITERIOS` publicados e atualizados.
Status: ✅ CONCLUÍDA

### Subetapa 01.1 — Bootstrap do repositório [Plan] [LLM: Sonnet]
Objetivo: inicializar projeto Vite+PWA+TS+Supabase, `.gitignore`/`.env` reais a partir dos `.example`, conexão com projeto Supabase confirmada.
Conclusão: `npm run dev` sobe local; client Supabase conecta e autentica um usuário de teste.
Qualidade: nenhuma credencial commitada; estrutura de pastas `src/` alinhada ao padrão do CRM Sindcom.
Evidência: print do app rodando local + query de teste retornando linha do `public.accounts`. **Adaptado na execução:** o schema núcleo (`public.accounts`) só é aplicado na Subetapa 01.2 — nesta subetapa o projeto Supabase tinha `public` vazio. A evidência de conectividade usou `auth.users` (schema de plataforma, existe em todo projeto Supabase, independente do schema aplicado): um usuário de teste foi criado via Admin API, autenticado de ponta a ponta pelo app real (login → dashboard → logout) e depois apagado. Ver `handoffs/instrucoes.md` §5.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA

### Subetapa 01.2 — Núcleo + `aba_people` [Plan] [Accept] [LLM: Sonnet]
Objetivo: aplicar migrations do núcleo (`public`, `access`, `licensing`) e de `aba_people` completo (pessoas + 4 papéis + tags/notas/campos customizados + `converter_lead()`), conforme `docs/02_MODELO_DE_DADOS.md` §3.
Conclusão: todas as tabelas criadas; RLS ativa; `converter_lead()` testada (lead convertido mantém tags/notas); suíte de RLS portada do Maximus e adaptada passa 100%.
Qualidade: nenhuma FK quebrada; nomenclatura conforme `docs/02` §1 sem exceção não documentada; hardening equivalente às migrations `040`–`042`/`063`–`065`/`064`/`077` do Maximus já embutido no DDL inicial (ver `handoffs/instrucoes.md` §4).
Evidência: query mostrando pessoa com papel `lead` convertida para `cliente` mantendo `pessoa_id` e tags associadas; resultado da suíte de testes RLS.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — núcleo (`001_core_public.sql`, `002_core_licensing.sql`, `003_core_access.sql`) + `aba_people` (`004_aba_people.sql`) aplicados via MCP; hardening de privilégio de função (`005_harden_function_privileges.sql`) e de exposição/GRANT de schema (`006_expose_schemas_and_narrow_grants.sql`) aplicados na mesma subetapa, fechando dois achados reais medidos ao vivo (não hipotéticos) — ver `handoffs/instrucoes.md` §5. Suíte de RLS portada (`crm/tests/rls/`, fixture via `crm/scripts/seed_test_users.mjs`) — 11/11 testes verdes, incluindo `converter_lead()` idempotente com `pessoa_id`/tags/notas preservados e a prova da camada `access.can()` isolada de `is_account_member`.

### Subetapa 01.3 — Portar `aba_catalog`, `aba_scheduling`, `aba_finance` [Plan] [Accept] [LLM: Sonnet]
Objetivo: traduzir e aplicar as migrations dessas três schemas a partir do original do Maximus (mapa em `docs/02` §7 e `db/migrations/README.md`), ajustando FKs para `aba_people.clientes(id)`.
Conclusão: schemas aplicados; RLS testada; nenhuma referência a `customer_id` quebrada pela unificação de pessoas; `btree_gist` habilitada em `extensions` antes da restrição de exclusão por intervalo de `aba_scheduling`.
Qualidade: nomenclatura 100% português dentro do schema; hardening equivalente às migrations `067`/`068`/`071`/`078`/`079` do Maximus já embutido.
Evidência: query cruzando `aba_finance.contratos` → `aba_people.clientes` → `aba_people.pessoas` retornando a cadeia completa.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — `aba_catalog` (`008_aba_catalog.sql`, com `definir_variante_padrao()` já embutido), `aba_scheduling` (`009_aba_scheduling.sql`, com fuso único, restrições de exclusão por intervalo, verificação de expediente/folga e enfileiramento/cancelamento automático de lembretes já no DDL inicial), `aba_finance` (`010_aba_finance.sql` + `011_aba_finance_operations.sql`, com termos de contrato/parcelas, moeda BRL por padrão e as seis operações — `vender_plano`, `estornar_sessao`, `atualizar_status_fatura`, `marcar_faturas_vencidas`, `expirar_planos`, `planos_vencendo_em` — já no DDL inicial) aplicados via MCP, precedidos por `007_enable_btree_gist.sql` e fechados por `012_expose_new_module_schemas.sql`. Hardening dobrado desde a primeira migration em todo schema novo (GRANT estreito nunca `TRUNCATE`, `REVOKE ... FROM PUBLIC` *e* `FROM anon` em toda função, `ALTER DEFAULT PRIVILEGES` já dentro da migration que cria o schema — fechando a pendência que a Subetapa 01.2 tinha deixado registrada). **Decisão de escopo registrada:** o CHECK "profissional ativo exige funcionário" e a RPC de liga/desliga do Maximus (migration 075) ficaram de fora — dependem de um fluxo de convite→funcionário (migration 074) que o Vitrine ainda não construiu; só o desenho de FK (`profissionais.funcionario_id → aba_people.funcionarios`) foi portado. Ver `db/migrations/README.md` e `handoffs/instrucoes.md` §5. Suíte de RLS estendida (`crm/tests/rls/02_aba_catalog.spec.ts`, `03_aba_scheduling.spec.ts`, `04_aba_finance.spec.ts`) — 29/29 testes verdes, incluindo a cadeia `aba_finance.contratos → aba_people.clientes → aba_people.pessoas`, a restrição de exclusão por sobreposição de agenda (23P01), a falha fechada de agendamento sem expediente cadastrado (23514) e o isolamento admin+ de `regras_comissao`.

### Subetapa 01.4 — Portar `aba_health` [Plan] [Accept] [LLM: Sonnet]
Objetivo: traduzir e aplicar `aba_health`, preservando IBAC/ABAC (`concessoes_prontuario`, `log_acesso`) sem simplificação.
Conclusão: RLS testada com profissional sem concessão (deve falhar leitura) e com concessão (deve funcionar); toda leitura E toda escrita clínica gera linha em `log_acesso`.
Qualidade: nenhum atalho que permita `agent` genérico ler prontuário sem concessão; hardening equivalente às migrations `053`/`058`/`069`/`070`/`076` do Maximus já embutido (log por função nunca só por política; `FORCE ROW LEVEL SECURITY`; log cobrindo escrita, não só leitura; profissional exige funcionário ativo).
Evidência: teste de RLS mostrando os dois cenários (negado/permitido) + linha de log gerada para leitura e para escrita.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — `aba_health` aplicado via MCP em três migrations: `013_aba_health.sql` (schema, 7 tabelas, `aba_health.pode_acessar()` já com as três camadas — papel, permissão por módulo, atributo profissional + funcionário ativo (Maximus 076) —, `FORCE ROW LEVEL SECURITY`, leitura de conteúdo clínico só através de `ler_prontuario()`/`ler_evolucoes()`/`ler_respostas_anamnese()`/`ler_consentimentos()` com log obrigatório gravado na mesma transação (Maximus 053), escrita clínica logada por trigger em `criacao`/`atualizacao` (Maximus 070)), `014_aba_health_attachments_bucket.sql` (bucket privado `anexos-clinicos`, decisão por `pode_acessar_anexo()` em vez de "membro da conta", consentimento de imagem travando só a leitura — Maximus 069) e `015_expose_aba_health_schema.sql`. Suíte de RLS (`crm/tests/rls/05_aba_health.spec.ts`) — 8/8 testes verdes: cenário negado (agent sem concessão/atributo profissional), cenário permitido (concessão nominal, com contagem de log antes/depois provando o incremento), leitura direta de coluna clínica bloqueada por privilégio de coluna (42501, prova de que "leitura sem log é impossível"), escrita gerando log de criação e atualização automaticamente, evolução travada recusando alteração (23514), e a regra do Maximus 076 provada isolada: profissional ativo com funcionário ativo lê; desativar o funcionário revoga o acesso mesmo com `profissionais.ativo` continuando `true`. Suíte completa do projeto: 37/37 testes verdes. `typecheck` limpo; `get_advisors` (security) sem achado novo.

### Subetapa 01.5 — `aba_sales`, `aba_automations`, `aba_ai` (schemas novos) [Plan] [Accept] [LLM: Sonnet]
Objetivo: criar os três schemas novos conforme `docs/02` §4-6, com RLS no padrão `is_account_member + access.can`.
Conclusão: schemas aplicados; `access.modules` com as chaves `sales`/`automations`/`ai`; RLS testada.
Qualidade: `oportunidades` referencia `pessoa_id`, nunca `contact_id`.
Evidência: query criando oportunidade ligada a um lead não convertido e a um cliente já ativo, ambas funcionando.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — três schemas aplicados via MCP (`016_aba_sales.sql`, `017_aba_automations.sql`, `018_aba_ai.sql`, expostos por `019_expose_sales_automations_ai_schemas.sql`). `aba_sales` (funis/etapas_funil/oportunidades) já a partir do DDL de referência de `docs/02` §4. `aba_automations` e `aba_ai` traduzidos das tabelas soltas em `public` do Maximus (nunca modularizadas, ainda no modelo antigo `user_id`/`auth.uid()`) para o padrão `account_id` + `is_account_member` + `access.can()` — não porte 1:1, redesenho de autorização sobre a mesma lógica de negócio. Dois hardenings aplicados preventivamente na tradução: `automacao_logs` corrigida de uma política `FOR ALL` do Maximus (nome prometia só leitura) para só `SELECT`; `aba_ai.buscar_conhecimento_textual()` nasce `SECURITY INVOKER` desde o início, prevenindo de origem o vazamento entre contas que o Maximus só descobriu depois (GHSA-fg5p-2qc3-jmxr, migration 032 de lá). **Decisões de escopo registradas:** `pgvector`/busca semântica ficou de fora (already decidido no backlog de versionamento, alvo `+1.0`) — só o caminho lexical (`tsvector`/`ts_rank`) entrou; `claim_ai_reply_slot()` e as colunas de controle de resposta automática por conversa ficam para a Subetapa 01.6 (dependem de `aba_messaging.conversas`, que ainda não existe). Suíte de RLS ampliada (`crm/tests/rls/06_aba_sales.spec.ts`, `07_aba_automations.spec.ts`, `08_aba_ai.spec.ts`) — 57/57 testes verdes no projeto, incluindo a evidência da subetapa (oportunidade ligada a lead não convertido e a cliente ativo, ambas funcionando) e a prova de isolamento entre contas na busca textual de IA. `typecheck` limpo; `get_advisors` (security) sem achado novo além de um INFO esperado (fila `automacao_execucoes_pendentes` com RLS ativa e zero políticas — deny-by-default deliberado, server-only).

### Subetapa 01.6 — Portar `aba_messaging` + Meta Cloud API [Plan] [Accept] [LLM: Sonnet]
Objetivo: traduzir `aba_messaging` (contatos_canal, conversas, mensagens etc.), implementar Edge Function de webhook Meta com validação HMAC-SHA256. Search-first obrigatório (`CLAUDE.md` §11) na versão vigente da Graph API antes de escrever a primeira linha.
Conclusão: webhook recusa requisição não assinada; mensagem de teste recebida e persistida corretamente.
Qualidade: `ENCRYPTION_KEY`/`META_APP_SECRET` só em Edge Function, nunca expostos ao client; hardening equivalente às migrations `055`–`057` do Maximus já embutido (segredo de provedor nunca exposto pela API, webhook secret em hash, dedupe de evento com `account_id`).
Evidência: log da Edge Function mostrando rejeição de payload sem assinatura + aceite de payload assinado.
Esforço máximo do /goal: 5 tentativas (integração externa tende a exigir mais ajuste)
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — *search-first* confirmado contra a documentação vigente da Meta antes de escrever código (Graph API v26.0, lançada 2026-07-29; assinatura `X-Hub-Signature-256` HMAC-SHA256 sobre o corpo bruto; handshake `GET` com `hub.mode`/`hub.verify_token`/`hub.challenge`). `aba_messaging` aplicado via MCP (`020_aba_messaging.sql`, 11 tabelas, expostas por `021_expose_aba_messaging_schema.sql`) absorvendo as tabelas que no Maximus ficaram soltas em `public` (contacts/conversations/messages/etc.), traduzidas do modelo antigo `user_id` para `account_id` + RLS de dois níveis. `supabase/functions/whatsapp-webhook/index.ts` implantado (`verify_jwt: false`, autenticação real por assinatura HMAC em tempo constante) — corrige `configuracao_whatsapp.verify_token` removida na tradução (o handshake é por app, não por número, vestígio do modelo antigo do Maximus). Hardening das migrations `055`–`057` do Maximus já embutido desde o início: segredo de provedor e token de acesso nunca legíveis por `authenticated` (nem por owner); dedupe de evento por `(account_id, provedor, id_externo)`; coluna de hash pronta para quando Evolution GO entrar. **Evidência (script `crm/scripts/test_webhook_meta.mjs`, 15/15 asserções verdes):** log real da função — `POST | 401 | .../whatsapp-webhook` para requisição sem assinatura e com assinatura inválida; `POST | 200 | .../whatsapp-webhook` para payload assinado corretamente, com a mensagem de teste persistida em `aba_messaging.mensagens` (contato/conversa criados via upsert) e reenvio do mesmo evento tratado como idempotente (não duplicou). **Achado de segurança registrado e decisão de Max:** `hub.verify_token` aparece em texto puro nos logs de acesso do Edge Function — comportamento do próprio protocolo da Meta (handshake por `GET` com query string), não falha da implementação; Max decidiu (2026-08-16) não rotacionar por ora — ver `handoffs/instrucoes.md` §5/§6. Suíte de RLS ampliada (`crm/tests/rls/09_aba_messaging.spec.ts`) — 65/65 testes verdes no projeto. `typecheck` limpo; `get_advisors` (security) sem achado novo.

### Subetapa 01.7 — Varredura de segredos [Plan] [LLM: Sonnet]
Objetivo: rodar `gitleaks detect` sobre todo o histórico do repositório antes de qualquer push que amplie esse histórico, fechando o item 11 da checklist de conformidade.
Conclusão: saída zero. Todo achado é ou removido do histórico (com aprovação explícita de Max, por ser operação destrutiva) ou suprimido por fingerprint com justificativa registrada em `handoffs/instrucoes.md` — nunca reescrita de histórico por reflexo.
Qualidade: nenhum segredo real (chave, senha, token) suprimido por engano — só placeholder de doc ou credencial de teste confirmada como tal.
Evidência: saída do `gitleaks detect --report-format json` anexada; se houver supressão, o `.gitleaksignore` (ou equivalente) versionado com o motivo.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet na primeira; Opus na segunda.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas) — não seguir para a 01.8 com a varredura vermelha.
Status: ✅ CONCLUÍDA — `gitleaks` (8.30.1) não estava disponível no ambiente Windows; instalado via `winget install --id Gitleaks.Gitleaks -e` (pacote oficial do projeto upstream, ver `handoffs/instrucoes.md` §5). `gitleaks detect --source . --report-format json -v --redact` rodado sobre os 8 commits do histórico completo (`8bad058`…`7abcb92`): **`no leaks found`**, relatório JSON = `[]`. Nenhuma supressão por fingerprint necessária — nenhum `.gitleaksignore` criado, pois não houve achado (nem verdadeiro nem falso positivo). `git log --all --full-history -- .env` e `git ls-files | grep .env` confirmam que `.env` nunca foi rastreado pelo git em nenhum commit. Árvore de trabalho limpa (`git status --short` vazio) — nada pendente de commit que pudesse ampliar o histórico sem passar pela varredura.

### Subetapa 01.8 — Portão de segurança adversarial [Manual] [LLM: Opus]
Objetivo: executar o "Portão de segurança adversarial obrigatório" descrito na seção "Pendências vigiadas" abaixo, cobrindo todo o escopo aplicado nas subetapas 01.2–01.6, antes do primeiro deploy real (Etapa 02).
Conclusão: os 7 passos do portão executados em bench isolado (branch/worktree, nunca `main`); relatório final com parecer explícito (recomenda ou não recomenda trazer os avanços para `main`).
Qualidade: todo achado — explorável ou não — registrado em `handoffs/instrucoes.md` (seções 4 a 7, conforme a natureza); toda falha real corrigida vira item `[Goal]` com Conclusão/Qualidade/Esforço máximo declarados.
Evidência: relatório do bench + parecer. **O CODE nunca executa o merge do bench para `main` por conta própria — mesmo com parecer 100% favorável.** Ordenar o merge é atribuição exclusiva de Max (`CLAUDE.md` §13).
Esforço máximo: sem teto de tentativas — é auditoria, não `/goal` de implementação; roda até cobrir os 6 vetores mínimos da pendência vigiada.
Escalonamento de LLM: Opus do início ao fim — é a subetapa de maior risco da Etapa 01.
Status: ✅ CONCLUÍDA — bench `bench/01.8-seguranca-adversarial` (branch, não worktree: `.env`/`crm/.env.test` são arquivos reais gitignorados na raiz, e um worktree exigiria copiar segredos para uma segunda pasta). Os 7 passos executados. **35 ataques novos** escritos (`crm/tests/rls/10_adversarial_nucleo.spec.ts`, `11_adversarial_superficie.spec.ts`, `12_adversarial_webhook.spec.ts`), cobrindo os 6 vetores mínimos + LGPD/`aba_health` + fragilidade específica do Vitrine. **7 achados**, sendo **6 falhas reais exploráveis** — todas corrigidas e provadas (vermelho antes, verde depois) — e 1 não explorável, medido e aceito. Correções: `db/migrations/022_hardening_portao_adversarial.sql` (aplicada via MCP) + `whatsapp-webhook` v3 (implantada). Suíte final: **100/100 verdes** (65 originais sem regressão + 35 adversariais); `typecheck` limpo; `get_advisors` (security) sem achado novo; `gitleaks` `no leaks found`. Relatório e parecer em `docs/RELATORIO_01.8_PORTAO_ADVERSARIAL.md` — **parecer favorável ao merge**. O CODE entregou o parecer e parou, com `main` intocada em `d293951` (`CLAUDE.md` §13); **Max ordenou o merge em 2026-08-17**, executado em seguida por ordem dele (fast-forward, histórico linear preservado). `main` = `2ce1623`, alinhada ao Supabase (migration `hardening_portao_adversarial` aplicada, `whatsapp-webhook` v3); suíte revalidada no estado fundido — 100/100 verdes, `typecheck` limpo. Bench mantido no remoto como registro auditável.

#### Itens `[Goal]` gerados pelos achados da 01.8 (passo 4 do portão) — todos fechados nesta subetapa

- **[Goal] A01 — Tomada de conta por `INSERT` em `public.profiles` (CRÍTICO).** Conclusão: usuário sem perfil não consegue mais se inserir em conta alheia com papel arbitrário. Qualidade: a correção não depende de policy nova permissiva — nega por ausência de policy, e o trigger de coluna passa a cobrir `INSERT OR UPDATE` como segunda camada; `handle_new_user` e futuras RPCs de convite continuam funcionando. Esforço máximo: 3 tentativas. **Fechado em 1** — migration `022`, provado por `10_adversarial_nucleo.spec.ts`.
- **[Goal] A02 — `admin` reescrevia `public.accounts.owner_user_id` (ALTO).** Conclusão: a titularidade da conta não muda por `UPDATE` direto de nenhum papel. Qualidade: `admin` continua podendo editar o que é legitimamente configuração (nome da conta); só a coluna de titularidade fica travada. Esforço máximo: 2 tentativas. **Fechado em 1** — migration `022`.
- **[Goal] A03/A04/A05/A07 — Credencial legível pela API (ALTO/MÉDIO).** Conclusão: nenhuma das 8 colunas de credencial do banco é legível por `authenticated` ou `anon`; `service_role` mantém acesso. Qualidade: provado por controle negativo que o acesso legítimo não quebrou (`admin` lê a metadata do endpoint; `service_role` lê o segredo que precisa para assinar). Esforço máximo: 3 tentativas. **Fechado em 1** — migration `022`.
- **[Goal] A06 — Webhook da Meta cruzando fronteira de conta (MÉDIO/ALTO).** Conclusão: evento de status assinado só altera mensagem da conta resolvida pelo `phone_number_id`. Qualidade: provado contra a função **real implantada**, com assinatura HMAC verdadeira, não contra simulação da sentença SQL; as 15 asserções da evidência da Subetapa 01.6 continuam verdes. Esforço máximo: 3 tentativas. **Fechado em 1** — `whatsapp-webhook` v3.
- **[Não-achado] A08 — `public.rls_auto_enable()` executável por `anon`.** Objeto de plataforma do Supabase. Medido: `POST /rest/v1/rpc/rls_auto_enable` devolve `400 / 0A000 — cannot display a value of type event_trigger`; o corpo nunca executa. **Não explorável — aceito sem correção**, por decisão registrada de não mexer em objeto gerenciado pela plataforma. Reavaliar se o tipo de retorno mudar.

### Subetapa 01.9 — Geração do HANDOFF_BUILD [Plan] [LLM: Sonnet]
Objetivo: consolidar o que ficou 100% verde na Etapa 01 em `handoffs/HANDOFF_BUILD.md`, preenchendo os placeholders com o estado real (migrations aplicadas, artefatos, armadilhas conhecidas, primeiro passo da Etapa 02).
Conclusão: `HANDOFF_BUILD.md` sem placeholder `<...>` restante.
Qualidade: toda afirmação de "100% verde" no handoff é rastreável a uma evidência das subetapas 01.1–01.8.
Evidência: arquivo publicado + commit.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet nas duas.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — `handoffs/HANDOFF_BUILD.md` preenchido por inteiro (zero placeholder `<...>`), rastreando cada afirmação de "100% verde" à sua evidência: 22 migrations aplicadas (`db/migrations/001`–`022`, confirmadas via MCP `list_migrations` contra o projeto real), 100/100 testes de RLS verdes, webhook v3 no ar, bucket `anexos-clinicos` privado confirmado via `storage.buckets`, varredura de segredos zerada, portão adversarial com parecer favorável e merge executado. Seção "O que a Etapa 02 deve construir" com as 8 subetapas (02.0–02.7) resumidas a partir deste documento; seção de armadilhas puxando as entradas mais relevantes para quem constrói UI a partir de agora, incluindo as duas ressalvas herdadas da 01.8 (`select('*')` em tabela com narrowing, XSS armazenado como item obrigatório da 02.6).

---

## PORTÃO DE SAÍDA DA ETAPA 01 — ✅ ABERTO (2026-08-17)

Todas as condições do portão de saída declarado acima estão satisfeitas: todos os schemas do MVP aplicados no Supabase do projeto (núcleo + 9 schemas `aba_*`), RLS testada 100% verde em cada um (100/100 testes), varredura de segredos zerada (`gitleaks` `no leaks found`), portão de segurança adversarial executado com parecer registrado e merge ordenado por Max (`docs/RELATORIO_01.8_PORTAO_ADVERSARIAL.md`), e `HANDOFF_BUILD.md` preenchido sem placeholder. `main` = `c5f8e4f`, alinhada ao Supabase. **A Etapa 02 pode abrir.**

---

## ETAPA DE TRANSIÇÃO 1 → 2 — Registro de decisões de design [Plan] [LLM: Sonnet]

**Data:** 2026-08-18. Não é uma subetapa numerada (`0X.n`) — é o registro do trabalho feito entre o fechamento formal da Etapa 01 (01.9) e a abertura de fato da Etapa 02 (02.0), motivado por um evento fora do roteiro original: Max concluiu o estágio de design no Claude Design e entregou o pacote de wireframes antes da 02.0 rodar.

**Objetivo:** ler e interpretar por completo o pacote `design/wireframes-crm-sa-de-e-est-tica/` (16 telas), registrar as decisões de arquitetura/UI que ele obriga, e verificar se o fluxo de convite/funcionário/atributo profissional do Vitrine ainda bate com o que foi implantado no CRM Maximus — pergunta que Max fez diretamente ao ver as telas 1n/1o.

**Conclusão:**
- `docs/01_ARQUITETURA.md` §7 — navegação/ordem de módulo (ordem do wireframe vence, migration de realinhamento fica para a Etapa 02), login multi-conta adiado para `+1.0` (decisão de Max), perfis de UI 1n/1o mapeados sobre o RBAC existente (`agent`+atributo profissional / `admin`, sem papel novo), fluxo de convite→funcionário verificado migration a migration contra o CRM Maximus (018/019/074/075/076) — mesmo desenho de 5 passos, ponta final já portada desde a Subetapa 01.4, peças que faltam continuam deferidas conscientemente para a Etapa 02, mais uma regra nova (atributo profissional só para `agent`).
- `docs/04_DESIGN_E_MARCA.md` §5 — paleta, tipografia, radius/espaçamento/sombra, biblioteca de ícones (`lucide-react`) e padrões de componente ratificados a partir das 16 telas reais (não dos dois `_ds/*` do pacote, que não foram usados — comparação registrada em `handoffs/instrucoes.md` §5).
- `design/README.md` — inventário completo das 16 telas com módulo/componentes-chave.
- `docs/02_MODELO_DE_DADOS.md` — nota apontando a pendência de RPCs de convite/funcionário para quem for mexer em `aba_people.funcionarios`/`aba_scheduling.profissionais` na Etapa 02.
- `handoffs/instrucoes.md` §5 — dois registros novos (verificação do fluxo Maximus; descarte fundamentado dos `_ds/*`).

**Qualidade:** nenhuma decisão registrada como definitiva sem antes confirmar com Max os dois pontos que contradiziam o schema já aplicado (login multi-conta vs. `profiles.user_id UNIQUE`; papéis `PERFIS` do wireframe vs. `account_role_enum`) — ambos resolvidos por decisão explícita dele antes da escrita nos docs.

**Pendência que nasce daqui para a Etapa 02:** construir as RPCs de convite (`peek`/`resgatar`), o trigger de nascimento automático de funcionário e a RPC de liga/desliga do atributo profissional (com a regra nova "só `agent`") — ver `docs/01_ARQUITETURA.md` §7.4. Onde exatamente isso entra no roteiro de subetapas (dentro da 02.1 ou como subetapa própria) é decisão da **Subetapa 02.0**, não desta rodada. **✅ Resolvido na Subetapa 02.0 (2026-08-18), por decisão de Max: vira a Subetapa 02.2, própria e anterior à de Pessoas**, acrescida de `transfer_account_ownership` (prometida pela correção A02 da 01.8) e da RPC `criar_convite()`, peça sem equivalente no Maximus — lá a criação do convite era feita por route handler Next.js, que não existe numa SPA estática.

**Evidência:** os 5 arquivos listados acima, publicados e commitados nesta mesma sessão.

---

## ETAPA 02 — CONSTRUÇÃO E DEPLOY DO MVP (v01 / fatia vertical)
Objetivo geral: construir a UI, semear dados de demonstração, testar e refazer até o MVP 100% verde e no ar. Gerar HANDOFF_UPGRADE ao final.
Modo predominante: [Manual Mode] + [Goal] (um /goal por subetapa)
Portão de entrada: `HANDOFF_BUILD.md` preenchido sem placeholder, Etapa 01 com portão de saída verde (schemas aplicados, RLS 100% verde, varredura de segredos zerada, portão adversarial com parecer registrado).
Portão de saída: fluxo ponta a ponta das subetapas 02.1–02.12 testado manualmente e funcionando (as 16 telas do design ratificado, sobre os 9 módulos do v01); seed de demonstração populado; subdomínio no ar (02.13.a); varredura de segredos zerada de novo (o deploy pode ter introduzido `.env` de produção); portão de segurança adversarial reexecutado cobrindo a superfície nova da Etapa 02 (UI + convite + `pg_cron` + deploy); `HANDOFF_UPGRADE.md` preenchido. Enquanto vermelho, a Etapa 03 não abre nenhuma subetapa além da 03.0.
Observações: coletar evidências; commit com prefixo padronizado + push ao fim de cada subetapa. Subetapas complexas podem rodar em sessões separadas (segurança + economia de tokens).

### Subetapa 02.0 — Leitura de Referências, Inclusão de Design e Revisão do Plano da Etapa 02 [Plan] [LLM: Opus]
Objetivo: aplicar a convenção `0X.0` (ver seção acima) ao início da Etapa 02 — reler `HANDOFF_BUILD.md` e as decisões da Etapa 01 contra o estado real do repositório e do banco; eliminar ideias divergentes vindas do CRM-Sindcom quanto a padrões de UI/UX frente ao novo design elaborado na etapa de Trasição 1 > 2; revisar este documento à luz do que a Etapa 01 efetivamente entregou (schemas, nomes, funções) em vez do que fora planejado. Conferir o fluxo de convite/funcionário e provisionar a construção das RPCs que fazem o convite funcionar de ponta a ponta (aceitar convite, nascer funcionário automaticamente, ligar/desligar o atributo profissional). 
Conclusão: divergências entre o planejado na Etapa 01 e o aplicado de fato resolvidas ou explicitamente registradas; subetapas 02.1–02.4 confirmadas ou ajustadas; design elaborado na Etapa de Transição 1 > 2 integrado ao novo plano para a Etapa 2; plano de ação da Etapa 02 apresentado a Max.
Qualidade: nenhuma subetapa 02.x referencia tabela/coluna/função com nome divergente do que foi realmente aplicado no Supabase.
Evidência: relatório curto de divergências (ou "nenhuma") + este documento atualizado, se necessário.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet na primeira; Opus na segunda.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — executada em 2026-08-18 (Opus, `[Plan]`, uma tentativa). **Nenhuma divergência de nomenclatura entre o planejado e o aplicado:** `list_migrations` devolveu exatamente as 22 migrations de `db/migrations/001`–`022`; a varredura de `information_schema.tables` nos 12 schemas bate 1:1 com `docs/02_MODELO_DE_DADOS.md` §3–§8 (`public` 7 tabelas, `access` 2, `licensing` 2, `aba_people` 10, `aba_catalog` 5, `aba_scheduling` 8, `aba_finance` 11, `aba_health` 7, `aba_sales` 3, `aba_automations` 8, `aba_ai` 4, `aba_messaging` 11); a varredura de `pg_proc` confirmou o inventário de funções documentado. O critério de Qualidade ("nenhuma subetapa 02.x referencia nome divergente") está satisfeito. **Nove gaps reais encontrados — nenhum é erro do que foi feito, todos são trabalho que nenhuma subetapa possuía:** (G1) `crm/src/index.css` ainda com a paleta neutra padrão do shadcn, marcada no próprio arquivo como "placeholder até `docs/04` fechar cor" — `docs/04` §5 fechou; (G2) `crm/src/lib/auth.tsx` e `app/RoleGate.tsx` ainda dizem "entra quando `access`/`public.profiles` existirem (Subetapa 01.2 em diante)", e `access.readable_modules()` nunca é chamada; (G3) nenhuma RPC do fluxo de convite existe em `pg_proc` — 834 linhas a portar do Maximus (018/019/074/075); (G3b) a criação de convite do Maximus vivia em route handler Next.js e não tem para onde ir numa SPA estática — resolvido como RPC com `pgcrypto`, ver `handoffs/instrucoes.md` §5; (G4) `access.modules.position` desalinhado da ordem ratificada (banco: `catalog`3/`messaging`6/`sales`7; wireframe: `sales`3/`catalog`6/`messaging`7); (G5) a antiga 02.4 exigia print de "todos os módulos" com só 3 construídos; (G6) `Propostas` e `Espera e encaixes` aparecem na sidebar do wireframe sem modelo de dados; (G7) `pg_cron` não instalado (`installed_version = null`) apesar de `docs/01` §2 declará-lo o agendador; (G8) `@tanstack/react-table`, biblioteca de drag-and-drop e `date-fns` ausentes de `crm/package.json`. **Duas decisões de Max nesta sessão:** (1) o bloco de convite/funcionário/atributo profissional vira **subetapa própria, antes de Pessoas** — é a resposta à pergunta que a Etapa de Transição 1→2 deixou explicitamente para a 02.0; (2) os módulos restantes ganham **CRUD completo antes do deploy**, cobrindo as 16 telas do design. A ressalva de que a decisão (2) contraria o princípio-guia ("MVP em uma semana") foi apresentada a Max antes da escolha e ele a manteve. Etapa 02 reescrita de 8 para 17 subetapas (02.0–02.16), com a varredura de segredos e o portão adversarial preservados como os dois últimos passos antes do HANDOFF.

> **Nota de referência (registrada na Subetapa 02.0):** o **CRM-Sindcom deixa de ser fonte de UI/UX** nesta Etapa. O design ratificado na Etapa de Transição 1→2 (`docs/04_DESIGN_E_MARCA.md` §5, 16 telas) é a única fonte de paleta, tipografia e padrão de componente. O Sindcom permanece referência legítima para **configuração de build (Vite/Vitest/PWA) e para o runbook de deploy FTP** (`docs/deploy.md` de lá, a reconsultar na Subetapa 02.13.a) — nada além disso.
>
> **Qualidade fixa de toda subetapa de tela (02.1–02.12), não repetida item a item:** (a) `.select()` sempre com colunas explícitas nas seis tabelas com narrowing de coluna — `select('*')` devolve `42501 permission denied for table`, que parece falha de RLS e desvia a investigação (`handoffs/instrucoes.md` §6); (b) todo conteúdo vindo do banco renderizado escapado — XSS armazenado é fronteira aberta por design e item obrigatório do portão adversarial da 02.15; (c) componentes conforme `docs/04_DESIGN_E_MARCA.md` §5.5; (d) nenhuma checagem de permissão duplicada no client — a régua continua sendo `access.can()`/RLS, o front só monta o que `access.readable_modules()` devolve.

### Subetapa 02.1 — Fundação visual e de sessão do app [Goal] [Manual] [LLM: Sonnet]
Objetivo: dar ao app a identidade visual e a sessão que a Etapa 01 deixou em placeholder declarado. Aplicar os tokens de `docs/04_DESIGN_E_MARCA.md` §5.2/§5.3 em `crm/src/index.css` como CSS variables (nunca hex solto em componente — `docs/04` §4); carregar `IBM Plex Sans`/`IBM Plex Mono`; reconstruir `crm/src/app/AppShell.tsx` no shell do wireframe (sidebar 236px + header 56px + faixa de breadcrumb mono + área de conteúdo `#f4f6f7`); fazer o `AuthProvider` (`crm/src/lib/auth.tsx`) resolver `public.profiles` (`account_id`, `account_role`, `full_name`) e o `RoleGate` usar `access.readable_modules()`; montar `crm/src/app/nav.ts` a partir dessa RPC; aplicar migration de `UPDATE access.modules SET position` para a ordem ratificada do wireframe; instalar as dependências de UI que o design exige (`@tanstack/react-table`, biblioteca de drag-and-drop, `date-fns`).
Conclusão: login leva a um shell com a paleta/tipografia ratificadas, sidebar montada dinamicamente na ordem `people→scheduling→sales→finance→health→catalog→messaging→automations→ai` + `Configurações/Suporte/Sair` fixos, nome do usuário real no header; trocar o `account_role` do usuário de teste no banco muda o conjunto de itens visíveis sem nenhum `if` de papel escrito no front.
Qualidade: `crm/src/app/nav.ts` **não contém lista de módulos hardcoded**; nenhum hex da paleta aparece fora de `index.css`; a migration de `position` é `UPDATE` puro, sem DDL. Os itens de navegação do wireframe sem modelo de dados (`Propostas` em `aba_sales`, `Espera e encaixes` em `aba_scheduling`) ficam **fora** da navegação do v01 — já constam do backlog de versionamento, não se inventa tabela para eles.
Evidência: print do shell autenticado ao lado da tela `1b` do wireframe + print do mesmo shell com um `viewer`, mostrando o conjunto reduzido de itens + `SELECT key, position FROM access.modules ORDER BY position` depois da migration.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). `crm/src/index.css`/`tailwind.config.js` reescritos com a paleta ratificada (`docs/04` §5.2/§5.3) convertida hex→HSL, IBM Plex Sans/Mono carregada via Google Fonts (mesma URL do `Shell.dc.html`); `AuthProvider` (`lib/auth.tsx`) resolve `public.profiles` (account_id/account_role/full_name) a cada mudança de sessão; `lib/access.ts` (hook `useReadableModules`) chama `access.readable_modules()` via `supabase.schema('access').rpc(...)`; `app/nav.ts` mapeia módulo→ícone/rota sem decidir *quais* módulos aparecem (isso é só o RPC); `AppShell.tsx` reconstruído no shell do wireframe (sidebar 236px/header 56px/breadcrumb mono/conteúdo `#f4f6f7`), com Configurações/Suporte/Sair fixos no rodapé da sidebar (não no header, correção de leitura mais precisa do `Shell.dc.html`) e o logo como link para `/` (o wireframe não lista Dashboard como item de nav). Busca global, notificações e botões "Período"/"Filtros" do header **ficaram de fora** por decisão desta subetapa — nenhum tem função ainda, e chrome decorativo sem função contraria a Qualidade fixa (d) das subetapas de tela. Migration `023_realinhar_navegacao_modulos.sql` (UPDATE puro) aplicada via MCP — `access.modules.position` confirmado na ordem ratificada por query direta. Instaladas `@tanstack/react-table` (^9.1.2 — v9 é a `latest` atual no registro npm, confirmado por `npm view`, não presumido de memória), `date-fns` e `@dnd-kit/{core,sortable,utilities}` (DnD ativamente mantido, substitui `react-beautiful-dnd` descontinuado). **Evidência:** print autenticado como `owner` (9 módulos, incluindo Prontuário) e como `viewer` (8 módulos, Prontuário ausente — `access.default_permission()` nega `health` a todo papel não-owner por padrão, sem override na conta de teste) — trocar o usuário mudou o conjunto de itens sem nenhum `if` de papel no front, confirmado também por `read_page` (árvore de acessibilidade) listando os `href` presentes em cada sessão. `typecheck` limpo; suíte de RLS 100/100 sem regressão; `get_advisors` sem achado novo (os 5 warnings são os já documentados no HANDOFF_BUILD). Itens de nav sem modelo de dados (`Propostas`, `Espera e encaixes`) confirmados fora da navegação, como decidido na 02.0.

### Subetapa 02.2 — Equipe: convite → funcionário → atributo profissional [Goal] [Manual] [LLM: Sonnet]
Objetivo: construir o fluxo de equipe de 5 passos verificado contra o CRM Maximus na Etapa de Transição 1→2 (`docs/01_ARQUITETURA.md` §7.4) e deferido desde as Subetapas 01.2/01.3. Portar do Maximus: `018_account_member_rpcs.sql` (`set_member_role`, `remove_account_member`, `transfer_account_ownership` — esta última prometida explicitamente pela correção A02 da Subetapa 01.8), `019_invitation_rpcs.sql` (`peek_invitation`/`redeem_invitation` → `peek_convite`/`resgatar_convite`, incluindo a limpeza da conta pessoal órfã, já que o `handle_new_user` do Vitrine também cria conta própria no cadastro), `074_employees_born_from_invitation.sql` (trigger de nascimento automático de `aba_people.funcionarios`) e `075_professionals_require_employee.sql` (`aba_scheduling.definir_profissional` + `CHECK profissionais_ativo_exige_funcionario`). Construir a aba **Equipe** da tela de Configurações (`1m`). Traduzir os nomes conforme `CLAUDE.md` §2/§14 — portar a lógica, nunca reescrever a permissão do zero.
Conclusão: owner cria convite pela UI e recebe o link uma única vez; um segundo usuário aceita o convite, é movido para a conta do owner com o papel certo, nasce automaticamente como `aba_people.funcionarios` ativo, e o owner liga/desliga o atributo profissional dele por um interruptor.
Qualidade: **peça sem equivalente no Maximus** — lá a criação do convite era feita por route handler Next.js (o servidor gerava e hasheava o token); aqui não há servidor, então vira RPC `criar_convite()` `SECURITY DEFINER` que gera o token com `pgcrypto` (schema `extensions`, já instalado) e devolve o texto em claro uma única vez, nunca legível depois. **Regra nova de Max (não existe no Maximus):** o atributo profissional só pode ser concedido a funcionário com `account_role = 'agent'` — `admin` nunca vira profissional ativo (`docs/01_ARQUITETURA.md` §7.3). Suíte de RLS própria em `crm/tests/rls/` provando os 5 passos, a regra nova, e que nenhum caminho novo permite a um `admin` se apossar da conta (regressão do achado A02 da 01.8). Toda função nova nasce com `REVOKE EXECUTE FROM PUBLIC` **e** `FROM anon`.
Evidência: print do fluxo ponta a ponta (convite criado → aceito por segundo usuário → funcionário aparece → atributo profissional ligado) + suíte de RLS 100% verde, incluindo os testes novos.
Esforço máximo do /goal: 5 tentativas (porte de 4 migrations + peça inédita)
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). Migration `024_equipe_convite_funcionario_profissional.sql` aplicada via MCP: Parte 1 porte 1:1 de `set_member_role`/`remove_account_member`/`transfer_account_ownership` (público, inglês — núcleo herdado); Parte 2 `criar_convite()`/`peek_convite()`/`resgatar_convite()` (token gerado e hasheado com `pgcrypto` dentro do banco, já que não há servidor Next.js para fazer isso como no Maximus); Parte 3 trigger `aba_people.nascer_funcionario_do_perfil()` (`AFTER INSERT`/`AFTER UPDATE OF account_id` em `public.profiles`) + `CHECK funcionarios_ativo_exige_login` + índice único `idx_funcionarios_profile_unico`, com backfill dos 4 perfis de teste pré-existentes; Parte 4 `aba_scheduling.definir_profissional()` + trigger `sincronizar_perfil_profissional()` (deriva `profile_id` do `funcionario_id`, nunca aceito como parâmetro) + `CHECK profissionais_ativo_exige_funcionario`, com a regra nova de Max (só `account_role='agent'`) checada no corpo. **Adaptação de esquema registrada:** o domain-check de `resgatar_convite` ("recusa se a conta já tem dado") não pode usar `aba_people.pessoas` — o próprio trigger da Parte 3 garante que toda conta nova já tem uma pessoa (o self-funcionário do dono), o que geraria falso positivo em 100% dos resgates; usa `leads`/`clientes`/`fornecedores` (nunca tocados pelo trigger) — ver `handoffs/instrucoes.md` §5. UI: aba **Equipe** em `/configuracoes` (lista de membros com papel/status de funcionário/profissional, formulário de convite mostrando o link uma única vez, botão liga/desliga profissional, seletor de papel) + página pública `/convite?token=...` (peek + cadastro/login + resgate). **Evidência:** fluxo completo verificado na UI real (Chrome) como `owner` — convite gerado com link copiável, atributo profissional ligado e desligado para `rls.agent` com a pill mudando em tempo real; o trecho "segundo usuário aceita" é coberto pela suíte automatizada (sessões isoladas de verdade, mais confiável que simular no mesmo navegador, que compartilha `localStorage` entre abas). Suíte de RLS nova (`crm/tests/rls/13_equipe_convite.spec.ts`, 10 testes) cobrindo os 5 passos, a regra nova, as recusas de segurança (conta com dado, já-membro-de-conta-compartilhada) e a regressão do achado A02 (admin não se apossa da conta pelo caminho novo). **5 arquivos de teste pré-existentes precisaram de ajuste** por causa das duas CHECKs novas em tabela compartilhada — ver `handoffs/instrucoes.md` §5. Suíte completa: **110/110 verdes**; `typecheck` limpo; `get_advisors` sem achado novo além dos já documentados (as novas funções `SECURITY DEFINER` aparecem no advisor exatamente como esperado — `peek_convite` para `anon`+`authenticated`, as demais só `authenticated`).

### Subetapa 02.3 — CRUD de Pessoas (`aba_people`) [Goal] [Manual] [LLM: Sonnet]
Objetivo: telas `1c` (lista unificada com tabs e contagem) e `1d` (ficha da pessoa) — listagem/criação/edição de lead → conversão para cliente via `aba_people.converter_lead()`, com tags/notas/campos customizados visíveis e persistentes através da conversão.
Conclusão: fluxo completo testado manualmente: criar lead → adicionar tag/nota → converter → tag/nota continuam visíveis no cliente.
Qualidade: UI segue os padrões de componente de `docs/04_DESIGN_E_MARCA.md` §5.5 (tabela paginada com seleção múltipla via TanStack Table, header mono uppercase, badge pill nas 4 famílias semânticas) — **não** os do CRM-Sindcom. A conversão passa pela RPC, nunca por `INSERT` direto em `clientes`.
Evidência: print do fluxo completo (lead com tag → cliente convertido com a mesma tag).
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). Nenhuma migration nova: `aba_people` já estava completo desde a Subetapa 01.2 (`converter_lead()` incluído) — 02.3 é 100% UI sobre o schema existente. `crm/src/features/people/api.ts` (hooks React Query: `usePessoas`/`usePessoa`/`useCriarPessoa`/`useConverterLead`/tags/notas/campos customizados, mesmo padrão de queries independentes combinadas no client já usado em `useEquipe.ts`, nunca embedding cross-tabela do PostgREST). `PessoasListPage.tsx` (tela `1c`): abas Todas/Leads/Clientes/Equipe/Fornecedores com contagem real, tabela paginada com seleção múltipla via `@tanstack/react-table` v9 (API nova, não a do v8 — ver `handoffs/instrucoes.md` §5), badge pill nas 4 famílias semânticas (`components/ui/badge.tsx`, novo), formulário "Nova pessoa" com papel selecionável (lead/cliente/fornecedor), exclusão em lote dando função real à seleção múltipla (com confirmação inline, nunca `window.confirm` — bloquearia automação de browser e é o padrão já rejeitado no projeto). `PessoaFichaPage.tsx` (tela `1d`, rota nova `/pessoas/:id`): cabeçalho editável, badges de todos os papéis simultâneos (uma pessoa pode ser Lead histórico + Cliente ao mesmo tempo, exatamente o ganho de `docs/02` §3.1), seletor de status por papel, botão "Converter em cliente" (some sozinho quando já convertido), abas Linha do tempo/Campos personalizados (reais) + Prontuário/Financeiro/Documentos (placeholder explícito — pertencem às Subetapas 02.8/02.9/backlog, mesmo espírito de `components/shared/Placeholder.tsx`), tags (anexar existente + criar nova, admin+ só na criação por `pt_tags_insert`), notas internas na coluna direita. Dois bugs reais encontrados e corrigidos na própria sessão, registrados em `handoffs/instrucoes.md` §5: (1) fallback de prioridade de papel classificava pessoa sem nenhum papel como "Lead" por engano (achado testando contra a fixture órfã `Funcionário Fictício 01.4`, confirmado por `execute_sql` direto no banco); (2) breadcrumb do `AppShell` (Subetapa 02.1) comparava rota por igualdade exata e caía em "core > dashboard" na sub-rota nova `/pessoas/:id` — corrigido na causa (`startsWith`), já cobrindo toda rota de detalhe que as Subetapas 02.4–02.12 vierem a criar. **Evidência (Chrome, `rls.owner`):** lead "Ana Beatriz Teste" criado pela UI → tag "VIP" criada e anexada → nota interna registrada → "Converter em cliente" → badges passam a mostrar Lead **e** Cliente simultaneamente, status "Ativa", tag "VIP" e nota continuam visíveis na ficha e na lista (coluna Tags), contagem de abas migra de Leads·2 para Clientes·1/Leads·1 corretamente. Seleção múltipla + exclusão em lote testada removendo a pessoa de teste. Campo personalizado ("Data de aniversário") criado, valor preenchido e confirmado persistido via `execute_sql` direto (`aba_people.pessoa_campos_customizados`), depois limpo do banco junto com a tag de teste. Suíte de RLS revalidada duas vezes (antes e depois da limpeza) — **110/110 verdes, sem regressão** (nenhuma migration tocada nesta subetapa); `typecheck` limpo; `get_advisors` (security) sem achado novo (os WARN/INFO existentes já são os documentados desde 01.8/02.2).

### Subetapa 02.4 — CRUD de Vendas (`aba_sales`) [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1f` — kanban de funil (`aba_sales.funis`/`etapas_funil`) com oportunidades arrastáveis, sempre ligadas a `pessoa_id`.
Conclusão: criar/mover/fechar oportunidade funcionando; `valor` e `etapa_id` refletidos corretamente no banco.
Qualidade: nenhuma oportunidade sem `pessoa_id`; `status` respeitando o ciclo `ativa → ganha | perdida` (`docs/02` §9).
Evidência: print do kanban com oportunidade movida entre etapas + query confirmando o estado.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). Nenhuma migration nova: `aba_sales` já estava completo desde a Subetapa 01.5 — 02.4 é 100% UI sobre o schema existente. `crm/src/features/sales/api.ts`: `useFunilPadrao()` (a conta trabalha com um único funil "Comercial padrão" no v01 — gerenciar múltiplos funis/etapas fica fora do Objetivo desta subetapa, sem demanda real ainda), `useCriarFunilPadrao()` (provisiona o funil com as 5 etapas do wireframe na primeira visita, estado vazio explícito em vez de dado inventado silenciosamente), `useOportunidades`/`useCriarOportunidade`/`useMoverOportunidade`/`useFecharOportunidade` — as duas últimas com `.eq("status","ativa")` no `WHERE` do `UPDATE`, fechando o ciclo terminal do `docs/02` §9 (`ativa → ganha | perdida`, nunca reversível) sem precisar de `CHECK`/trigger novo no banco. `SalesKanbanPage.tsx` (tela `1f`): colunas = etapas do funil, cards arrastáveis via `@dnd-kit/core` (só oportunidades `ativa` são arrastáveis — `useDraggable({disabled: status !== "ativa"})`), atualização otimista no `onMutate` do `useMoverOportunidade` para o drag parecer instantâneo, `DragOverlay` durante o arrasto, ações "Ganha"/"Perdida" inline no card (com `onPointerDown` parando propagação para não iniciar drag ao clicar), formulário "Novo negócio" com seletor de pessoa (nunca `contact_id`, sempre `pessoa_id` — `docs/02` §4), estatísticas do cabeçalho (nº negócios, valor em aberto, ticket médio) calculadas só sobre oportunidades `ativa`. **Evidência (Chrome, `rls.owner`):** funil padrão criado pela UI (5 etapas na ordem certa, confirmado por `execute_sql`); negócio "Pacote 10 sessões" (R$ 2.400,00) criado na coluna "Novo contato"; arrastado via drag real do mouse para "Proposta enviada" — `etapa_id` refletido corretamente no banco; marcado "Ganha" — `status` mudou para `ganha`, badge verde substituiu os botões de ação, card parou de ser arrastável, e o "em aberto" do cabeçalho caiu para R$ 0,00 (estatística exclui negócio fechado). Estado final conferido via `execute_sql` direto (`titulo/valor/status/etapa/pessoa` batendo 1:1 com a UI) antes de remover o negócio de teste; funil padrão mantido (é configuração real da conta, não dado de teste descartável). Suíte de RLS revalidada sem regressão — **110/110 verdes** (nenhuma migration tocada); `typecheck` limpo.

### Subetapa 02.5 — Mensageria (Meta Cloud API) [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1j` — conversa em 3 painéis recebendo/enviando mensagem via Meta Cloud API real (conta de teste), sobre o webhook já no ar desde a Subetapa 01.6 (v3).
Conclusão: mensagem enviada do CRM chega no WhatsApp de teste; mensagem recebida aparece na conversa em tempo real.
Qualidade: nenhuma credencial de canal exposta no client (`aba_messaging.configuracao_whatsapp`/`provedores_canal` têm narrowing de coluna — o envio sai por Edge Function, não pelo browser); qualquer função de servidor nova reafirma `account_id` no filtro à mão (achado A06 da 01.8). **Nunca inventar número de telefone para teste de envio** (`handoffs/instrucoes.md` §6). Search-first na versão vigente da Graph API antes de escrever o envio.
Evidência: print da conversa nos dois sentidos.
Esforço máximo do /goal: 5 tentativas (integração externa tende a exigir mais ajuste)
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
Status: ⏸️ **EM ANDAMENTO — recebimento PROVADO, envio bloqueado por restrição de país da conta de teste da Meta (não concluída).** **Resultado final da sessão de 2026-08-18 (Opus) — leia este parágrafo antes do histórico abaixo, que registra duas hipóteses descartadas.** Com um token novo (o anterior estava morto — cada "Gerar token" no painel invalida o antecessor, e o campo exibia "⚠️ Not generated yet", que é a explicação real do `#131005 Access denied`), a subetapa foi exercitada ponta a ponta: **(1) RECEBIMENTO PROVADO COM MENSAGEM REAL** — Max escreveu do WhatsApp dele para o número de teste, `eventos_provedor` gravou o payload real (`wa_id 553599956058`, `profile.name "Maxwell Ribeiro"`), o webhook fez upsert do contato com o nome do perfil, abriu a conversa, gravou a mensagem (`tipo_remetente=cliente`, `status=entregue`) e a UI listou tudo com a pill "Janela de 24h aberta" correta — **a segunda metade da Conclusão está satisfeita**; **(2) CADEIA DE ENVIO DO CRM PROVADA** — preflight CORS → `POST` autenticado → papel checado → conversa/contato resolvidos → token decifrado → Graph API v26.0 → **`wamid` real devolvido** → `POST | 200`, e o webhook ainda recebeu o evento de status e **atualizou a mensagem para `falhou` no banco** (o caminho protegido pelo achado A06 da 01.8, funcionando ao vivo); **(3) ÚNICO BLOQUEIO RESTANTE, EXTERNO** — a entrega ao aparelho falha com **`130497 — Business account is restricted from messaging users in this country`**, e a prova de que não é código nosso é que **o botão "Enviar mensagem" do próprio painel da Meta falha com o mesmo erro**. Sair disso é conta/verificação de empresa na Meta. **(4) NONO DÍGITO BRASILEIRO — ✅ CORRIGIDO na mesma sessão:** a Meta devolve `wa_id` com 12 dígitos e a lista de permitidos guarda 13, então enviar com o `wa_id` cru batia em `131030 — Recipient phone number not in allowed list`. Desenho adotado: **`wa_id` é a forma canônica no banco** (chave que casa contato do webhook com contato criado à mão) e a conversão para a forma discável acontece **só na borda de saída** (`normalizarTelefoneParaEnvio` no `whatsapp-enviar` v4), com a conversão inversa na entrada manual (`paraFormaWaId` em `features/messaging/api.ts`) para a mesma pessoa não virar dois contatos — erro que de fato ocorreu antes da correção. Lógica validada contra 7 casos (celular BR nas duas formas, fixo BR, celular/fixo SP, número dos EUA, entrada com máscara) com ida-e-volta estável. **Prova ao vivo:** log `destino canonico=553599956058 normalizado=5535999956058`, o `131030` desapareceu, a Meta passou a aceitar o destinatário e devolver `wamid`, e o único erro restante é o `130497` externo. Detalhe completo em `handoffs/instrucoes.md` §5, primeira entrada.

**DECISÃO ESTRATÉGICA DE MAX (2026-08-18):** a verificação da empresa junto à Meta corre **em paralelo**, conduzida manualmente por ele, enquanto o CODE segue construindo as demais subetapas. A expectativa é que, ao chegar ao portão de saída da Etapa 02, o número de produção já esteja aprovado — e aí a 02.5 fecha com número real e validado, sem `130497`. **Consequência prática para o roteiro: a 02.5 não bloqueia a 02.6 nem as seguintes.** Ela reabre para fechamento quando a Meta aprovar, e o que faltará é só reconectar com a credencial de produção e refazer a evidência de envio — todo o resto já está construído, no ar e provado.

**Histórico da investigação (duas hipóteses descartadas — preservadas pelo método, não pela conclusão):**
Status anterior: ⏸️ **EM ANDAMENTO — BLOQUEADA POR DEPENDÊNCIA EXTERNA (não concluída).** Sessão de 2026-08-18 (Sonnet, escalada para Opus no diagnóstico). **Construído e no ar:** `supabase/functions/whatsapp-configurar` (v4) e `whatsapp-enviar` (v3) — verificação da credencial contra a Meta antes de salvar, cifra AES-256-GCM do token com `ENCRYPTION_KEY` (formato de 3 partes `iv:ct:tag` portado 1:1 de `CRM_Maximus/src/lib/whatsapp/encryption.ts`, reimplementado em Web Crypto porque Deno não tem o `crypto` do Node), `account_id` reafirmado em todo filtro (achado A06 da 01.8), papel checado no corpo (owner/admin para conectar, agent+ para enviar), CORS explícito. `crm/src/features/messaging/{api.ts,MessagingPage.tsx}` — tela `1j` em 3 painéis, Realtime já habilitado desde `020_aba_messaging.sql` (nada novo precisou ser ligado), janela de 24h calculada a partir da última mensagem do cliente e travando o compositor quando fechada. `typecheck` limpo; nenhuma migration nova. **Configuração externa concluída com Max, passo a passo, no painel da Meta** (ele fez todo copiar/colar de credencial; eu nunca toquei em segredo): número de teste reivindicado, número pessoal dele verificado como destinatário, URL de callback + verify token salvos com handshake `200`, campo `messages` assinado em v26.0, `subscribed_apps` devolvendo `{"success":true}`, `configuracao_whatsapp` gravada com `status='conectado'` e token cifrado (632 chars). **BLOQUEIO:** nenhuma mensagem real chega ao webhook — `eventos_provedor`/`contatos_canal`/`conversas`/`mensagens` seguem em 0 e o log não registra **nenhum** `POST` da Meta, embora a mensagem saia do WhatsApp de Max com dois tiques. **Causa raiz identificada e confirmada por *search-first*:** o app da Meta está em modo **"Em desenvolvimento" / não publicado**, e nesse modo a Meta **não despacha webhook de dado de produção** — nem para administrador, desenvolvedor ou testador do app; só entrega payload de teste disparado do próprio painel. Não é defeito do Vitrine: handshake, assinatura HMAC, CORS e resolução de conta estão corretos e provados. Sair do bloqueio depende de **publicar o app**, que por sua vez exige a "Etapa 3 — Verificação da empresa" da Meta (documentos + prazo de dias, bloqueio de calendário, não técnico). Dois achados registrados em `handoffs/instrucoes.md` §5/§6 (o bloqueio em si, com os três caminhos de saída mapeados; e o CORS obrigatório em Edge Function chamada do browser), mais um **gap secundário a resolver junto**: a `MessagingPage` só lista conversas nascidas do webhook — não há caminho de "iniciar conversa nova", então o envio (que funciona em dev mode para destinatário verificado) fica sem por onde ser exercitado na UI. **A subetapa reabre em sessão nova, com a decisão de Max sobre qual caminho seguir.**
**Atualização na mesma sessão — Plano B executado (Opus), recebimento provado e segundo bloqueio isolado.** Por decisão de Max, seguiu-se o caminho (B). **O caminho de recebimento está provado contra a infraestrutura real da Meta:** o botão "Teste" do campo `messages` no painel dispara um `POST` assinado com o `META_APP_SECRET` verdadeiro, e o log registra a cadeia completa — `requisição ACEITA — assinatura válida` (21:44:56) → `nenhuma conta configurada para phone_number_id=123456123 — descartado` (o payload de exemplo traz placeholder, e o bloco é somente-leitura) → `POST | 200`. Isso eleva a prova de assinatura HMAC de "contra o nosso próprio script" para "contra a assinatura real da Meta", e mostra a resolução de conta funcionando fail-closed; o único elo não exercitado (match do `phone_number_id` → grava conversa) já é coberto pelas 15 asserções de `crm/scripts/test_webhook_meta.mjs` da Subetapa 01.6. **Gap secundário fechado:** `useIniciarConversa()` + formulário "+ Nova" na lista — abre conversa a partir de um telefone digitado (normalizado para o formato `wa_id` só-dígitos, reaproveitando conversa não-fechada pelo mesmo critério do webhook, para a mesma pessoa não virar dois contatos). A janela de 24h deixou de **bloquear** o compositor e passou a apenas **sinalizar**: quem decide a janela é a Meta a partir do histórico dela, e o nosso cache local pode estar incompleto — travar por ele impediria um envio que a Meta aceitaria. **Segundo bloqueio, irmão do primeiro:** com isso, o envio real foi exercitado e **toda a cadeia do CRM funcionou** (preflight `OPTIONS 200` → `POST` → papel checado → conversa/contato resolvidos → **token decifrado com sucesso** → chamada à Graph API v26.0), mas a Meta recusou com **`(#131005) Access denied — "There was a problem with the access token or permissions you are using for the API call."`**. O mesmo token passa na leitura (`GET /{phone_number_id}`, `200` às 21:18:56) e falha no envio (`POST /messages`) — permissões distintas (`whatsapp_business_management` vs. `whatsapp_business_messaging`), ou seja, **não é token expirado nem cifra errada, é concessão de permissão do app**, mesma família do bloqueio de publicação. **Próximo passo, barato e decisivo, para a sessão nova:** usar o botão "Enviar mensagem" da própria "Etapa 1. Experimente" do painel — se ele também falhar, nenhuma credencial desse app envia hoje e o problema é 100% externo; se funcionar, a diferença está no token guardado e vale investigar do nosso lado. **Três achados de engenharia registrados em `handoffs/instrucoes.md` §5** (o teste de webhook pelo painel como técnica permanente; `form_input` não dispara `onChange` do React em input controlado — usar `left_click`+`type`; `h-full` não resolve dentro do `main` do `AppShell`, exige altura explícita — conferir na 02.6). `typecheck` limpo em todas as rodadas.

### Subetapa 02.6 — Agenda (`aba_scheduling`) + perfis de UI reduzida [Goal] [Manual] [LLM: Sonnet]
Objetivo: telas `1e` (grid semana × hora, bloco por profissional), `1n` ("Meu dia", perfil profissional) e `1o` ("Balcão", perfil recepção). CRUD de agendamento sobre `aba_scheduling.agendamentos`/`agendamento_servicos`, respeitando a restrição de exclusão por intervalo e a verificação de expediente já no banco.
Conclusão: criar/editar/cancelar agendamento funcionando; sobreposição de horário recusada pelo banco e tratada na UI com mensagem legível; `1n` e `1o` renderizando o subconjunto correto para cada perfil.
Qualidade: `1n`/`1o` são **filtro de UI sobre o RBAC existente**, nunca papel novo — `agent` + atributo profissional / `admin` sem o atributo (`docs/01_ARQUITETURA.md` §7.3), montados por `access.readable_modules()`. Toda comparação de horário no fuso configurado da conta (`aba_scheduling.fuso_horario_conta()`) — agenda que ignora fuso quebra em silêncio. O erro `23P01` (sobreposição) e o `23514` (fora de expediente) precisam ser distinguidos na mensagem ao usuário.
Evidência: print da agenda semanal + print de `1n` e `1o` com usuários reais dos dois perfis + print da recusa de sobreposição.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). **Nenhuma migration nova**: `aba_scheduling` já estava 100% aplicado desde a Subetapa 01.3 (`fuso_horario_conta()`, restrição de exclusão por intervalo em `agendamentos`/`agendamento_servicos`, trigger `verificar_expediente_agendamento` com `RAISE EXCEPTION ... USING ERRCODE = '23514'` distinguindo expediente/folga/meia-noite/recurso) — 02.6 é 100% UI sobre o schema existente, mesmo padrão de 02.3/02.4. `crm/src/features/scheduling/api.ts` (hooks React Query: `useProfissionais`/`useMeuProfissional`, `useHorariosProfissional`/`useDefinirExpedientePadrao`/`useProfissionaisSemJornada` — jornada padrão seg–sáb 09h–18h criada sob demanda, mesmo espírito de "estado vazio explícito" da `useCriarFunilPadrao` da 02.4, porque a verificação de expediente falha fechada sem nenhuma linha cadastrada —, `useRecursos`/`useServicos` (leitura só), `useClientesParaSelecao` (agendamentos.cliente_id referencia `aba_people.clientes`, nunca pessoa em geral), `useAgendamentosIntervalo`/`useCriarAgendamento`/`useAtualizarStatusAgendamento`/`useCancelarAgendamento`/`useCriarAusencia`, e `mensagemErroAgendamento()` — distingue `23P01` (sobreposição) de `23514` (expediente/folga/meia-noite/recurso, por substring do texto de `RAISE EXCEPTION`) em mensagem legível, conforme a Qualidade desta subetapa. Três telas: `AgendaSemanaPage.tsx` (`1e`, CRUD completo — grade semanal com blocos posicionados por minuto real dentro de janela 07h–20h, "Novo atendimento", "Bloquear horário" sobre `ausencias`, painel de detalhe com transições de status e cancelamento), `MeuDiaPage.tsx` (`1n`, agenda do dia do profissional logado com iniciar/concluir atendimento e produção do mês) e `BalcaoPage.tsx` (`1o`, chegadas do dia + check-in + atalho para "Novo agendamento"/"Cadastrar pessoa"/"Abrir conversa no WhatsApp"). `AgendaPage.tsx` decide entre as três em `/agenda` sem rota nova, só filtro de UI (docs/01_ARQUITETURA.md §7.3): `agent`+atributo profissional ativo → `1n`; `admin` sem o atributo → `1o`; qualquer outro caso (owner, agent sem atributo, viewer) → `1e` completa — decisão desta subetapa, já que o wireframe não mapeia `owner` para nenhuma das duas telas reduzidas. **Duas adaptações de escopo registradas**: grade em 7 colunas (Seg–Dom), não as 6 do wireframe (Seg–Sáb), para não esconder agendamento real de domingo que o banco aceitaria; `1o`/`1n` substituem "Sala de espera"/"Encaixes possíveis" (backlog "Lista de espera e encaixe", já fora do v01) e "Caixa do dia" (Subetapa 02.8) por notas explícitas em vez de dado inventado, mesmo padrão das abas placeholder da ficha de pessoa na 02.3. **Evidência (Chrome, conta de teste real):** como `rls.owner` — pessoa-cliente "Marina Teste" criada, atributo profissional ligado em `rls.agent` via Equipe, jornada padrão definida pelo aviso "sem jornada cadastrada", atendimento 09:00–10:00 criado e visível na grade; segunda tentativa no mesmo horário recusada com **"Conflito de horário: o profissional (ou a sala) já tem outro atendimento nesse intervalo."** (`23P01`); tentativa às 20:00 (fora da jornada 09–18) recusada com **"Fora do expediente cadastrado para o profissional nesse horário — cadastre a jornada dele antes de agendar."** (`23514`); horário bloqueado ao meio-dia via "Bloquear horário" e nova tentativa nesse intervalo recusada com **"O profissional está de folga nesse período."** (`23514`, mensagem distinta da de expediente) — as três mensagens confirmadas literalmente diferentes entre si. Painel de detalhe: Confirmar → Iniciar atendimento → Cancelar testados, com o painel refletindo o novo status em tempo real (deriva de `agendamentos` fresco, não de cópia local). Como `rls.agent` (profissional): `1n` mostrou o atendimento do dia, "Iniciar"/"Concluir" funcionaram, "Em atendimento agora" e "Minha produção no mês" refletiram o estado real. Como `rls.admin`: `1o` mostrou "Chegadas de hoje" com contagem e status reais, "Check-in" funcionou, um segundo atendimento (15:00) criado pelo próprio Balcão e confirmado por Check-in. **Efeito colateral real observado e revertido**: concluir o atendimento disparou de verdade `aba_finance.ao_concluir_agendamento()` (trigger entre schemas já existente desde a 01.3), gerando uma linha em `lancamentos_comissao` — prova de que a integração scheduling→finance funciona ao vivo; removida na limpeza de dado de teste, junto com o agendamento, a ausência, a jornada e a pessoa "Marina Teste", e o atributo profissional de `rls.agent` desligado de volta ao estado inicial. Suíte de RLS revalidada **sem regressão da 02.6** — 106/108 testes executados verdes (mais 3 pulados); os 2 que falharam (`09_aba_messaging.spec.ts` e `12_adversarial_webhook.spec.ts`, ambos por `duplicate key value violates unique constraint "configuracao_whatsapp_account_id_key"`) são **pré-existentes, de fora do escopo desta subetapa** — a conta de teste tem uma configuração real de WhatsApp conectada desde a Subetapa 02.5, e o fixture desses dois testes tenta inserir uma segunda linha sem tratar o conflito; registrado em `handoffs/instrucoes.md` §5 para quem for mexer em `aba_messaging` a seguir. `typecheck` limpo; `get_advisors` (security) sem achado novo.

### Subetapa 02.7 — Catálogo (`aba_catalog`) [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1i` — CRUD de categorias, serviços, variantes de serviço e planos/itens de plano, com tabs e contagem.
Conclusão: criar serviço com variantes, definir a variante padrão via `aba_catalog.definir_variante_padrao()`, montar um plano com itens; tudo refletido no banco.
Qualidade: a variante padrão é definida pela função, nunca por `UPDATE` direto (a função é que garante a unicidade).
Evidência: print do catálogo populado + query mostrando serviço → variantes → variante padrão e plano → itens.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). **Nenhuma migration nova**: `aba_catalog` já estava 100% aplicado desde a Subetapa 01.3 (categorias/serviços/variantes_servico/planos/itens_plano, `definir_variante_padrao()` já embutida) — 02.7 é 100% UI sobre o schema existente. `crm/src/features/catalog/api.ts` (hooks React Query para as 5 tabelas + `useDefinirVariantePadrao()`, único caminho que chama a RPC via `.rpc("definir_variante_padrao", ...)`, nunca `UPDATE` direto em `variantes_servico.padrao`). `CatalogoPage.tsx` com abas reais "Serviços · N"/"Planos · N" (contagem verdadeira, não decorativa) e painel "Categorias" (criar + ativar/desativar). `ServicosTab.tsx` (cards em destaque dos 3 primeiros serviços ativos + tabela completa com coluna "Variante padrão" + painel de detalhe do serviço selecionado, listando variantes com botão "Definir padrão" e formulário de nova variante). `PlanosTab.tsx` (cards de plano + painel de detalhe com itens do plano — serviço + variante opcional + sessões — e remoção de item). **Adaptação de escopo registrada**: a tela `1i` do wireframe mostra abas "Insumos"/"Salas e recursos" e colunas "Comissão %"/"Vinculado a" (mapa clínico) sem tabela nenhuma em `aba_catalog` — mesmo padrão já aplicado a `Propostas`/`Espera e encaixes` na Subetapa 02.0/02.1: não inventar dado, ficam fora. A coluna "Vinculado a" foi substituída por "Variante padrão" (dado real, e diretamente relevante à Qualidade desta subetapa). **Evidência (Chrome, `rls.owner`):** categoria "Estética facial" criada; serviço "Limpeza de pele" criado nela (60 min, R$ 180) e refletido no card em destaque e na tabela; duas variantes criadas ("60 minutos" R$180/60min, "90 minutos" R$260/90min); "Definir padrão" acionado na variante de 90 minutos — badge "Padrão" migrou de nenhuma variante para essa, card em destaque passou a mostrar R$ 260,00/"padrão: 90 minutos", coluna da tabela também atualizou; confirmado via `execute_sql` que só a variante de 90 minutos tem `padrao=true` (a de 60 minutos virou `false` — troca atômica pela função, nunca dois `UPDATE`s soltos do client). Plano "Pacote 5 sessões" (R$ 1.200) criado, item adicionado (Limpeza de pele · variante 90 minutos · sessões) e confirmado em `itens_plano` via query. Dados de teste removidos ao final (`categorias`→`servicos`→`variantes_servico`→`planos`→`itens_plano`, ordem que respeita as FKs `RESTRICT`/`CASCADE`). Suíte de RLS revalidada sem regressão desta subetapa — 106/108 executados verdes (as 2 falhas restantes continuam sendo as pré-existentes da Subetapa 02.5, documentadas em `handoffs/instrucoes.md` §5, Subetapa 02.6). `typecheck` limpo; `get_advisors` (security) sem achado novo.

### Subetapa 02.8 — Financeiro (`aba_finance`) [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1g` — KPIs, gráfico de linha de 2 séries e tabs Lançamentos/Comissões/Conciliação sobre `contratos`/`faturas`/`pagamentos`/`planos_cliente`/`lancamentos_comissao`.
Conclusão: venda de plano pela UI via `aba_finance.vender_plano()`, fatura emitida, pagamento registrado dando baixa automática, saldo de plano e comissão refletidos; estorno de sessão via `estornar_sessao()` funcionando.
Qualidade: todo movimento financeiro passa pelas seis operações já no banco (`vender_plano`, `estornar_sessao`, `atualizar_status_fatura`, `marcar_faturas_vencidas`, `expirar_planos`, `planos_vencendo_em`) — nenhuma escrita direta em `saldos_plano`/`extrato_plano`, que são mantidos por trigger. `regras_comissao` continua restrita a `admin+`. Gráficos por SVG inline, sem biblioteca de chart (`docs/04` §5.5).
Evidência: print da tela populada + query da cadeia `contratos → clientes → pessoas` e do extrato de plano antes/depois de um estorno.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). **Nenhuma migration nova**: `aba_finance` já estava 100% aplicado desde a Subetapa 01.3 (`vender_plano()`, `estornar_sessao()`, `atualizar_status_fatura()`/`recalcular_valor_fatura()` (trigger), `marcar_faturas_vencidas()`, `expirar_planos()`, `planos_vencendo_em()`, mais os triggers `baixar_fatura_por_pagamento()` e `ao_concluir_agendamento()` já vindos da 01.3/02.6) — 02.8 é 100% UI sobre o schema existente. `crm/src/features/finance/api.ts`: hooks para as 5 tabelas da tela + `useResumoFinanceiro()` (KPIs, série de 6 meses Faturado×Recebido a partir de `faturas.data_emissao`/`pagamentos.pago_em`, formas de pagamento do mês — todas por agregação client-side de dado real, sem view nova no banco); `useVenderPlano()` cria `contratos`+`faturas`+`itens_fatura` por `INSERT` direto (fora das seis operações protegidas) e só então chama `vender_plano()` para gerar `planos_cliente`/`saldos_plano` — nunca escrita direta nessas duas tabelas, conforme a Qualidade; `useEstornarSessao()` chama só `estornar_sessao()`. `FinanceiroPage.tsx` (`1g`): 4 KPI cards, gráfico de linha 2 séries via SVG inline (`docs/04` §5.5, sem lib de gráfico) e abas `LancamentosTab`/`ComissoesTab`/`ConciliacaoTab`. `LancamentosTab` tem "+ Novo lançamento" com dois modos (fatura avulsa / venda de plano), painel de detalhe com "Registrar pagamento" e, quando a fatura tem plano vendido vinculado, a seção "Plano vendido" com saldo por serviço e "Estornar sessão". `ComissoesTab` não duplica checagem de papel no client — a RLS de `lancamentos_comissao` (`admin+`) já devolve conjunto vazio para quem não pode ver, mesmo padrão da Qualidade fixa da Etapa 02. **Achado real corrigido na sessão**: as mutações de venda/pagamento/estorno só invalidavam `["faturas"]`/`["planos-vendidos"]`, não `["resumo-financeiro"]` — os KPIs continuavam em R$0 depois de uma venda real até a página ser recarregada; corrigido adicionando a invalidação que faltava em `useCriarFaturaAvulsa`, `useVenderPlano`, `useRegistrarPagamento` e `useAtualizarStatusComissao`. **Adaptação registrada**: a Evidência original citava a cadeia `contratos → clientes → pessoas`, mas o desenho inicial de `useVenderPlano()` não passava por `aba_finance.contratos` — corrigido durante a própria sessão para criar o contrato como documento comercial "guarda-chuva" da venda antes da fatura, fechando a evidência pedida sem inventar uso fora do que a tela pede. **Evidência (Chrome, conta de teste real, ponta a ponta):** cliente "Bruno Teste" + categoria/serviço/plano "Pacote 5 sessões terapia" (R$900) criados; atributo profissional religado em `rls.agent` (reaproveitando o fluxo da 02.2/02.6) com regra de comissão de 30% inserida para evidência (`regras_comissao`, tabela restrita a `admin+`, sem UI própria nesta subetapa — fora do Objetivo); **venda de plano pela UI** → `contrato`+`fatura` (R$900, `aberta`) + `planos_cliente` (`ativo`) + `saldos_plano` (5 totais/0 usadas) confirmados via `execute_sql`; KPI "A receber" e o gráfico refletiram R$900 assim que corrigida a invalidação; **pagamento integral registrado** → trigger `baixar_fatura_por_pagamento()` mudou a fatura para `paga` sozinho (confirmado no banco, nunca setado pelo client) e o KPI "Recebido no mês" passou a R$900; **atendimento criado na Agenda consumindo o plano** (novo campo "Consumir sessão do plano" em `NovoAtendimentoForm`, com `usePlanosClienteAtivos()` novo em `scheduling/api.ts`) e concluído → `saldos_plano.sessoes_usadas` foi de 0 para 1 e `lancamentos_comissao` nasceu sozinho (R$200 base × 30% = R$60, `pendente`) via `ao_concluir_agendamento()`, sem nenhuma escrita do client nessas duas tabelas; **estorno pela UI** (`estornar_sessao()`) devolveu `sessoes_usadas` a 0, com `aba_finance.extrato_plano` mostrando os dois lançamentos exatos — delta `-1` "Consumo por atendimento concluído" e delta `+1` "Estorno manual de sessão" —, a evidência de extrato antes/depois pedida; **comissão aprovada e marcada paga** pela UI, confirmado `status='pago'`/`pago_em` preenchido no banco; **cadeia `contratos → clientes → pessoas`** confirmada por `execute_sql` (join `contratos.cliente_id → aba_people.clientes.id → aba_people.pessoas.id`, retornando nome e valor corretos). Todos os dados de teste (contrato/fatura/pagamento/planos_cliente/saldos/extrato/comissão/agendamento/jornada/profissional/categoria/serviço/plano/pessoa) removidos ao final, na ordem que respeita as FKs. Suíte de RLS revalidada sem regressão desta subetapa — 106/108 executados verdes (as 2 falhas restantes continuam sendo as pré-existentes da Subetapa 02.5, documentadas em `handoffs/instrucoes.md` §5). `typecheck` limpo.

### Subetapa 02.9 — Prontuário (`aba_health`) [Goal] [Manual] [LLM: Opus]
Objetivo: telas `1h` (prontuário e anamnese, tabs Anamnese/Evoluções/Anexos/Consentimentos) e `1p` (biblioteca de mapas clínicos). É a subetapa de maior risco jurídico da Etapa — `aba_health` tem regime próprio de RLS, mais restritivo, sem exceção por nenhum motivo (`CLAUDE.md` §5).
Conclusão: profissional com concessão lê e escreve prontuário pela UI; profissional sem concessão recebe negativa legível; toda leitura e toda escrita geram linha em `aba_health.log_acesso`.
Qualidade: **nenhuma leitura clínica por `select` direto na tabela** — só através de `ler_prontuario()`, `ler_evolucoes()`, `ler_respostas_anamnese()` e `ler_consentimentos()`, que gravam o log na mesma transação; anexos servidos pelo bucket privado `anexos-clinicos` via URL assinada, nunca por link público; consentimento de imagem trava a **exibição** da foto para todos, inclusive para quem a enviou (decisão de Max mantida — ver Pendências vigiadas). Os 4 mapas clínicos (odontograma/corporal/facial/acupuntura) são **placeholder SVG no wireframe e não têm arte de produção em lugar nenhum do repo** (`docs/04` §5.5): esta subetapa entrega a estrutura de seleção e persistência de marcação; a arte definitiva é asset novo, a decidir com Max, e não bloqueia a conclusão.
Evidência: print dos dois cenários (negado/permitido) + contagem de `log_acesso` antes/depois de uma leitura e de uma escrita pela UI.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim — mesmo tratamento da Subetapa 01.4/01.8.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Opus do início ao fim). **Primeira subetapa da Etapa 02 com migration nova desde a 02.1**: `db/migrations/025_aba_health_marcacoes_mapa.sql` acrescenta `mapa_tipo` (CHECK nos 4 mapas) e `marcacoes` (CHECK de array JSON) a `aba_health.evolucoes`. **A decisão de desenho foi coluna na evolução, não tabela nova**, e o motivo é de segurança, não de comodidade: tabela nova exigiria política de RLS própria, função `ler_*()` própria com log, trigger de escrita próprio e entrada própria na revogação por coluna — quatro superfícies novas no schema de maior risco jurídico do produto, para guardar um atributo que só existe dentro de uma sessão. Como coluna, a marcação herda tudo que já estava provado desde a 01.4 (RLS por `pode_acessar()`, log por trigger, leitura só por `ler_evolucoes()`, trava de evolução assinada). **Medido, não presumido** (`CLAUDE.md` §11): `has_column_privilege()` logo após a migration confirmou que as duas colunas nascem **sem `SELECT`** para `authenticated` e **com `UPDATE`** — idênticas a `avaliacao`/`anexos`/`registrado_em`, e portanto invisíveis a qualquer caminho que não seja a função com log. **UI:** `features/health/` com `api.ts` (as 4 funções `ler_*()`, escrita que nunca pede conteúdo de volta, anexo por `createSignedUrl` de 60s — contrato reconfirmado na documentação vigente antes de codificar), `mapas.ts` (catálogo fechado dos 4 mapas: 8 regiões faciais, 18 corporais, 32 dentes FDI, 7 pontos de acupuntura, com estados e cores por mapa), `MapaClinico.tsx` (um componente, quatro mapas, como o `1p` especifica), `ProntuarioPage.tsx` (tela `1h`), `MapasClinicosPage.tsx` (tela `1p`, que não lê prontuário nenhum e por isso não gera log), as 4 abas e `ConcessoesPanel.tsx`. **Gap real fechado nesta subetapa:** a Conclusão declarava "profissional com concessão lê e escreve pela UI", mas nenhuma tela **concedia** — o painel de concessões (portado de `grants-panel.tsx` do Maximus) é o que permite ao produto produzir o próprio cenário permitido, em vez de depender de SQL. **Achado real corrigido durante a evidência:** o mapa esvaziava assim que a sessão era assinada, quebrando o "histórico comparável entre sessões" que o próprio wireframe `1p` promete — corrigido para exibir a última sessão assinada daquele mapa em modo leitura. **Evidência (Chrome, `rls.owner`, ponta a ponta, dado de teste criado e removido):** cliente "Marina Teste 02.9" criado pela UI de Pessoas; `log_acesso` **0 linhas antes**; ficha clínica gravada → **2 linhas** (1 `criacao` por trigger + 1 `leitura` pela releitura via função); atributo profissional ligado pela aba Equipe (fluxo da 02.2), sessão aberta, Zona T marcada com nota, rascunho salvo (marcação confirmada no banco por `execute_sql`) e sessão assinada (`travada = true`) → **9 linhas** (`criacao` 2, `atualizacao` 2, `leitura` 5). **Cenário negado provado pela UI e contra o próprio proprietário**: concessão de efeito `negar` registrada pelo painel novo → a tela devolve a recusa legível e o log fica **15 → 15**, provando que nada foi lido nem registrado; revogada a negação, o acesso volta e o log vai a **17**. **Consentimento de imagem provado nos dois estados**: PNG enviado com sucesso e **exibição bloqueada para o próprio usuário que o enviou**; registrado o consentimento de uso de imagem, a mesma foto passa a ser servida por URL `/object/sign/` com token (assinada, TTL 60s) — o objeto confirmado no bucket com `public = false` e caminho exatamente na convenção `conta-<uuid>/cliente-<uuid>/<arquivo>`, resolvida por `aba_health.cliente_do_anexo()`. Suíte de RLS ampliada com **7 testes novos** (`05_aba_health.spec.ts`), incluindo a prova de que `marcacoes`/`mapa_tipo` devolvem `42501` no `select` direto **mesmo para o owner** — 113/117 executados verdes, as 2 falhas restantes são as pré-existentes da 02.5 (`configuracao_whatsapp`, `handoffs/instrucoes.md` §5). `typecheck` limpo; `get_advisors` (security) sem achado novo e **nenhum** em `aba_health`. Todo o dado de teste removido ao final (bucket incluído, pela Storage API — `storage.objects` recusa `DELETE` por SQL), contagem final zerada em todas as tabelas do schema.

### Subetapa 02.10 — Automações (`aba_automations`) + `pg_cron` [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1k` — lista de fluxos + editor de passos conectados verticalmente (cor por tipo: Gatilho azul / Condição sage / Ação tan) e, pela primeira vez, o **motor** que executa: instalar `pg_cron` e agendar a drenagem de `aba_automations.automacao_execucoes_pendentes`, a `aba_finance.marcar_faturas_vencidas()`, a `aba_finance.expirar_planos()` e o disparo de `aba_scheduling.lembretes`.
Conclusão: um fluxo criado pela UI dispara e deixa rastro em `fluxo_execucoes`/`fluxo_execucao_eventos`; um job de `pg_cron` roda no horário e é observável.
Qualidade: **search-first obrigatório** (`CLAUDE.md` §11) — confirmar na documentação vigente da Supabase como `pg_cron` é habilitado no plano em uso e em que schema a extensão deve morar (nunca `public`) antes de escrever a migration. Todo job roda com privilégio que **ignora RLS**: cada um reafirma `account_id` no `WHERE` à mão — a RLS não participa desse caminho (achado A06 da 01.8). `automacao_logs` continua só com `SELECT` para o usuário final (hardening da 01.5) — o log de auditoria do motor não é editável por quem ele audita.
Evidência: print do editor de fluxo + `SELECT * FROM cron.job` + linha de `fluxo_execucoes` gerada por execução real.
Esforço máximo do /goal: 5 tentativas (extensão nova + agendamento)
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa, executada em Opus (divergência do escalonamento declarado, que previa Sonnet nas 3 primeiras: a sessão estava em Opus e Max foi avisado no início). **Search-first rendeu antes da primeira linha de código** (`CLAUDE.md` §11): a documentação vigente prescreve `create extension pg_cron with schema pg_catalog` — **`pg_catalog`, não `extensions`**, contrariando a armadilha genérica de `handoffs/instrucoes.md` §6, que vale contra `public`. Medido depois de aplicar: `pg_extension` confirma `pg_catalog` e o schema `cron` criado pela própria extensão. **Migrations novas: `026_motor_automacoes_pg_cron.sql`** (motor: `executar_etapas()`, `executar_automacao()`, `drenar_execucoes_pendentes()`, `iniciar_fluxo()`, `avancar_fluxo()`, `expirar_fluxos_ociosos()`, `disparar_lembretes_vencidos()`, mais os cinco `cron.schedule`) e **`027_observabilidade_agendador.sql`** (`listar_jobs_agendados()` restrita a `admin+` e `listar_execucoes_pendentes()` com a fronteira de conta reafirmada à mão). **A pendência vigiada do `pg_cron`, aberta na Subetapa 02.0, está fechada**: as quatro rotinas órfãs têm agendador, mais a expiração de fluxo ocioso. **Disciplina do achado A06 da 01.8 aplicada em cada função:** todo job roda como `postgres`, sem RLS; a fronteira entre inquilinos é o `account_id` da própria linha de origem, repetido explicitamente em cada `INSERT` e em cada `WHERE` — nunca valor de sessão, nunca parâmetro aceito sem conferência. **Duas decisões de honestidade do motor:** (1) `enviar_whatsapp` é **registrada como `nao_executado`** com o motivo, porque o envio depende do canal da 02.5 — inventar um envio que não acontece seria pior que declarar a lacuna, e a tela mostra o aviso no próprio passo; (2) execução parada num `esperar` fecha o log como `parcial`, não `sucesso`, porque ela não terminou. **Achado real corrigido na mesma migration:** o estado novo `lembretes.status = 'pronto'` abria uma janela em que um atendimento cancelado **depois** de o lembrete ficar pronto ainda enviaria o aviso — `cancelar_lembretes_pendentes()` só alcançava `'pendente'`, e passou a alcançar os dois estados. **Evidência (Chrome, `rls.owner`, dado de teste criado e removido):** automação "Lembrete 24h antes" criada pela UI com gatilho `atendimento_concluido` e três passos (condição com ramos sim/não, aplicar tag, enviar WhatsApp); botão "Testar" executou e o log gravou as três etapas com resultado honesto (`condicao` → executado; `definir_tag` → ignorado, "execução sem pessoa associada"; `enviar_whatsapp` → `nao_executado`, com o motivo). Fluxo conversacional "Boas-vindas ao lead novo" criado, ativado e disparado pela UI → **`fluxo_execucoes` `concluida`, motivo "fim do fluxo", 5 linhas em `fluxo_execucao_eventos`** (`iniciado`, `no_visitado`×3, `concluido`), confirmado por `execute_sql`. **`SELECT * FROM cron.job` devolve os 5 jobs ativos**, e `cron.job_run_details` prova corridas reais (`drenar-execucoes-pendentes` às 07:12, 07:13, 07:14, todas `succeeded`) — o painel do agendador mostra isso dentro do produto. Suíte ampliada com **13 testes novos** (`14_motor_automacoes.spec.ts`), verdes de primeira. **Investigação registrada:** a suíte completa passou a falhar em módulos não tocados, com o sintoma clássico de RLS quebrada (conjunto vazio); a causa real, **medida nos `edge_logs`**, é `429` no `/auth/v1/token` — rate limit de autenticação do Supabase, disparado por rodar a suíte quatro vezes em poucos minutos (cada `clientAs()` faz login novo; ~40 aceitos e 21–34 recusados por minuto). Cada arquivo passa isolado. **Não é regressão desta subetapa** — registrado em `handoffs/instrucoes.md` §5, com o procedimento de dois minutos que distingue falha de autenticação de falha de autorização, e em §7 como candidato de infraestrutura (cachear token por papel), relevante antes da Subetapa 02.15, que reexecuta a suíte muitas vezes. **Linha de base final, medida depois de oito minutos sem autenticar: 124/130 verdes, 5 pulados e 1 falha — a pré-existente de `configuracao_whatsapp` da Subetapa 02.5.** `05_aba_health` passa 15/15 isolado, confirmando que sua aparição na lista de falhos era o mesmo efeito do `429`, não regressão da 02.9. `typecheck` limpo; `get_advisors` (security) sem achado novo.

### Subetapa 02.11 — IA (`aba_ai`) [Goal] [Manual] [LLM: Sonnet]
Objetivo: tela `1l` — métricas de uso, card de comportamento do agente (modelo/horário/4 interruptores de permissão), base de conhecimento e transferências, sobre `aba_ai.ia_configuracoes`/`ia_documentos_conhecimento`/`ia_trechos_conhecimento`/`ia_log_uso`.
Conclusão: conta cola a própria chave de IA pela UI, o agente responde uma mensagem de teste e o consumo aparece em `ia_log_uso`; busca na base de conhecimento devolve trecho relevante via `aba_ai.buscar_conhecimento_textual()`.
Qualidade: **bring-your-own-key** — nenhuma chave global de LLM no `.env` do projeto; `ia_configuracoes.chave_api` gravada criptografada com `ENCRYPTION_KEY` (AES-256-GCM) por Edge Function, nunca em texto puro e nunca legível de volta pelo client (a coluna tem narrowing desde a migration 022). Busca semântica por `pgvector` continua **fora do escopo** (backlog `+1.0`) — só o caminho lexical.
Evidência: print da tela + linha de `ia_log_uso` de uma chamada real + prova de que `select` da chave por `authenticated` devolve `42501`.
Esforço máximo do /goal: 5 tentativas (integração externa)
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA COM RESSALVA DECLARADA — fechada em 1 tentativa, em Opus (por escolha de Max; o escalonamento previa Sonnet nas 3 primeiras). **Migrations novas:** `028_aba_ai_permissoes_agente.sql` (4 colunas de permissão + `horario_atuacao` + `aba_ai.resumo_uso_ia()`) e `029_corrigir_busca_conhecimento.sql`. **Edge Functions novas, ambas implantadas e ACTIVE:** `ia-configurar` (verifica a chave contra o provedor **antes** de gravar, cifra em AES-256-GCM no mesmo formato de `whatsapp-configurar`, devolve só os 4 últimos caracteres) e `ia-responder` (decifra, recupera conhecimento, chama o provedor, grava `ia_log_uso`). **Bring-your-own-key preservado**: nenhuma chave de LLM no `.env` do projeto; `chave_api` confirmada ilegível para `authenticated` por medição (`has_column_privilege` = false) mesmo depois das colunas novas. **DECISÃO DE SEGURANÇA — o interruptor "Ler prontuário" do wireframe `1l` nasce travado por CHECK no banco (`ia_configuracoes_prontuario_sempre_negado`), não apenas ausente da UI:** o agente lê com `service_role`, que ignora RLS, portanto leria `aba_health` sem passar por `pode_acessar()` e sem gravar `log_acesso` — quebrando de uma vez as duas garantias construídas na Subetapa 02.9 e o `CLAUDE.md` §5 ("nenhuma exceção, por nenhum motivo"). Provado nos testes que nem `owner` nem `service_role` conseguem ligá-lo (23514). Os outros três interruptores são ligáveis e viram instrução explícita no prompt de sistema — o que é declarado como tal, porque instrução não é trava. **Dois defeitos reais encontrados e corrigidos:** (1) `buscar_conhecimento_textual()` usava `plainto_tsquery`, que liga os termos com **AND** — medido no banco: a pergunta "quanto custa a limpeza de pele" **não** recuperava o trecho "A limpeza de pele profunda custa 180 reais…", porque ele não contém a palavra "quanto"; com a config `'simple'` (sem remoção de palavra vazia) o problema é sistemático, e o sintoma — zero trecho — é plausível demais para levantar suspeita. Migration 029 passa a ligar por OR sobre a saída **já escapada** de `plainto_tsquery` (preservando a proteção contra injeção de operador da 01.5), com `ts_rank` ordenando e piso de relevância. (2) `functions.invoke` esconde o motivo do erro em `error.context` (um `Response` não lido), não em `error.message`: a tela mostrava "Edge Function returned a non-2xx status code" em vez de "API key is invalid" — as três causas que o operador precisa distinguir (chave errada, sem crédito, provedor fora do ar) chegavam idênticas. **Evidência:** print da tela `1l`; **chave falsa recusada com a mensagem real do provedor e nada gravado no banco** (`ia_configuracoes` = 0 linhas depois da tentativa), provando que a verificação prévia funciona; `select` de `chave_api` devolvendo `42501` para o próprio `owner`, e `select('*')` falhando pelo mesmo narrowing; busca de conhecimento devolvendo o trecho certo por pergunta em linguagem natural e vazio para pergunta sem correspondência; isolamento entre contas provado passando `p_account_id` alheio (devolve vazio). **RESSALVA — a chamada real ao provedor não foi exercida:** a Conclusão pedia "o agente responde uma mensagem de teste e o consumo aparece em `ia_log_uso`", o que exige uma chave de IA válida; por desenho *bring-your-own-key* o projeto não tem chave própria, e o CODE não insere credencial em campo por regra de segurança. **Max optou (2026-08-19) por fechar sem a chamada real** — todo o caminho está construído e implantado, e o único trecho não exercido é a resposta do provedor com chave válida. Registrado como pendência vigiada. Suíte ampliada com **13 testes novos** (`15_aba_ai_agente.spec.ts`), 13/13 verdes; `typecheck` limpo; `gitleaks` `no leaks found`.

### Subetapa 02.12 — Dashboard + Configurações [Goal] [Manual] [LLM: Sonnet]
Objetivo: telas `1b` (dashboard geral: 4 KPI cards, barra semanal, donut de serviços, 3 painéis) e `1m` (configurações da conta: nav de 9 seções, grid de módulos ativos, seletor de 4 templates de layout). A aba **Equipe** de `1m` já veio da Subetapa 02.2 — aqui entram as demais seções.
Conclusão: dashboard lendo números reais do banco (não mock) e configurações permitindo editar o que é legitimamente configuração de conta.
Qualidade: KPI e gráfico saem de query real; o seletor de templates de layout materializa visualmente as 4 opções de `docs/04_DESIGN_E_MARCA.md` §2 — no v01 pode entregar **um único template ativo** com os demais desabilitados (múltiplos templates é item `+1.0` do backlog, não pré-requisito de lançamento, `docs/04` §4). `accounts.owner_user_id` não é editável por formulário nenhum — a transferência de titularidade só pela RPC da 02.2 (trava da correção A02 da 01.8).
Evidência: print do dashboard com dados reais + print das configurações.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em Opus (escolha de Max), dentro do teto de 4 tentativas. **Migrations novas:** `032_preferencias_de_conta.sql` (`public.account_preferences` — tema, densidade, cor de destaque, tipografia e template por conta) e `033_matriz_de_permissoes.sql` (`access.matriz_permissoes()`, leitura da matriz papel × módulo × ação já resolvida pelo banco). **Nenhum número do dashboard é literal** — os quatro KPIs, o gráfico de 12 semanas, o donut de 90 dias e os três painéis saem de consulta, e cada card declara a janela de tempo em texto. **Três decisões que mudaram o desenho, todas por medição e não por leitura:** (1) **`access.can()` devolve `TRUE` para `owner` antes de consultar `access.module_permissions`** (medido em `pg_get_functiondef`), então o interruptor de módulo do wireframe `1m` gravaria a linha e **não esconderia nada para quem clicou** — Max decidiu (2026-08-19) entregar o grid em leitura, com o atalho declarado na tela, e a edição real na seção "Perfis e permissões"; (2) **contador de ausência sobre `aba_health` erra para CIMA** — "clientes sem anamnese" conta o que não vê, e quem não tem alcance clínico veria o número **máximo**, com cara de dado real; resolvido sondando `aba_health.pode_acessar(NULL,'leitura')` antes de contar e mostrando "sem alcance clínico" no lugar do número; (3) **profissional sem grade** entrava no numerador da taxa de ocupação sem entrar no denominador, inflando a taxa da clínica — agora sai dos dois lados e aparece como "sem grade", nunca como 0%. **Template único virou trava de banco, não de UI:** `layout_template` tem CHECK de valor único (`fixed_sidebar`), provado recusando `top_nav` com `23514` **inclusive para `service_role`** — os outros três templates aparecem na tela, desabilitados, com o motivo escrito. **`accounts.owner_user_id` sem formulário em lugar nenhum**, com o trigger `enforce_account_privilege_columns` reafirmado por teste. **Defeito real encontrado fora do escopo e corrigido:** o KPI "Vencido" do Financeiro (02.8) filtrava `status IN ('aberta','enviada')` e o job diário da 02.10 reescreve para `'vencida'` — **medido:** R$ 960,00 em atraso viravam **R$ 0,00** assim que a rotina rodava, sem erro nenhum na tela. **Modo escuro passou a existir**, derivado da paleta ratificada (nenhuma variante dark havia sido ratificada por Max — está declarado como derivação); o `.dark` herdado do shadcn não redefinia `--success`/`--warning`/`--destructive-tint`, o que deixaria as quatro famílias de badge ilegíveis, e as cores de série cravadas em hex no gráfico da 02.8 foram tokenizadas. **Escopo ampliado por decisão explícita de Max:** a tabela de preferências antecipa parte do item `branding` do backlog `+1.0`; o upload de logomarca ficou de fora (exige bucket de Storage por conta) e aparece desabilitado com o motivo. **Evidência:** print do dashboard com os quatro KPIs conferidos contra `execute_sql` (9 atendimentos hoje vs. 6 no mesmo dia da semana anterior = +50%; 10 leads vs. 5 = +100%; ocupação 50% vs. 33% = +16,7 p.p.; R$ 2.535 vs. R$ 1.299 = +95,2%), prints das Configurações em claro e escuro, ida e volta de tema/cor pela própria tela conferida no banco. **Dado de teste removido por completo** — a conta voltou a 14 pessoas, 0 agendamentos, 0 faturas, 0 profissionais, exatamente como estava. Suíte ampliada com **13 testes novos** (`16_configuracoes_preferencias.spec.ts`), 13/13 verdes; `typecheck` limpo; `npm run build` verde; `gitleaks` `no leaks found`; advisor de segurança sem achado novo. **Suíte completa: 163 verdes, 3 pulados, 1 falho de 167** — a única falha é a de `configuracao_whatsapp` herdada da 02.5, cuja causa foi identificada aqui e virou o item (b) da Subetapa 02.13.b. **A verificação precisou ser feita em 5 lotes com ~5,5 min entre eles**, porque a suíte deixou de fechar em execução única — ver a pendência vigiada correspondente; o lote é contorno de verificação desta subetapa, não conserto.

### Subetapa 02.12b — Correções da revisão visual do tema escuro [Goal] [Manual] [LLM: Opus]
Objetivo: fechar os achados da revisão tela a tela que Max fez do modo escuro entregue na 02.12 (evidências em `screenshots/01`–`08`), mais os itens de navegação que ela revelou.
Conclusão: nenhum campo de formulário ilegível em nenhuma das 16 telas; caminho funcionando como navegação real; anamnese incompleta impossível de gravar.
Qualidade: correção na causa, não na tela onde o sintoma apareceu — Max relatou 3 telas com campo ilegível, e a medição mostrou **87 dos 103 campos do app** sem o token de fundo; consertar as 3 deixaria as outras 13 erradas e as próximas nascendo erradas. Invariante clínica vale no banco, não só no formulário.
Evidência: varredura instrumentada de 29 campos em 13 rotas no modo escuro, zero com fundo claro; os quatro painéis do prontuário medindo o mesmo (259px); caminho navegando de `/prontuario/:id` para `/prontuario`; `23514` ao tentar gravar anamnese com 3 de 5 respostas.
Status: ✅ CONCLUÍDA. **Migration nova:** `034_anamnese_completa_obrigatoria.sql`. **Sete achados fechados:**
1. **Campo de formulário ilegível no escuro** — fundo branco do navegador com texto claro. Corrigido em **camada base** (`input, select, textarea` recebem o token; `option` idem; `color-scheme` declarado por tema, para o navegador desenhar seletor de data, lista de `select`, caixa de seleção e barra de rolagem no esquema certo). Seletor de elemento puro de propósito, para qualquer classe do componente continuar vencendo.
2. **`← Prontuário` duplicava o caminho** — removido nos dois lugares onde aparecia.
3. **O caminho não navegava em nenhuma tela** — virou trilha estruturada, com o segmento de página como link. O primeiro segmento (`aba_health`, `core`) segue sem link: é namespace, não lugar.
4. **Estética do caminho** — faixa com o fundo do canvas e segmentos em pílula, o mesmo vocabulário das tags.
5. **Os quatro painéis do prontuário tinham tamanhos diferentes** — a página crescia conforme a aba escolhida. Resolvido com altura explícita na raiz + `min-h-0` na cadeia (padrão já documentado em `handoffs/instrucoes.md` §5 desde a 02.5), com rolagem interna. Os rótulos dos 5 passos da anamnese saíram: repetiam, truncados em 9,5px, o título que aparece logo abaixo em corpo legível — e eram o que forçava o painel a ser mais largo.
6. **CRÍTICO — anamnese gravava incompleta.** `AnamneseTab` só recusava quando NENHUMA pergunta havia sido respondida; Max encontrou em uso real uma anamnese com **"3 resposta(s)"** de 5. Corrigido na tela (botão desabilitado, recusa nomeando o que falta) **e no banco** (migration 034, trigger `exigir_anamnese_completa`, `23514`), porque validação de formulário não sobrevive a um `insert` direto pela API e `aba_health` tem regime declarado sem exceção. Trigger e não CHECK por duas razões: a regra compara com `formularios_anamnese.perguntas`, que é outra tabela, e um CHECK invalidaria as linhas incompletas já gravadas — que são registro clínico e não se apagam por migration.
7. **Dashboard sem link na sidebar** — alcançável só pela logomarca. Item próprio no topo, acima de Pessoas.
**Achado de brinde, e grave:** a migration 034 foi escrita, aplicada e **não apareceu no `git status`** — engolida por `*anamnese*.sql` no `.gitignore`. É a armadilha que `handoffs/instrucoes.md` §6 já descrevia ("`.gitignore` por palavra solta engole código em silêncio") acontecendo com o próprio projeto: o banco teria a trava e o repositório não teria a migration, e a próxima clonagem de CRM-filho nasceria sem ela. Corrigido com `!db/migrations/*.sql`, sem afrouxar o bloqueio a dump clínico no resto da árvore; varredura confirmou que nenhuma outra migration estava sendo ignorada. Suíte de saúde ampliada de 15 para **20 testes**, 20/20 verdes; `typecheck` limpo; `build` verde.

### Subetapa 02.13.a — Seed de demonstração + deploy [Goal] [Manual] [LLM: Sonnet]
Objetivo: popular `seed/` com dados fictícios cobrindo os 9 módulos v01; build + deploy FTP em subdomínio de demonstração. Reconsultar o runbook de deploy FTP do CRM-Sindcom (`docs/deploy.md` de lá, com as armadilhas medidas em produção) — o repositório não está clonado localmente, precisa ser reobtido.
Conclusão: subdomínio no ar, navegável, com dados de demonstração em todos os 9 módulos.
Qualidade: nenhum dado real de cliente no seed; nenhum número de telefone real (nem inventado — o provedor completa dígitos ao rotear e pode atingir terceiro, incidente já registrado em projeto irmão); nenhuma credencial de FTP commitada.
Evidência: URL do subdomínio + print de cada módulo populado.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em Opus, dentro do teto. **URL no ar: https://vitrine.strategicepiphany.com** (HTTPS, PWA, HTTP 200 nas rotas profundas). **Seed em `crm/scripts/seed_demo{,_parte2}.mjs`** (executáveis ali, e não em `seed/`, porque dependem de `crm/node_modules` — não há `package.json` na raiz; `seed/README.md` é a documentação). Conta única `[demo] Clínica Vitrine — Estética & Saúde`, idempotente, com `--limpar`. **Equipe conforme especificação de Max:** 1 proprietária, 1 recepcionista (`admin`), 3 profissionais ativos + 1 afastado (`agent`), 1 auxiliar (`viewer`) — o teto de assentos é elevado de 3 para 10 pelo próprio seed, senão `enforce_seat_limit` recusa a partir da quarta pessoa. **12 leads · 10 clientes · 2 fornecedores · 600 atendimentos em 13 semanas · 14 oportunidades (2 por etapa do funil) · 22 faturas · 8 evoluções · 22 mensagens · 10 transmissões · 6 fluxos.** **Cobertura verificada por MEDIÇÃO, não pelo log do script: 130 situações contadas, todas com ≥2** — a lista de estados saiu de uma varredura das 49 restrições `CHECK ... = ANY (ARRAY[...])` dos schemas `aba_*`, e a conferência acusou **16 lacunas que o próprio script dava como cobertas** (formas de pagamento com 1 registro cada, quatro estados de modelo de mensagem ausentes por completo, `transmissoes` com 3 dos 5 estados, tipos de mapa clínico com 1). **Qualidade preservada:** nenhum telefone real nem inventado — todos no indicativo **+999**, reservado pela ITU e não roteável, em vez de um número plausível que o provedor poderia completar até uma conta de terceiro; nenhuma credencial commitada (senha da demonstração só no `.env`); `planos_cliente`/`saldos_plano` nascem por `aba_finance.vender_plano()`, o caminho do produto, e não por escrita direta; sem chave de IA real (marcador inequívoco, agente desligado) e sem token de WhatsApp real. **Três achados corrigidos ao verificar tela a tela, que a leitura do script não pegaria:** (1) o dashboard mostrava **taxa de ocupação de 2%** — número correto sobre 18 atendimentos em 6 datas, mas uma vitrine que exibe 2% mostra clínica parada, não produto funcionando; agenda regerada com volume real (53%); (2) Mensagens exibia "Conectar WhatsApp" e escondia as 6 conversas semeadas, por faltar linha em `configuracao_whatsapp`; (3) a tela de IA parava no portão de aceite do termo, por faltar `aceites_termo_ia`. **Novo no repositório:** `crm/public/.htaccess` (fallback de SPA — sem ele, recarregar em `/prontuario` devolvia 404, armadilha do runbook do CRM-Sindcom) e `crm/scripts/deploy_ftp.mjs`, que trata as duas armadilhas medidas daquele runbook (host FTP não pode ser o nome atrás da Cloudflare; `451` no canal de dados sob TLS exige reenvio com `--ftp-ssl-control`). **Deploy medido:** 9 arquivos, 9 com TLS pleno, **0 divergências de integridade** (tamanho local × remoto), HTTP 200 em `/`, `/configuracoes` e `/prontuario`. `gitleaks` `no leaks found`.

### Subetapa 02.13.b — Devolver a suíte de RLS ao estado executável [Goal] [Manual] [LLM: Sonnet]
Objetivo: a suíte de RLS é o **portão de fase** deste projeto (`handoffs/instrucoes.md` §4) e deixou de ser executável inteira — não por regressão de código, mas porque cada arquivo de teste reautentica os quatro papéis do zero. Esta subetapa devolve a ela a capacidade de rodar de ponta a ponta, sob demanda e em laço, **antes** de a Subetapa 02.15 depender disso.

Dois defeitos, ambos de infraestrutura de teste, nenhum de produto:

**(a) Rate limit de auth — a suíte estoura o limite dentro da própria execução.** `tests/rls/helpers.ts` tem `roleClientCache`, mas ele é cache **de módulo**, e o Vitest isola o registro de módulos **por arquivo de teste** — ou seja, esse cache nunca atravessou a fronteira entre arquivos. Com 17 arquivos × até 4 papéis são **até 68 `signInWithPassword` em ~45 segundos**. Medido na Subetapa 02.12 em três execuções: bloqueada, bloqueada 3 min depois e bloqueada **de novo após 12 minutos de espera** — 10 das 11 falhas eram `Request rate limit reached`, espalhadas por módulos que a subetapa não havia tocado. **Esperar não resolve, porque o estouro é dentro da rodada, não entre rodadas.** Correção: fazer a sessão de cada papel sobreviver entre arquivos — `globalSetup` do Vitest ou cache de token em disco —, com renovação por expiração.

**(b) A falha "pré-existente" de `configuracao_whatsapp`, aberta desde a 02.5, tem causa identificada.** O `beforeAll` de `09_aba_messaging.spec.ts` (e o `semearInquilino` de `12_adversarial_webhook.spec.ts`) insere uma `configuracao_whatsapp` para a **conta principal de teste**, assumindo que ela não tem nenhuma. A restrição `configuracao_whatsapp_account_id_key` é *uma configuração por conta*, e a Subetapa 02.5 deixou lá uma configuração **real e legítima**, criada pela UI ao produzir a evidência daquela subetapa. Não é resíduo a limpar: é o teste assumindo uma conta vazia que deixou de estar vazia. Correção: semear em conta descartável (`createThrowawayUser`), como as subetapas mais novas já fazem, em vez de na conta compartilhada.

Conclusão: `npx vitest run tests/rls` fecha **100% verde em execução única**, sem lotes, sem espera artificial, e **duas execuções seguidas** também fecham verdes — que é a condição real de que a 02.15 depende.
Qualidade: nenhum teste enfraquecido, pulado ou removido para chegar ao verde — o número de casos ao final é maior ou igual ao de hoje (167), e a única mudança de comportamento permitida é em `helpers.ts` e nos dois `beforeAll` citados. Credencial de teste continua fora do repositório; se o token for cacheado em disco, o arquivo é gitignorado e some da árvore ao final. **A suíte é o portão — afrouxá-la para ela passar inverteria o propósito dela.**
Evidência: saída de `npx vitest run tests/rls` com 167+ casos e zero falha, **duas vezes seguidas, sem intervalo entre elas**; contagem de `POST /auth/v1/token` por minuto nos `edge_logs` antes e depois, mostrando a queda no número de logins.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto — e a 02.15 abre sabendo que precisa rodar a suíte em lotes, com o custo declarado.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa. **`npx vitest run tests/rls` fecha 17/17 arquivos e 172/172 casos em execução única**, sem lotes e sem espera; **quatro execuções consecutivas, sem intervalo nenhum, todas 172/172**. **(a) Rate limit:** a causa não era a janela entre execuções e sim a aritmética dentro de uma — `roleClientCache` é cache de módulo e o Vitest dá registro próprio a cada arquivo, então ele nunca atravessou a fronteira entre arquivos (17 × 4 = até 68 logins em ~45s). Corrigido com `tests/rls/globalSetup.ts`, que autentica os quatro papéis **uma vez por execução** no processo principal e grava as sessões num cache em disco gitignorado; `clientAs()` passa a montar o cliente com `setSession()` em vez de `signInWithPassword()`. Enquanto o token vale (1h), a execução seguinte não faz login nenhum — medido: 1ª rodada "4 logins novos, 0 reaproveitadas", 2ª/3ª/4ª "0 logins novos, 4 reaproveitadas". **(b) `configuracao_whatsapp`:** a restrição é única **por conta**, e a Subetapa 02.5 deixou uma configuração real na conta de teste compartilhada; dois fixtures inseriam a sua às cegas. `09_aba_messaging` passa a **reaproveitar** a configuração existente (e só apaga no fim o que ele próprio criou — configuração de outra subetapa não é resíduo dele); o A06 de `12_adversarial_webhook` passa a usar **duas contas descartáveis**, já que o que ele mede é isolamento entre contas e qual conta faz o papel de "A" é indiferente. **Evidência nos `auth_logs` do Supabase:** na pior hora anterior, **723 logins com 162 respostas 429**; nas quatro execuções seguidas de agora, **~22 logins/min e zero 429**. **Qualidade preservada:** nenhum teste pulado, desativado ou enfraquecido (`grep` por `.skip/.todo/xit/xdescribe` = nenhum), e a contagem **subiu de 167 para 172** casos — os 3 que antes apareciam como pulados voltaram a rodar. **Achado de brinde:** `semearInquilino` gerava telefone brasileiro plausível (`5511` + aleatório), contra a regra do projeto de nunca inventar número; trocado pelo indicativo `+999`, reservado pela ITU. Fixture não envia, mas a regra não abre exceção por intenção. `typecheck` limpo; `gitleaks` `no leaks found`.

### Subetapa 02.14 — Varredura de segredos (pós-deploy) [Plan] [LLM: Sonnet]
Objetivo: repetir a varredura da 01.7 — o deploy real pode ter introduzido `.env.deploy`/credencial de FTP/registro novo no histórico.
Conclusão: saída zero, nas mesmas condições da 01.7.
Qualidade: idêntica à 01.7.
Evidência: saída do `gitleaks detect` anexada.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet na primeira; Opus na segunda.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — fechada em 1 tentativa (Sonnet). **`gitleaks detect` sobre o histórico completo (53 commits, 4,34 MB): `no leaks found`, relatório JSON vazio** — mesmas condições da 01.7, nenhuma supressão por fingerprint necessária, nenhum `.gitleaksignore` criado. Árvore limpa antes da varredura. **Controle positivo rodado**, para que "zero achados" signifique ausência de segredo e não scanner cego: `gitleaks --no-git` contra o `.env` real acusa **7 achados** (3 `jwt`, 4 `generic-api-key`), provando que o scanner enxerga a forma dos segredos deste projeto. **Verificação inédita da 02.14, que a 01.7 não tinha como fazer — o que o site público realmente serve:** baixados `index.html` + os dois `assets/` de https://vitrine.strategicepiphany.com (HTTP 200, **byte-a-byte idênticos ao `crm/dist` local**, `sha256`) e cruzados contra os 30 valores do `.env` com 12+ caracteres. **Presentes: só a URL do Supabase e a chave `anon`** (públicas por projeto, protegidas por RLS). **Ausentes: `SUPABASE_SERVICE_ROLE_KEY`, `ENCRYPTION_KEY`, `HOSTGATOR_VITRINE_FTP_PASS`/`USER`/`HOST`, `META_APP_SECRET`, `META_WEBHOOK_VERIFY_TOKEN`, `OPEN_ROUTER_KEY`, `TOKEN_ID`, `FACEBOOK_PAGE_TOKEN`, `INSTAGRAM_TOKEN`, `SENHA`.** O único achado do `gitleaks` no bundle é a regra `jwt`, e ele foi identificado decodificando a claim: `"role":"anon"`, `"ref":"uitwttyyppxvcgfdhnlz"` — **nenhum JWT com `role:service_role` no site**. Nenhuma credencial de FTP no repositório: `deploy_ftp.mjs` lê tudo de `process.env`, `.env.*` está coberto pelo `.gitignore` e nenhum caminho `.env` foi rastreado em commit nenhum do histórico.

**ACHADO GRAVE, e ele não é de vazamento — é do sentido contrário.** A varredura inversa (`git check-ignore` sobre as 295 fontes da árvore, pergunta que a 01.7 não fazia) encontrou **`crm/src/features/settings/secoes/ChavesDeIA.tsx` — 69 linhas, escritas na 02.12, importadas por `ConfiguracoesPage.tsx` que ESTÁ versionado — nunca commitadas**, engolidas por `.gitignore:204 *chave*`. Consequência **medida, não deduzida**: `git worktree` na árvore commitada (`8c1bda4`) + `npm run build` → `error TS2307: Cannot find module './secoes/ChavesDeIA'`. **O repositório não compilava**, com `git status` limpo e o site no ar correto — porque `dist/` é montado do disco local, não do commit. As duas linhas largas (`*chave*`/`*chaves*`) contradiziam a política escrita em prosa doze linhas acima no próprio `.gitignore`; trocadas pela lista com extensão, no formato dos blocos `credencial`/`secret`/`senha` vizinhos, **e corrigidas também em `.gitignore.example`**, o template herdado por todo CRM-filho. Build verde na árvore corrigida; `typecheck` limpo. Terceira ocorrência da mesma armadilha (`*secret*` no Maximus, `*anamnese*.sql` na 02.12b, `*chave*` aqui) — registrada em `handoffs/instrucoes.md` §5/§6 com a regra nova: **varredura de segredos roda nas duas direções**.

**Segundo achado, de integridade documental:** `handoffs/instrucoes.md` estava **colado dentro de si mesmo** desde o commit `76e601f` (02.13.a) — 555 → 1111 linhas, o conteúdo inteiro duplicado a partir do meio da linha 546, decapitando a entrada "todo segredo no `.env` vai entre aspas simples". Nenhuma das duas cópias era descartável (a completa tinha a linha quebrada; a truncada tinha a única cópia da entrada da 02.13.b), então foi reconstruído por composição, com o trecho perdido recuperado da mesma regra escrita por extenso em §5. **Prova de que nada se perdeu: 331 entradas `- **` únicas antes e 331 depois**; a comparação linha a linha devolve apenas as duas metades quebradas, agora unidas. Arquivo é lido na abertura de toda sessão (`CLAUDE.md` §10) — duplicado, dobrava o custo de leitura de cada sessão futura.

**Observação registrada, sem ação (decisão de Max):** a regra `screenshots/` também alcança `design/wireframes-crm-sa-de-e-est-tica/project/uploads/screenshots/` (cópias das mesmas referências de design) e torna inócuo o `screenshots/.gitkeep` — o diretório não sobrevive a um clone. Sem impacto de segurança e sem impacto de build; mexer ali afrouxaria uma regra que protege imagem com dado clínico e caminho de painel, então fica como está até Max decidir.

### Subetapa 02.15 — Portão de segurança adversarial (superfície da Etapa 02) [Manual] [LLM: Opus]
Objetivo: reexecutar o portão de segurança adversarial (mesmos 7 passos da 01.8), desta vez cobrindo a superfície nova — as 16 telas reais, o fluxo de convite/aceite de usuário final, os jobs de `pg_cron`, o deploy FTP e o subdomínio público.
Conclusão: mesmos critérios da 01.8, aplicados à superfície da Etapa 02.
Qualidade: idêntica à 01.8. **Itens obrigatórios herdados:** XSS armazenado testado contra a UI real (o banco guarda o payload literal por design — a defesa é da camada de renderização); fluxo de convite atacado (token adivinhado, convite reusado, escalada de papel no aceite); todo job de `pg_cron` conferido quanto ao `account_id` no `WHERE`.
Evidência: relatório + parecer. Merge para `main`/produção segue exclusivo de Max.
Esforço máximo: sem teto — auditoria.
Escalonamento de LLM: Opus do início ao fim.
Status: ✅ CONCLUÍDA — bench `bench/02.15-seguranca-adversarial` (branch, não worktree, pelo mesmo motivo da 01.8: `.env`/`crm/.env.test` são arquivos reais gitignorados na raiz). Os 7 passos executados. **14 ataques novos e permanentes** (`17_adversarial_isolamento_fk.spec.ts`, `18_adversarial_edge_functions.spec.ts`) + varredura de XSS armazenado com 8 payloads em 5 vetores contra 5 telas reais. **5 achados: 1 grave, 1 médio, 1 baixo e 2 de processo — todos corrigidos e provados** (vermelho antes, verde depois, com controle positivo). Correções em `db/migrations/035`–`039`, aplicadas no banco real durante o bench. **Suíte final: 19 arquivos, 185 verdes + 1 declarado, em duas execuções consecutivas** (era 172 casos, agora 186, nenhum enfraquecido). `typecheck` limpo; `gitleaks` `no leaks found` (56 commits); `get_advisors` sem achado novo atribuível à subetapa. Relatório completo em `docs/RELATORIO_02.15_PORTAO_ADVERSARIAL.md` — **parecer favorável ao merge**, com ressalva declarada sobre a superfície da Meta. O CODE entregou o parecer e parou, com `main` intocada em `30b8864` (`CLAUDE.md` §13); **Max ordenou o merge em 2026-08-22**, executado em seguida por ordem dele (`--ff-only`, histórico linear preservado). `main` = `71c5827`, alinhada ao Supabase: 0 chaves vulneráveis, 144 protegidas, 5 migrations aplicadas, gatilho da `038` presente, correção F03 implantada em produção. Vitrine revalidada (4 rotas em HTTP 200, leitura anônima negada com 401) e suíte revalidada no estado fundido — 185 verdes + 1 declarado, `typecheck` limpo. Bench mantido no remoto como registro auditável.

#### Itens `[Goal]` gerados pelos achados da 02.15 (passo 4 do portão) — todos fechados nesta subetapa

1. **🔴 F01 — Escrita entre contas por chave estrangeira · GRAVE.** A RLS isola a linha, mas a verificação de integridade referencial **ignora row security por especificação do PostgreSQL** — então um usuário da conta A inseria linha sua apontando para linha da conta B. Provado ao vivo em `pagamentos` → fatura alheia, `pessoa_notas` → pessoa alheia e `oportunidades` → funil/pessoa alheios; **73 chaves** na mesma condição, em 9 schemas. Pior caso: `marcar_faturas_vencidas()` soma pagamentos por `fatura_id` sem `account_id`, então um estranho impedia a fatura de outra conta de vencer — número errado sem erro nenhum. `aba_health` resistiu (`42501`), pelo regime clínico mais restritivo. **Conclusão:** toda chave multi-inquilino composta por `account_id`, sem alterar chave primária nem consulta do app. **Qualidade:** migration dirigida por catálogo e idempotente (lista manual erra por omissão); `ON DELETE SET NULL (coluna)` nas 21 que usam SET NULL, senão a exclusão do pai anularia o próprio `account_id` e quebraria em produção; `NOT VALID` + `VALIDATE` para não parar o site; passivo medido antes (**zero** linhas já cruzadas — resultado diferente seria incidente, não erro de migration). **Esforço máximo:** 3 tentativas. **Fechado:** `035`, 0 vulneráveis / 73 protegidas / 25 `UNIQUE`.
2. **🔴 F01-b — Guarda permanente contra a 74ª chave.** A correção acima vale para hoje; uma chave nova nascida na Etapa 03 ou na primeira clonagem de CRM-filho recriaria a classe com sintoma **zero**. **Conclusão:** função de auditoria por catálogo + teste de suíte que falha se ela devolver qualquer linha. **Qualidade:** a função é restrita a `service_role` (introspecção conta o desenho do banco a quem a chama). **Fechado:** `039` + primeiro caso de `17_adversarial_isolamento_fk`.
3. **🟠 F02 — Convite sem teto de validade · MÉDIO.** `criar_convite` tinha piso de 1 dia e nenhum teto; convite de 36500 dias vira porta permanente se o link vazar. Não é escalada — é persistência. **Conclusão:** teto de 7 dias (decisão de Max), prazo menor ainda permitido. **Fechado:** `036`, com teste de regressão.
4. **🟡 F03 — Verificação de configuração antes da autenticação · BAIXO.** `ia-configurar` e `ia-responder` conferiam `ENCRYPTION_KEY` antes de exigir sessão, contando a um chamador anônimo que o servidor estava mal configurado e o nome da variável. Não vaza chave e não dispara em ambiente configurado. **Achado de forma interessante:** as duas funções de WhatsApp já autenticavam primeiro — foi o código mais novo que **regrediu**, o inverso do A03–A07 da 01.8. **Conclusão:** verificação movida para depois da resolução de papel. **Fechado** nas duas funções, implantado no projeto de teste; **implantação em produção pendente do merge** (lá o ramo não dispara).
5. **⚪ F04 — A suíte de testes rodava dentro do banco de produção · PROCESSO.** 12 contas de teste vivas em produção eram o sintoma; a causa era a suíte atacar o mesmo projeto que serve a vitrine pública. **Conclusão:** projeto Supabase separado (US$ 0/mês), 39 migrations aplicadas do zero — o que prova que o repositório sozinho reconstrói o schema —, paridade exata conferida objeto a objeto. **Qualidade:** o guarda de ambiente **para a execução** se as variáveis faltarem ou se a URL de teste for a de produção; *fallback* silencioso seria pior que não separar. Cinco specs furavam o guarda lendo produção direto — corrigidos. **Fechado**, com `crm/scripts/provisionar_banco.mjs` como artefato reaproveitável.
6. **⚪ F05 — Entulho de fixture em produção · PROCESSO.** 12 contas e 12 usuários removidos; produção ficou com a fixture compartilhada e a conta de demonstração. **Fechado.**

### Subetapa 02.16 — Geração do HANDOFF_UPGRADE [Plan] [LLM: Sonnet]
Objetivo: preencher `handoffs/HANDOFF_UPGRADE.md` com o estado real do MVP no ar.
Conclusão: arquivo sem placeholder `<...>` restante; `CHANGELOG.md` com a entrada `+1.0` do lançamento.
Qualidade: toda afirmação rastreável a evidência das subetapas 02.0–02.15.
Evidência: arquivo publicado + commit.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet nas duas.
Se esgotar: parar e emitir relatório curto.

(… 02.n conforme necessário. Subetapa nova de escopo entra **antes da 02.14**, para que a varredura de segredos e o portão adversarial continuem sendo os últimos passos antes do HANDOFF_UPGRADE. A 02.13 foi desdobrada em **02.13.a** (seed + deploy) e **02.13.b** (infraestrutura de teste) a pedido de Max em 2026-08-19 — o sufixo de letra é o padrão para inserir subetapa sem renumerar as seguintes, que já estão referenciadas em pendências, entradas de `handoffs/instrucoes.md` e commits.)

---

## ETAPA 03 — UPGRADES E VERSIONAMENTOS
Objetivo geral: melhorias, correções de bug e novas funcionalidades sobre o produto já lançado.
Versionamento: +0.1 = correções/melhorias | +1.0 = novas funcionalidades/serviços.
Modo predominante: definir por subetapa, conforme backlog abaixo.
Portão de entrada: `HANDOFF_UPGRADE.md` preenchido, MVP v01 no ar e 100% verde (portão de saída da Etapa 02 verde).
Portão de saída: não se aplica como bloco único — cada subetapa `03.n` é uma unidade de versionamento independente, com seu próprio critério 100% verde declarado no momento em que abre (backlog abaixo é só a fila, não o compromisso de escopo).
Observações: ao abrir qualquer subetapa `03.n`, aplicar primeiro a convenção `0X.0` (Subetapa 03.0, abaixo) se for a primeira subetapa depois de uma pausa longa ou de mudança relevante no repositório desde a última sessão de Etapa 03.

### Subetapa 03.0 — Leitura de Referências e Revisão do Plano da Etapa 03 [Plan] [LLM: Opus]
Objetivo: instância da convenção `0X.0` para a Etapa 03. Reler `HANDOFF_UPGRADE.md`, o backlog de versionamento e as pendências vigiadas; confirmar que o item do backlog a abrir ainda faz sentido frente ao estado real do produto e do primeiro cliente (se já houver); revisar este documento.
Conclusão: item do backlog escolhido confirmado ou reformulado; critério 100% verde daquele item declarado como subetapa própria antes de codificar.
Qualidade: nenhuma subetapa `03.n` aberta sem Conclusão/Qualidade/Evidência declaradas primeiro.
Evidência: subetapa `03.n` redigida neste documento antes do primeiro commit de código daquele item.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet na primeira; Opus na segunda.
Se esgotar: parar e emitir relatório curto.

### Subetapa 03.1 — Evolution GO como módulo pago [Manual] [LLM: Opus]
Objetivo / Conclusão / Qualidade / Evidência: a definir quando o primeiro cliente contratar o canal — não abrir esta subetapa sem demanda real paga.

(… 03.n conforme necessário — cada uma passa primeiro pela 03.0 correspondente se a sessão for nova)

---

## Pendências vigiadas

- [ ] **Portão de segurança adversarial obrigatório antes de qualquer mudança de etapa ou deploy real** — gatilho: qualquer subetapa que implante em produção (deploy real). Motivo: existem categorias de falha que passam despercebida em revisão de código normal e na checklist funcional de `docs/05_COMPLIANCE_E_ETICA.md` (que prova que o comportamento *pretendido* funciona, não que não existe um caminho *não pretendido*). Este portão é o complemento adversarial daquele checklist — ataca de propósito em vez de só confirmar o caminho feliz. Risco de não fazer: falha de RLS, vazamento de dado clínico/pessoal ou sequestro de credencial descoberto em produção, por terceiro, em vez de aqui. **Institucionalizado como Subetapa 01.8 (fim da Etapa 01) e Subetapa 02.15 (fim da Etapa 02) — esta entrada permanece como a definição normativa dos 7 passos, referenciada por ambas.**
  - **1. Bench isolado:** abrir branch ou worktree dedicado, nunca commitado direto em `main` — zona de teste segura, sem risco para o histórico principal do repositório.
  - **2. Ataque deliberado**, cobrindo no mínimo: CRUD fora do que a role permite; tentativa de acesso direto ao banco/Supabase fora da camada de RLS; injeção de conteúdo malicioso (SQL, XSS armazenado, payload hostil em coluna `jsonb`); tentativa de burlar ou reescrever política de RLS; alteração de parâmetro ou valor padrão protegido (teto de assentos em `licensing`, valores de `enum`/`CHECK`); tentativa de sequestro de credencial do Supabase, do GitHub/repositório ou do VPS/Oracle; exposição indevida de dado pessoal (LGPD, com atenção redobrada a `health`); qualquer outra fragilidade específica do Vitrine que o teste revelar.
  - **3. Registro em `handoffs/instrucoes.md`:** todo achado (explorável ou não) vira entrada nas seções 4 ("Padrões e boas práticas herdadas"), 5 ("Problemas e soluções deste projeto"), 6 ("Armadilhas conhecidas") ou 7 ("Candidatos a promoção"), conforme a natureza do achado — no mesmo espírito da regra 10 do `CLAUDE.md` (nunca apagar entrada antiga).
  - **4. Plano de correção:** cada falha real vira item no formato `[Goal]` já usado neste documento — Conclusão, Qualidade e Esforço máximo (teto de tentativas de loop) declarados, para orientar a função `/goal`.
  - **5. Execução:** rodar o plano até 100% verde ou até esgotar o teto de tentativas declarado em cada item; o que não fechar vira relatório curto, não fica escondido.
  - **6. Relatório final:** parecer explícito — recomenda ou não recomenda trazer os avanços do bench para `main`.
  - **7. Regra permanente e não negociável: o CODE nunca executa esse merge por conta própria.** Mesmo com todos os testes 100% verdes e parecer final favorável. O CODE entrega o parecer e para — ordenar o merge é atribuição exclusiva de Max.
  - **Instância 02.15 executada em 2026-08-21** — 5 achados (1 grave, 1 médio, 1 baixo, 2 de processo), todos corrigidos; migrations `035`–`039`; suíte de 172 para 186 casos. Ver `docs/RELATORIO_02.15_PORTAO_ADVERSARIAL.md`. **A entrada permanece aberta por desenho: ela é a definição normativa dos 7 passos, e o portão volta a ser exigido a cada mudança de etapa ou deploy real.** Aprendizado a levar para a próxima instância: o vetor que rendeu não foi nenhum dos previstos, e sim **a ponta da chave estrangeira** — a pergunta que a RLS, por especificação, nunca é chamada a responder. Começar a próxima auditoria perguntando *o que a camada de proteção não tem como responder*, antes de perguntar *se ela está certa*. Cobrir também o que ficou de fora aqui: a superfície da Meta (webhook pelo caminho HTTP, `whatsapp-configurar`, `whatsapp-enviar`), congelada por decisão de Max.
  - **Instância 01.8 executada em 2026-08-17** (6 falhas reais encontradas e corrigidas — ver Subetapa 01.8 e `docs/RELATORIO_01.8_PORTAO_ADVERSARIAL.md`). Aprendizado a levar para a 02.15: os vetores que mais renderam não foram os de RLS de tabela (todos verdes), e sim **coluna de privilégio dentro de linha autorizada**, **credencial legível pela API** e **código de servidor rodando com `service_role`** — começar por eles. **Superfície nova a cobrir, definida na Subetapa 02.0:** as 16 telas reais (XSS armazenado), o fluxo de convite/aceite (token adivinhado, convite reusado, escalada de papel no aceite) e os jobs de `pg_cron` (que rodam fora da RLS e precisam do `account_id` no `WHERE`).
- [ ] `select('*')` quebra em tabela com narrowing por coluna — gatilho: primeira tela da Etapa 02 que toque `webhook_endpoints`, `api_keys`, `account_invitations`, `aba_ai.ia_configuracoes` ou as duas tabelas de segredo de `aba_messaging` — risco: o erro é `42501 permission denied for table`, que parece falha de RLS e manda a investigação para o lado errado; a correção é listar colunas explicitamente no `.select()`. Consequência aceita conscientemente da correção A03/A04/A05/A07 da Subetapa 01.8 (esconder credencial vale o custo). Ver `handoffs/instrucoes.md` §6. **Absorvida na Subetapa 02.0 como Qualidade fixa de toda subetapa de tela (02.1–02.12)** — deixa de depender de alguém lembrar dela no momento certo; segue listada aqui como definição normativa.
- [x] **FECHADA na Subetapa 02.10 (2026-08-19).** `pg_cron 1.6.4` instalado em `pg_catalog` (destino prescrito pela documentação vigente, confirmado por `pg_extension` depois de aplicar), schema `cron` criado pela própria extensão e **cinco jobs ativos**: `drenar-execucoes-pendentes` (a cada minuto), `disparar-lembretes-vencidos` (5 min), `expirar-fluxos-ociosos` (horário), `marcar-faturas-vencidas` e `expirar-planos` (diários, em minutos deslocados). Corridas reais confirmadas em `cron.job_run_details`. Observável dentro do produto por `aba_automations.listar_jobs_agendados()` (restrita a `admin+`) — o schema `cron` **não** foi exposto à API, porque isso entregaria `cron.schedule` ao navegador. Texto original abaixo, preservado como registro. ~~`pg_cron` declarado na arquitetura mas nunca instalado~~ — gatilho: **Subetapa 02.10** — medido na Subetapa 02.0: `pg_available_extensions` mostra `pg_cron 1.6.4` disponível com `installed_version = null`. `docs/01_ARQUITETURA.md` §2 o declara como o agendador que substitui o pinger externo do Maximus, e quatro rotinas já existem no banco sem nenhum motor que as chame (`aba_finance.marcar_faturas_vencidas()`, `aba_finance.expirar_planos()`, a drenagem de `aba_automations.automacao_execucoes_pendentes` e o disparo de `aba_scheduling.lembretes`) — risco: o produto sai no ar com fatura que nunca vence, plano que nunca expira e lembrete que nunca dispara, e o sintoma é ausência de comportamento, que não gera erro nenhum. Exige *search-first* na documentação vigente da Supabase antes da migration, e a extensão nunca em `public`.
- [ ] Itens de navegação do wireframe sem modelo de dados — gatilho: **Subetapa 02.1**, ao montar a sidebar — `Propostas` (`aba_sales`) e `Espera e encaixes` (`aba_scheduling`) aparecem no `Shell.dc.html` do pacote de design mas não têm tabela nenhuma no banco; "Lista de espera e encaixe" já consta do backlog de versionamento como *futuro*. A busca global do header (`Buscar pessoa, atendimento, serviço…`) também não tem subetapa dona — risco: alguém inventar tabela para casar com o desenho, em vez de tratar o desenho como aspiracional. **Decisão da Subetapa 02.0: ficam fora da navegação do v01.** Reabrir só como item de backlog com escopo próprio.
- [ ] Proteção contra senha vazada indisponível no plano gratuito do Supabase — gatilho: decisão de Max sobre migrar de plano — risco: senha comprovadamente vazada em incidente público não é bloqueada no cadastro. **Confirmado na documentação oficial em 2026-08-07:** a verificação contra o HaveIBeenPwned é recurso do plano Pro e acima. Não existe contorno gratuito dentro do Supabase Auth — a alternativa seria validação própria no cadastro, que é trabalho de produto, não de configuração. Segue em aberto por decisão consciente, sob o circuit breaker de R$0/mês.
- [ ] **Arte e vocabulário dos 4 mapas clínicos vivem fora do banco** — gatilho: quando o asset de produção dos mapas (facial, corporal frente/costas, odontograma de 32 dentes, meridianos) for decidido com Max. A Subetapa 02.9 entregou a mecânica completa (região selecionável, estado por região, marcação persistida em `aba_health.evolucoes.marcacoes`), mas a arte continua sendo o esquema de wireframe (`docs/04` §5.5) e o catálogo de região/estado mora em `crm/src/features/health/mapas.ts`, não numa tabela de módulo. Risco: o wireframe `1p` diz explicitamente que "nomenclatura de pontos e meridianos vem de tabela do módulo — nunca digitada livre"; enquanto o catálogo for de código, cada conta usa o mesmo vocabulário e nenhuma consegue ajustá-lo sem deploy, e uma marcação gravada com vocabulário antigo deixa de ser desenhada se o catálogo mudar (`marcacoesValidas()` descarta região/estado fora do catálogo — falha silenciosa, não erro). Não é bloqueio do MVP: o CHECK do banco garante array e tipo de mapa válido, que é o que o banco pode garantir sem congelar vocabulário. Reabrir junto com o asset, como item de escopo próprio.
- [x] **FECHADA em 2026-08-19 — chamada real ao provedor exercida de ponta a ponta.** Max disponibilizou uma chave do OpenRouter no `.env` e o ciclo completo rodou: catálogo consultado (415 modelos, 20 gratuitos), chave verificada e aceita pelo provedor, cifrada e gravada, aceite do termo registrado, agente ativado, base de conhecimento semeada, **pergunta real respondida** ("quanto custa a limpeza de pele?" → "A limpeza de pele profunda custa R$ 180,00 e tem duração de 60 minutos. Para confirmar a disponibilidade de horários, por favor, entre em contato com nossa equipe.") e **linha gravada em `aba_ai.ia_log_uso`** (`openrouter`, 267 tokens de entrada + 279 de saída = 546). Repetido **pela própria tela** com outra pergunta, com o rodapé mostrando provedor, modelo, tokens, trechos usados e "consumo registrado". Três coisas foram provadas de brinde: a busca corrigida na 029 funcionou (1 trecho recuperado por pergunta em linguagem natural — antes seriam 0); o agente **obedeceu à permissão desligada** e recusou afirmar disponibilidade de horário, exatamente como a instrução de `pode_consultar_horarios = false` manda; e o OpenRouter funcionou pelo caminho compartilhado com a OpenAI. Texto original abaixo, preservado como registro. ~~Chamada real ao provedor de IA nunca foi exercida~~ — gatilho: quando uma conta conectar uma chave de IA válida (a de Max, ou a do primeiro cliente na prova de fogo da Etapa 03). A Subetapa 02.11 construiu e implantou o caminho inteiro — `ia-configurar` verifica a chave contra o provedor antes de gravar (provado: chave falsa recusada com a mensagem real, nada gravado), `ia-responder` decifra, recupera conhecimento, chama o provedor e grava `ia_log_uso` —, mas **o trecho entre "provedor aceitou a chave" e "linha em `ia_log_uso`" nunca rodou com credencial válida**. Risco: o que pode quebrar aí é específico e não aparece em teste — nome de modelo inexistente, formato de resposta diferente do esperado, `usage` ausente, timeout da Edge Function. Decisão de Max em 2026-08-19: fechar a subetapa sem essa prova, por *bring-your-own-key* (o projeto não tem chave própria) e porque o CODE não insere credencial em campo. **Fechar assim que houver uma chave**: colar na tela `1l`, ligar o agente, usar "Perguntar" e conferir a linha em `ia_log_uso`.
- [ ] **Provedores de IA adicionais (OpenRouter) e LLM auto-hospedada** — gatilho: decisão de Max sobre escopo, levantada por ele em 2026-08-19 durante a Subetapa 02.11. **(a) OpenRouter** é barato de acrescentar e verificado: a API é compatível com o formato *chat completions* da OpenAI (`https://openrouter.ai/api/v1/chat/completions`, `Authorization: Bearer`), então a função `responderOpenAI()` de `ia-responder` serve com troca de URL base; os limites do plano gratuito conferem com o que Max citou — **50 requisições/dia sem crédito, 1000/dia com US$10 em crédito, teto de 20/min nos dois casos** (documentação oficial consultada em 2026-08-19). Custo: uma migration para ampliar o CHECK `provedor` em `ia_configuracoes` e `ia_log_uso`, mais UI. **(b) LLM auto-hospedada em VPS** contraria a arquitetura v01 declarada ("zero Docker, zero VPS, zero SSH", `handoffs/instrucoes.md` §2, com Docker/VPS entre as alternativas descartadas em `docs/01` §5) — reabrir isso não é acrescentar um provedor, é mudar a fundação operacional. **Fora do MVP por decisão de Max (2026-08-19); a ideia segue viva para versão futura.**

  **[CORREÇÃO DE ANÁLISE — 2026-08-19]** A primeira versão desta entrada afirmava que o benefício de não-treinamento "já está atendido sem VPS", com base na política comercial da Anthropic. **A análise estava errada por comparar o eixo errado**, e Max apontou o erro: modelo comercial de código fechado **não pode** ser auto-hospedado, então a comparação relevante não é contra a API paga — é contra a alternativa que ocupa o mesmo lugar no produto, o **free tier**. Nesse eixo o argumento de Max se sustenta. Verificado na documentação do OpenRouter em 2026-08-19: a política de dados **é de cada provedor, não do OpenRouter**, e existe configuração para "permitir roteamento a provedores que podem treinar com seus dados" **com ajustes separados para modelos gratuitos e pagos** — ou seja, treinar com o dado do usuário é uma prática real e reconhecida nesse mercado, e o roteamento gratuito não dá controle sobre qual provedor recebeu o quê. Ressalva de precisão: a FAQ deles não declara o treinamento como **obrigatório** para usar modelos gratuitos (o custo declarado é o limite de requisições), e o roteamento a provedores que treinam pode ser desligado — possivelmente ao custo de acesso a menos modelos gratuitos. **Conclusão corrigida:** contra free tier, a auto-hospedagem de modelo **aberto** (Llama, Gemma, Qwen) ganha em privacidade de forma decisiva; contra API comercial paga, empata em treinamento e ganha só em transferência internacional e ausência de suboperador.

  **Argumento comercial correto — não é "grátis", é custo fixo diluído.** Uma VPS tem custo mensal fixo; a vantagem estrutural para este produto é que esse custo é **rateado entre todos os CRM-filhos**, enquanto token é custo variável que cresce com o uso de cada cliente. Para um produto cujo modelo é clonar CRM-filho, isso inverte a curva de margem. Vender como "IA inclusa, sem cobrança por uso" é honesto; vender como "grátis" não é.

  **O que continua pesando contra:** qualidade em português de modelos de 8–9B para atendimento ao cliente (Gemma 2 9B e Qwen são os melhores dessa faixa; Llama 3 8B é fraco em PT-BR); GPU — 8–9B quantizado roda em GPU modesta, mas em CPU pura a latência inviabiliza conversa ao vivo; e operação (atualização de segurança, monitoramento, backup, resposta a incidente), que hoje não existe porque não existe servidor.

  **Risco adversarial a testar, e não é o que parece.** Max levantou a preocupação de o modelo "sequestrar" informação para si. Modelo aberto rodando isolado não tem telemetria para exfiltrar — esse risco é baixo. **O risco real é vazamento de contexto entre inquilinos**: um único servidor atendendo vários CRM-filhos, com prompt, cache de contexto ou histórico mal isolados, pode misturar dado de um cliente na resposta de outro. É exatamente a classe de falha que a Subetapa 01.8 encontrou no webhook (achado A06, `service_role` cruzando conta) e é isso que um portão adversarial precisaria atacar antes de qualquer promessa de LGPD ao cliente final.

  **Terceira via, mais barata que VPS e não avaliada ainda:** provedores que hospedam modelos **abertos** com garantia contratual de não-treinamento (Together, DeepInfra, Fireworks, Groq pago). Custo por token baixo, sem operar servidor, sem o risco de isolamento acima. Não zera o custo variável, mas entrega a maior parte do ganho de privacidade sem mudar a fundação da arquitetura. **Merece ser medida antes de decidir pela VPS.**
- [ ] **Busca de conhecimento não faz stemming — variação da palavra não casa** — gatilho: quando a qualidade das respostas do agente virar assunto, ou quando um CRM-filho de mercado não lusófono entrar. Medido em 2026-08-19 com dados reais: a config `'simple'` de `to_tsvector` (escolhida na Subetapa 01.5 **de propósito**, para o produto não ficar preso a um idioma) não reduz palavra ao radical, então `"cancelar"` **não** casa com `"Cancelamentos"` e `"hora"` não casa nem com `"horas"`. Com a config `'portuguese'` os dois casariam (ambos viram o lexema `cancel`). Consequência prática: o cliente que perguntar "posso cancelar em cima da hora?" não recupera o trecho da política de cancelamento, e o agente responde que vai confirmar com a equipe — comportamento seguro, mas inútil. **Não é defeito de implementação, é a decisão de multi-idioma cobrando seu preço**; resolver exige escolher entre (a) config por conta/idioma, (b) indexar em duas configs e buscar nas duas, ou (c) fixar o português e assumir o custo na exportação para outro mercado. Decisão de produto, de Max. Coberto por teste que documenta o comportamento atual (`15_aba_ai_agente.spec.ts`) — se mudar, o teste falha e obriga a decisão a ser consciente.
- [x] **FECHADA na Subetapa 02.13.b (2026-08-19).** `globalSetup` autentica os 4 papéis uma vez por execução e cacheia as sessões em disco (gitignorado); `clientAs()` usa `setSession()`. A suíte fecha **172/172 em execução única** e em **quatro execuções consecutivas sem intervalo**. Nos `auth_logs`: de 723 logins com 162 respostas 429 na pior hora, para ~22 logins/min e **zero 429**. O conflito de `configuracao_whatsapp`, aberto desde a 02.5, foi junto. Texto original abaixo, preservado como registro. ~~A suíte de RLS não fecha mais em execução única, em janela nenhuma — deixou de ser executável inteira~~ — gatilho: **imediato, antes da Subetapa 02.15**. Medido na 02.12, em três execuções: falhou com `Request rate limit reached` (mensagem literal do Supabase Auth) na primeira, de novo 3 minutos depois, e **de novo após 12 minutos de espera** — 10 das 11 falhas eram de autenticação, espalhadas por `sales`, `ai`, `catalog` e `cross_account`, módulos que a subetapa não tocou. **Esperar não resolve, porque o problema não é a janela: é a aritmética.** `roleClientCache` de `tests/rls/helpers.ts` é cache de módulo, e o Vitest isola o registro de módulos por arquivo — 17 arquivos × até 4 papéis = **até 68 `signInWithPassword` em ~45 segundos**, muito acima do teto do endpoint de token. A suíte cresceu de 11 para 17 arquivos desde que a linha de base de 124/130 foi medida, e cruzou o limite nesse caminho. Na 02.10 isso estava registrado como armadilha de diagnóstico (`handoffs/instrucoes.md` §5); agora é bloqueio de portão. **Contorno usado na 02.12 para verificar sem alterar a infraestrutura:** rodar em lotes de 3-4 arquivos com ~5,5 min entre eles — prova o código, mas não serve para a 02.15, que precisa reexecutar a suíte muitas vezes. **Correção certa, já descrita em "Candidatos a promoção":** cachear o token por papel entre arquivos (arquivo em disco ou `globalSetup` do Vitest), com renovação por expiração. Risco de não fazer: a 02.15 confunde bloqueio de auth com achado de segurança — ou, pior, conclui "tudo verde" numa execução em que metade dos casos nem autenticou. **Institucionalizada como Subetapa 02.13.b** (criada a pedido de Max em 2026-08-19, encaixada entre a 02.13.a e a 02.14) — esta entrada permanece como a definição normativa do problema e da medição, referenciada por ela. Junto com o item (b) daquela subetapa: a falha "pré-existente" de `configuracao_whatsapp`, aberta desde a 02.5, teve a causa identificada na 02.12 — o teste semeia na conta compartilhada assumindo que ela não tem configuração, e a 02.5 deixou lá uma real.
- [ ] **Modo escuro existe, mas só foi conferido em 3 das 16 telas** — gatilho: qualquer subetapa que toque UI, e obrigatoriamente antes do deploy da 02.13.a. A Subetapa 02.12 introduziu o tema por conta e varreu o hex literal que encontrou (`grep -rn "#[0-9a-fA-F]\{6\}" src`), corrigindo gráfico do Financeiro, toggles de IA/Automações e quatro painéis. **Conferidas visualmente no escuro: dashboard, Configurações e a casca do AppShell.** As outras treze não foram abertas em modo escuro. Risco: contraste insuficiente ou elemento invisível numa tela que ninguém olhou — e o cliente que ligar o tema escuro vai olhar todas. A paleta dark é **derivação** da paleta clara ratificada, não uma variante que Max fechou (`docs/04` §5.2 registra que o pacote de wireframes é 100% light); ratificá-la é decisão de Max.
- [ ] **Exceção de permissão órfã na conta de teste** — gatilho: quando a suíte for revista. `access.module_permissions` da conta `rls.owner` tem uma linha (`agent`/`health`/`read`/`false`) gravada em 2026-08-19 07:28, anterior à Subetapa 02.12 — resíduo de fixture de outra sessão que não foi limpa. Inofensiva (repete o padrão do banco, que já nega `health` para todo papel não-proprietário) e **deliberadamente não removida pela 02.12**, que não é dona dela. Fica registrada porque aparece na tela `1m` como "1 exceção" e pode confundir quem for ler a matriz depois. Reforça a regra de `handoffs/instrucoes.md` §5: fixture se apaga por completo, nunca se desativa.
- [ ] Teto de usuários editado direto no banco — gatilho: existir o painel de gestão dos CRMs-filho — risco: erro de digitação em produção, sem trilha de interface.
- [ ] Consentimento de uso de imagem trava a **exibição** da foto clínica, não o envio — e trava para todos, inclusive para quem tirou a foto — gatilho: a prova de fogo será quando um profissional de verdade usar o módulo — risco: a profissional não enxerga o "antes" ao acompanhar a evolução do tratamento. **Decisão de Max (2026-08-08): manter como está.** O motivo é a assimetria de custo — afrouxar depois é uma condição na migration 069, sem migração de dado, porque a foto já está guardada; apertar depois é impossível, porque foto já vista não desvê. Se a 03.1 confirmar o incômodo, a correção certa **não** é liberar geral: é separar as duas perguntas que hoje um interruptor só responde — "posso documentar" passa a ser o consentimento de tratamento de dados, e "posso divulgar" segue sendo o de uso de imagem, valendo para exportação e publicação. Entra como candidato à Subetapa 03.2 (generalizações apontadas pela prova de fogo).
- [ ] `aba_people.pessoas.contato_id` sem FK para `aba_messaging.contatos_canal` — gatilho: qualquer alteração futura na ordem de aplicação dos schemas de módulo — risco: se algum dia a ordem de aplicação mudar, é tentador "corrigir" adicionando a FK de volta sem lembrar que ela quebra a exportação avulsa de `aba_people` para cliente sem mensageria. Ver `handoffs/instrucoes.md` §5 para o registro completo da decisão. Não reintroduzir a FK sem resolver antes o problema de exportabilidade que ela reabre.

---

## Backlog de versionamento (documento vivo)

- [ ] CLI/checklist formal de clonagem de CRM-filho — impacto no MVP? Não — versão alvo +1.0
- [ ] Múltiplos templates de layout (sidebar/grid configurável) — impacto no MVP? Não — versão alvo +1.0
- [ ] `pgvector` para busca semântica em `aba_ai` — impacto no MVP? Não — versão alvo +1.0
- [ ] Evolution GO como módulo pago — impacto no MVP? Não — versão alvo +1.0
- [ ] `HaveIBeenPwned` no Supabase Auth — impacto no MVP? Não — versão alvo +0.1 (quando plano premium)
- [ ] Agendamento online pelo cliente — impacto no MVP? não — versão alvo +1.0.
- [ ] E-mail transacional — não — +1.0.
- [ ] Facebook e Instagram — não — futuro.
- [ ] Produtos e estoque (`commerce`) — não — +1.0.
- [ ] Nota fiscal de serviço — não — futuro.
- [ ] Cobrança Pix e boleto por API bancária — não — +1.0.
- [ ] Baixa automática de pagamento — não — +1.0.
- [ ] Quadros de tarefa (`tasks`) — não — +1.0.
- [ ] **Marca d'água de identificação na tela clínica** — impacto no MVP? Não — versão alvo `+1.0`. Sobrepor o identificador de quem está logado e o horário sobre os painéis de `aba_health`, discreto e não imprimível por cima do conteúdo. **Substitui o bloqueio de captura de tela, que foi avaliado e recusado** (ver `docs/05` §3.1): não impede a foto, mas torna a captura rastreável até quem a fez, que é o que de fato muda comportamento. Nasce da ideia de Max de 2026-08-19.
- [ ] **Obscurecer painel clínico ao perder o foco + travar sessão por inatividade** — impacto no MVP? Não — versão alvo `+0.1`. Defende o risco mais provável e mais mundano que a captura maliciosa: prontuário esquecido aberto no balcão da recepção, ou aparecendo por engano num compartilhamento de tela. Barato, e o único destes itens que reduz exposição de verdade em vez de só rastreá-la.
- [ ] Identidade visual por conta (`branding`) — não — +1.0.
- [ ] Construtor de papéis customizáveis — não — +1.0.
- [ ] Lista de espera e encaixe — não — futuro.
- [ ] Fidelidade e indicações — não — futuro.
- [ ] CLI de exportação de módulos — não — futuro.
- [ ] Telegram como canal completo — não — +1.0.
- [ ] Alarme de queda de sessão com deduplicação — não — +0.1.
- [ ] Conferência automática do webhook após reconexão — não — +0.1.
- [ ] Login multi-conta (seletor de CRM-filho pós-autenticação, tela `1a` do wireframe) — impacto no MVP? Não — versão alvo +1.0. Exigiria tabela de associação usuário↔conta N:N; schema atual (`public.profiles.user_id UNIQUE`) trava 1 usuário = 1 conta. Decisão de Max, Etapa de Transição 1→2 (2026-08-18) — ver `docs/01_ARQUITETURA.md` §7.2.

---

## CHECKLIST DE CONFORMIDADE (autoaplicada antes da entrega)

1. [x] Arquivos do repo devidamente conferidos em sua integralidade antes de iniciar a Etapa 01.1 — conferido na Subetapa 01.0.
2. [x] Princípio-guia presente, com o ajuste de faseamento declarado explicitamente em vez de fingido.
3. [x] Idea lock preenchido, sem decisões estruturais pendentes.
4. [x] As 3 etapas presentes e em sequência.
5. [x] Cada Etapa tem Objetivo geral, Modo predominante, Portão de entrada, Portão de saída e Observações.
6. [x] Cada portão de saída declara condição + prova executável + o que fica proibido enquanto vermelho.
7. [x] Toda subetapa `[Goal]` tem Esforço máximo declarado.
8. [x] Toda subetapa `[Goal]` tem Escalonamento de LLM e a regra "se esgotar → relatório curto".
9. [x] Etapa 01 prevê HANDOFF_BUILD (Subetapa 01.9); Etapa 02 prevê HANDOFF_UPGRADE (Subetapa 02.16, renumerada da 02.7 na Subetapa 02.0).
10. [x] Pendências vigiadas e Backlog de versionamento presentes.
11. [x] Subetapa de varredura de segredos no histórico do git, com evidência executável e saída esperada zero — Subetapas 01.7 e 02.14.
12. [x] Nenhuma credencial ou segredo aparece neste plano — apenas nomes de variáveis.
13. [x] `CHANGELOG.md` existe e está referenciado no README; `handoffs/instrucoes.md` existe e está referenciado no README e no HANDOFF_CODE.
14. [x] `.gitignore` e `.env` gerados como arquivos prontos, e o `.gitignore` ignora `.env` sem exceção. `.env.example` foi eliminado por decisão de Max (Subetapa 01.0) — o `.env` real já existe, gitignorado, sem versão de exemplo no repositório.
15. [x] Fim das Etapas 01 e 02 possuem **Portão de segurança adversarial obrigatório** antes de qualquer mudança de etapa ou deploy real — Subetapas 01.8 e 02.15.
