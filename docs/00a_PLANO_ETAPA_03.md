# 00a — Plano de trabalho da Etapa 03 (recorte enxuto)

> **O que é este arquivo.** Extrato de `docs/00_PLANO_E_CRITERIOS.md`, contendo só a Etapa 03 — as 822 linhas do plano completo cobrem as Etapas 01, 02 e 03, e toda sessão que abre para executar uma subetapa `03.n` só precisa da terceira. Existe para reduzir o que cada sessão lê antes de começar a trabalhar, não para substituir o plano completo.
>
> **Fonte canônica: `docs/00_PLANO_E_CRITERIOS.md`.** Este arquivo é uma **cópia sincronizada**, nunca a origem de uma decisão. Política fixada por Max em 2026-09-03: **toda subetapa concluída grava o Status nos dois lugares, no mesmo commit** — aqui e no docs/00 original. Nenhum dos dois fica "mais atualizado" que o outro por desenho; se algum dia divergirem, docs/00 vence, porque é ele que o `CLAUDE.md` §8 nomeia.
>
> **Ciclo de vida.** Quando a Subetapa 03.23 (portão adversarial do MVP, que fecha a Etapa) for concluída, este arquivo já terá exatamente o mesmo conteúdo relevante que docs/00 — nesse momento ele deixa de ter função e a subetapa de fechamento decide se ele é apagado ou mantido como registro histórico.
>
> **O que NÃO está aqui**, e continua só em docs/00: Idea lock, Etapas 01 e 02 inteiras, a Etapa de Transição 1→2 e 2→3, o Backlog de versionamento completo, o CHECKLIST DE CONFORMIDADE, e as Pendências vigiadas que não dizem respeito a nenhuma subetapa `03.n` — abaixo só o recorte que importa.

---

## Mapa de evolução — as 32 subetapas da Etapa 03

| Subetapa | Nome | Onda / Etapa do roteiro | LLM | Status |
|---|---|---|---|---|
| 03.0 | Leitura de Referências e Revisão do Plano da Etapa 03 | Etapa 1 do roteiro | Opus | ✅ CONCLUÍDA |
| 03.0.a | Revisão do plano da Etapa 03 à luz da pesquisa `analise-ice` | Etapa 1 do roteiro | Opus | ✅ CONCLUÍDA |
| 03.1 | Evolution GO como módulo pago | (fora de onda) | Opus | ⏸️ ADIADA |
| 03.2 | Uniformidade do repositório | Etapa 1 do roteiro | Sonnet | ✅ CONCLUÍDA |
| 03.3 | Divisão por rota + fontes auto-hospedadas | Onda 1 | Sonnet | ✅ CONCLUÍDA |
| 03.4 | Agendamento: espera, marcadores e cadeiras | Onda 1 | Sonnet | ✅ CONCLUÍDA |
| 03.5 | Ações dos usuários + consentimento de imagem | Onda 1 · P-sub | Sonnet (Opus indisponível) | ✅ CONCLUÍDA |
| 03.6 | Catálogo: faces, unidade e semente SIGTAP | Onda 2 | Sonnet | ✅ CONCLUÍDA · 🔁 revisada pela 03.6.a |
| 03.6.a | Catálogo: regra do código e requisitos por procedimento | Onda 2 | Sonnet (executada em Opus) | ✅ CONCLUÍDA |
| 03.6.b | Vocabulário do domínio: procedimento, pacote, plano e nível | Onda 2 · bench isolado | Opus | ✅ CONCLUÍDA |
| 03.7 | Odontograma | Onda 2 · P-sub | Opus | ✅ CONCLUÍDA · 🔁 revisada pela 03.7.a |
| 03.7.a | Odontograma autoral (substitui a implementação da 03.7) | Onda 2 · P-sub | Opus | ✅ CONCLUÍDA |
| 03.8 | Plano de tratamento: a entidade clínica | Onda 2 · P-sub | Opus | ✅ CONCLUÍDA |
| 03.8.a | Orçamento: o preço que se resolve, e a tabela que tem vigência | Onda 2 · P-sub | Opus | ⬜ não iniciada |
| 03.8.b | Contrato: itens heterogêneos, assinatura e a trava dupla | Onda 2 · P-sub | Opus | ⬜ não iniciada |
| 03.9 | Multiunidade + trava de plano por módulo | Onda 3 · **PORTÃO COMPLETO** | Opus | ⬜ não iniciada |
| 03.10 | Infraestrutura de token externo | Onda 4 · P-sub | Opus | ⬜ não iniciada |
| 03.11 | Caixa de entrada de exames | Onda 4 · P-sub | Opus | ⬜ não iniciada |
| 03.12 | Assinatura do paciente por link multicanal | Onda 4 · P-sub | Opus | ⬜ não iniciada |
| 03.13 | Exportação de prontuário | Onda 4 · P-sub | Opus | ⬜ não iniciada |
| 03.14 | Encaminhamento com contrarreferência | Onda 4 · P-sub | Opus | ⬜ não iniciada |
| 03.15 | Portão adversarial da comunicação externa | Onda 4 · **PORTÃO COMPLETO** | Opus | ⬜ não iniciada |
| 03.16 | Alertas clínicos derivados da anamnese | Onda 5 · P-sub | Opus | ⬜ não iniciada |
| 03.16.a | Dossiê do paciente: três famílias de abas com permissão por módulo | Onda 5 · P-sub | Opus | ⬜ não iniciada |
| 03.17 | Painel como lista de tarefas + estados vazios + exportação | Onda 5 | Sonnet | ⬜ não iniciada |
| 03.18 | Régua de cobrança, campanhas, template e cota | Onda 5 | Sonnet | ⬜ não iniciada |
| 03.19 | Link público de agendamento | Onda 5 · P-sub | Sonnet | ⬜ não iniciada |
| 03.20 | Estoque: alertas e validade | Onda 5 | Sonnet | ⬜ não iniciada |
| 03.21 | Tabela de métricas por CRM-filho | Onda 5 | Sonnet | ⬜ não iniciada |
| 03.21.a | Análise e distribuição de acessos: quem vê o quê, em tudo | Onda 5 · P-sub | Opus | ⬜ não iniciada |
| 03.22 | Implantação da UX Versão 03 | Etapa 3 do roteiro | Sonnet | ⬜ não iniciada |
| 03.23 | Portão de segurança adversarial do MVP | Etapa 4 do roteiro · **PORTÃO COMPLETO** | Opus | ⬜ não iniciada |

**Progresso: 12 de 32 concluídas (03.0, 03.0.a, 03.2, 03.3, 03.4, 03.5, 03.6, 03.6.a, 03.6.b, 03.7, 03.7.a, 03.8) · 1 adiada (03.1) · 19 restantes** *(contagem de 2026-09-04, após a 03.8; o total foi 26 em 2026-09-03, 30 em 2026-09-04 por D-I5 e 32 em 2026-09-04 por D-V2 e D-V6 — número sem data não denuncia que envelheceu, `handoffs/instrucoes.md` §5)* — duas subetapas entraram em 2026-09-03, a pedido de Max, inseridas sem renumerar as seguintes: **03.16.a** (dossiê do paciente) e **03.21.a** (análise e distribuição de acessos). Três portões completos aguardam (03.9, 03.15, 03.23) — nenhum deles é executado sem bench isolado, e nenhum termina em merge por conta do CODE (`CLAUDE.md` §13).


> **Status no próprio título.** Padrão fixado por Max em 2026-09-03: além do campo `Status:` no corpo da subetapa (`CLAUDE.md` §8), o mesmo marcador aparece **no fim da linha do `###`** — `· Status: ✅ CONCLUÍDA`, `· Status: ⏸️ ADIADA`, `· Status: ⚠️ PENDENTE`, `· Status: 🛑 ABANDONADA`. Subetapa não iniciada não leva marcador nenhum, e é essa ausência que a distingue. O motivo é de leitura: o sumário do editor e um `grep` por `^### Subetapa` passam a responder "onde estamos" sem abrir nenhum bloco. Vale nos **dois** planos, gravado no mesmo commit, como todo o resto do Status.

---

## ETAPA 03 — FECHAMENTO DO MVP ODONTOLÓGICO
Objetivo geral: implantar os **30** itens do MVP — os 24 levantados pelo benchmark (`design/benchmark/RELATORIO.md` §5 c, revisado por Max em 2026-09-02), mais os **dois acrescentados por Max em 2026-09-03** (Resumo do paciente e Prescrições de medicamento), mais os **quatro que entraram por D-I5 em 2026-09-04** vindos da pesquisa `analise-ice` (**34** opções de tratamento concorrentes, **35** requisitos por procedimento, **36** recusa implícita e re-consentimento, **41** vigência de tabela de preço) — escolher e implantar a UX, e passar pelo portão adversarial, sobre o produto já lançado e sem reconstruir nada do v01.
Versionamento: +0.1 = correções/melhorias | +1.0 = novas funcionalidades/serviços.
Modo predominante: [Manual Mode] + [Goal] (um /goal por subetapa), com [Plan] nas subetapas de revisão e [Manual] nos portões.
Portão de entrada: `HANDOFF_UPGRADE.md` preenchido, MVP v01 no ar e 100% verde (portão de saída da Etapa 02 verde) — satisfeito em 2026-08-22.
Portão de saída: os 30 itens entregues ou explicitamente reportados como inviáveis (item inviável se **reporta**, nunca se remove — `handoffs/HANDOFF_PLANO_MVP.md`); UX da Versão 03 implantada; portão adversarial do MVP executado com parecer registrado; `CHANGELOG.md` atualizado. Enquanto vermelho, as Etapas 5 a 7 do roteiro (teste em campo, lançamento, versionamentos futuros) não abrem.
Observações: o roteiro de sete etapas decidido por Max em 2026-09-02 mapeia assim nas subetapas abaixo — Etapa 1 (uniformidade) = 03.2; Etapa 2 (os 30 itens) = 03.3 a 03.21, incluindo a 03.6.a, a 03.7.a, a 03.8.a e a 03.16.a; Etapa 3 (UX) = 03.22; Etapa 4 (adversarial) = 03.23. As Etapas 5 a 7 são planos futuros e não constam aqui. Trabalho conduzido no branch `etapa-03/plano-mvp-odontologico`, com `main` intocada — merge é ordem exclusiva de Max (`CLAUDE.md` §13).

> ### ⏸️ PAUSA DA ETAPA 03 — sessão adicional `analise-ice` (2026-09-03)
>
> **A Etapa 03 está pausada na Subetapa 03.8, por ordem de Max, em 2026-09-03**, antes de qualquer linha de código dela. As sete subetapas concluídas (03.0, 03.2–03.7) continuam válidas e commitadas no branch `etapa-03/plano-mvp-odontologico`; nada foi desfeito.
>
> **Motivo:** Max localizou o **ICE Health System** (`https://help.icehealthsystems.com/` + canal de 32 vídeos no YouTube), um CRM odontológico documentado em nível de detalhe muito superior ao dos oito concorrentes do benchmark de 2026-08-31 — em especial no **odontograma** (dentes em SVG com face, raiz e região individualmente clicáveis; pop-up de detalhamento por dente com espaço próprio para trabalho e orçamento) e no **financeiro/orçamento**. Levantar isso agora é mais barato que descobri-lo depois: a 03.8 ainda não começou e a 03.7 acabou de fechar.
>
> **Como se retoma.** A pesquisa acontece numa **sessão própria, em branch próprio (`analise-ice`), restrita a `design/benchmark/`** — nenhum arquivo de `crm/`, `db/` ou `docs/` é tocado por ela sem passar por relatório de impacto aprovado por Max. A sequência acordada é: plano de ação → aprovação de Max → coleta (site + vídeos) → aprovação de Max sobre evidências e relatórios → **relatório de impacto** dizendo o que muda na Etapa 03 já executada → aprovação de Max → merge em `main` **ordenado por Max** (`CLAUDE.md` §13) → prompt de retomada da Etapa 03.
>
> **Expectativa declarada, não escondida:** o relatório de impacto provavelmente **reabre subetapas já marcadas `✅ CONCLUÍDA`**, com destaque para a **03.7 (odontograma)**, e redefine o desenho da **03.8 (orçamento)** antes de ela nascer. Reabrir subetapa concluída por decisão de Max é caminho previsto — o Status antigo não se apaga, ganha o registro da revisão ao lado.

> ### ▶️ RETOMADA DA ETAPA 03 — a pausa fechou em 2026-09-04
>
> **A pesquisa `analise-ice` foi executada, aprovada e fundida em `main` por ordem de Max**, e `main` foi fundida no branch `etapa-03/plano-mvp-odontologico` em 2026-09-04 (commit `6f694a2`). O bloco de pausa acima **fica inteiro, como registro** — a expectativa que ele declarou se confirmou ponto a ponto.
>
> **O que a pesquisa produziu:** 424 páginas do help center do ICE Health System e 32 vídeos (dois de ~50 min, com transcrição lida do início ao fim), 54 evidências creditadas em `design/benchmark/capturas/ice/INDICE.md`, a fonte detalhada em `design/benchmark/fontes/ice.md`, os itens 34-49 em `design/benchmark/RELATORIO.md` §5.4 e o parecer subetapa a subetapa em `design/benchmark/RELATORIO_DE_IMPACTO_ICE.md`.
>
> **O que ela mudou, e a expectativa estava certa:** duas subetapas concluídas reabrem (**03.6** e **03.7**, revisadas pelas novas **03.6.a** e **03.7.a**) e uma não iniciada é redesenhada por inteiro e partida em duas (**03.8** e **03.8.a**). Nenhuma outra é invalidada; **nenhuma migration precisa ser revertida**. O maior achado não foi sobre o ICE: `crm/src/features/health/odontograma.ts:96` lia a face do **achado** (onde há doença) onde o plano precisa da face do **trabalho** — código já escrito, já verde e errado quanto ao negócio.
>
> **Como a revisão foi conduzida:** pela **Subetapa 03.0.a**, aberta abaixo, em modo `[Plan]`, que respondeu as decisões D-I1 a D-I6 e reescreveu as subetapas antes de qualquer código. **O Status antigo de 03.6 e 03.7 não foi apagado** — ele fica onde está, e a revisão ganhou número próprio ao lado (`CLAUDE.md` §8 e §10).

---

### As seis decisões do Plano de Ação (D1–D6)

Tomadas em 2026-09-03, na sessão da Subetapa 03.0. As de Max estão datadas e nominadas; as do CODE trazem o raciocínio para poderem ser derrubadas.

| # | Decisão | Resposta | De quem |
|---|---|---|---|
| D1 | Onde entra o item 24 (multiunidade) | **Depois do bloco A, antes do bloco B** (Subetapa 03.9) | **Max, 2026-09-03** |
| D2 | Um portão adversarial ao fim, ou um por bloco | **Três portões completos** (03.9, 03.15 e 03.23) + P-sub nas demais | CODE |
| D3 | Como os módulos ficam ligáveis por nível de plano | **Camada nova, na mesma subetapa do item 24** (03.9) | **Max, 2026-09-03** |
| D4 | Quantas ondas e o que entra na primeira | **Cinco ondas**; a primeira é 03.3–03.5 | CODE |
| D5 | Se a Versão 03 já está decidida | **Decidida; implantação concentrada na Etapa 3 (03.22)** | **Max, 2026-09-03** |
| D6 | O que entra em `CHANGELOG.md` por subetapa | Regra abaixo; linha declarada em cada bloco `[Goal]` | CODE |

**Decisão extra de Max (2026-09-03), levantada pela medição da 03.0:** adotar `TOOL_Odontogram01` (`react-advanced-odontogram` 2.4.0, MIT, 426 KB gzip) em vez de estender o mecanismo nativo de marcação que a migration `025` já criou. A recomendação do CODE era a oposta; Max decidiu pela biblioteca. A escolha torna obrigatórias três restrições registradas na Qualidade da Subetapa 03.7 — não são opções de implementação.

**Regra de `CHANGELOG.md` (D6):** uma linha por subetapa que muda **o que uma clínica consegue fazer**. `+1.0` para capacidade nova, `+0.1` para correção ou melhoria, escrita como nota de versão do produto e não como lista de subetapas — o padrão que a entrada de 2026-08-22 estabeleceu. Duas exceções declaradas: a **03.2** (documentação pura) e a **03.10** (infraestrutura sem superfície de usuário; o valor aparece nos quatro consumidores dela). Os portões entram como `+0.1` dizendo o que foi atacado e o que se achou, no formato de 02.14 e 02.15.

> **[Superada em 2026-09-03 pela decisão D-I1 — o registro acima não se apaga.]** A pesquisa `analise-ice` mediu que o clique de `react-advanced-odontogram` é **no dente, nunca na face** (`src/odontogram.ts`: `tile.addEventListener("click", (e) => onToothClick(toothNo, e))`, e `onToothClick` só manipula `selectedTeeth`), e que o único popover é exclusivo de toque. Max decidiu em 2026-09-03 pelo **odontograma autoral**, com o trabalho anterior arquivado no branch `arquivo/odontograma-tool01` e na tag `odontograma-tool01`. Ver D-I1 abaixo e a Subetapa 03.7.a.

**Regra de `CHANGELOG.md` (D6):** uma linha por subetapa que muda **o que uma clínica consegue fazer**. `+1.0` para capacidade nova, `+0.1` para correção ou melhoria, escrita como nota de versão do produto e não como lista de subetapas — o padrão que a entrada de 2026-08-22 estabeleceu. Duas exceções declaradas: a **03.2** (documentação pura) e a **03.10** (infraestrutura sem superfície de usuário; o valor aparece nos quatro consumidores dela). Os portões entram como `+0.1` dizendo o que foi atacado e o que se achou, no formato de 02.14 e 02.15.

### As seis decisões da pesquisa `analise-ice` (D-I1–D-I6)

D-I1 e D-I2 foram tomadas por Max em **2026-09-03**, durante a pesquisa. D-I3 a D-I6 foram levadas a ele na **Subetapa 03.0.a** e respondidas em **2026-09-04**. Fonte de cada uma: `design/benchmark/RELATORIO_DE_IMPACTO_ICE.md` §6.

| # | Decisão | Resposta | De quem |
|---|---|---|---|
| D-I1 | O caminho do odontograma: (a) estender a biblioteca · (b) trocar de biblioteca · (c) construir o nosso | **(c) construir o nosso**, autoral e de fácil remodelagem, com o trabalho anterior **arquivado** em branch e tag, não apagado | **Max, 2026-09-03** |
| D-I2 | O nome do schema do orçamento: manter `aba_budget` ou passar a `aba_treatment` | **`aba_treatment`**, chave de módulo `treatment`, label **"Planos de tratamento"**. "Orçamento" continua sendo a palavra da interface | **Max, 2026-09-03** |
| D-I3 | Partir a 03.8 em 03.8 (clínica) + 03.8.a (financeira) | **Sim.** E, junto: **a 03.7 NÃO se parte** — remover a biblioteca e entregar metade do substituto deixa `/prontuario` quebrado; o instrumento certo ali é o teto, que sobe de 5 para 6 | **Max, 2026-09-04** |
| D-I4 | A forma de reabrir 03.6 e 03.7 | **Numerar `03.6.a` e `03.7.a`**, sem tocar no Status das originais. Reescrever no lugar exigiria um quinto marcador em `CLAUDE.md` §8, e nenhum dos quatro serve — `⚠️ PENDENTE` é definido como "sem ser um bloqueio", e a 03.7 refeita **bloqueia** a 03.8 pela corrente obrigatória | **Max, 2026-09-04** |
| D-I5 | Quais dos itens 34-49 entram no MVP | **Quatro: 34, 35, 36 e 41**, todos absorvidos pelas subetapas existentes, **sem criar subetapa nova**. O MVP passa de **26 para 30 itens**. Os demais (37, 38, 39, 40, 42-49) ficam reportados como futuro declarado | **Max, 2026-09-04** |
| D-I6 | Fazer agora as pesquisas dedicadas de periodontia (48) e ortodontia (49) | **Não agora** — os dois itens são futuro e o parecer não teria consumidor. **Mas a opção fica preservada por escrito:** o material bruto continua em disco, medido em 2026-09-04 (`~/.claude/jobs/analise-ice/`, **95 MB**), e a pesquisa é viável **sem nova coleta**. Virou pendência vigiada | **Max, 2026-09-04** |

**Por que D-I5 não cria subetapa nova.** Os itens **34** (opções concorrentes A/B) e **36** (recusa implícita e re-consentimento) não são acréscimos à 03.8: são a **forma** da entidade que ela constrói — fazê-la sem eles é fazê-la duas vezes. O **35** (requisitos por código) é coluna aditiva em `aba_catalog`, e o nosso `aceita_faces` já era o primeiro membro dessa família, sozinho. O **41** (vigência de tabela de preço) é o único dos quatro **sem conserto retroativo**: sem ele um reajuste é `UPDATE` e reescreve o passado do financeiro.

**Por que 37, 38, 39 e 40 ficaram de fora, com o custo declarado.** O **37** (step set) só se paga com tratamento multissessão faturado — **mas a 03.8.a nasce medindo cobertura de execução por FACE, não por procedimento, e é isso que o torna aditivo depois em vez de reescrita**. O **38** (consulta não agendada + prancheta) tem valor real de balcão e é expansão de escopo (`CLAUDE.md` §15). O **39** (*warnings*) fica registrado como **mecanismo** que a 03.17 pode adotar se couber, não como item. O **40** (nota administrativa com reconhecimento forçado) é barato e isolado, e ainda assim é item novo.

### As dez decisões de vocabulário e contrato (D-V1–D-V10)

Tomadas por Max em **2026-09-04**, na conversa aberta por ele ao ler o Status da Subetapa 03.6.a. O gatilho foi uma pergunta de precisão — *"'plano' e 'convênio' são coisas distintas; verifique se o CRM não as está tratando como sinônimo"* —, e a varredura que ela provocou achou mais do que a pergunta supunha: **a palavra "plano" tinha quatro donos diferentes dentro do mesmo produto**, e "convênio" não tinha nenhum. Fonte de cada uma: a própria conversa, com as medições registradas abaixo.

| # | Decisão | Resposta de Max | Quem/quando |
|---|---|---|---|
| D-V1 | Cada sentido da palavra "plano" ganha um termo próprio | **Quatro termos, quatro donos:** **pacote** (combo padrão ofertado a todos), **plano** (planejamento clínico personalizado de um paciente), **nível** (faixa comercial do produto — Bronze/Prata/Ouro/Diamante), **convênio** (operadora de saúde do paciente). E `serviço` volta a ser o guarda-chuva: a unidade atômica passa a se chamar **procedimento** | **Max, 2026-09-04** |
| D-V2 | Quando executar o renome | **Autonomia dada ao CODE**, com o critério declarado: menor gasto e menor risco sobre o backend. Decidido: **Subetapa 03.6.b, antes da 03.7.a** — a 03.8 vai criar `aba_treatment` e escrever a palavra "plano" no sentido certo em código novo, e renomear depois pagaria o mesmo refactor sobre superfície muito maior | **Max, 2026-09-04** |
| D-V3 | Como o contrato guarda itens de três tipos diferentes | **Arco exclusivo, não referência polimórfica.** `aba_finance.itens_contrato` com `procedimento_id`, `pacote_id` e `plano_id`, cada uma chave estrangeira **composta por `account_id`**, e um `CHECK` garantindo que exatamente uma está preenchida. O "cardápio" que Max desenhou como tabela de associação vira uma **view** (`aba_catalog.ofertas`), que não guarda dado e por isso não duplica preço | **Max, 2026-09-04**, sobre proposta do CODE |
| D-V4 | Aditivo de contrato | **Não existe no MVP.** Acréscimo de serviço ou de tempo é **contrato novo**; o vínculo entre os dois fica implícito pelo `cliente_id` compartilhado. Aditivo como documento vinculado ao contrato-pai fica para versionamento futuro | **Max, 2026-09-04** |
| D-V5 | Convênio no MVP | **Abortado por inteiro.** Nenhuma tabela, nenhuma RLS, nenhuma view, nenhuma coluna. O MVP trabalha só com os preços padronizados pelo próprio consultório; operadora, franquia, carência, cobertura e exceção continuam controle manual da recepção. Fica **provisionado** em `docs/02_MODELO_DE_DADOS.md` §13.4, para que o desenho de hoje não o impeça amanhã | **Max, 2026-09-04** |
| D-V6 | Partir a 03.8.a | **Sim.** Ela acabou de ganhar uma entidade inteira (o contrato heterogêneo). Vira **03.8.a — Orçamento** (preço resolvido por escada e vigência de tabela, item 41) e **03.8.b — Contrato** (itens heterogêneos, assinatura e a trava dupla de finalização) | **Max, 2026-09-04** |
| D-V7 | Autoria do plano clínico, e o que acontece quando o profissional sai | **Só o profissional autor edita o próprio plano** (RBAC `agent` + profissional, mais IBAC pelo autor). O próprio autor indica, antes de sair, o **coautor** que assume — no mesmo regime de troca de segredo profissional das referências e contrarreferências. **Se não indicar** (inclusive por morte), no momento em que o `owner` marca aquele profissional como **inativo**, os planos e contratos dele passam **automaticamente ao `owner`**, a quem cabe executar ou redistribuir — porque é comum o `owner` não ser dentista e não poder executar | **Max, 2026-09-04** |
| D-V8 | Contrato assinado como pré-requisito de execução | **Sim, por padrão e para todo serviço** — procedimento, pacote ou plano, mesmo para um trabalho único e simples como uma limpeza. Espelha a exigência do CFO de assinatura das duas partes no prontuário a cada trabalho, e sem ele a clínica corre risco judicial sério. Aprovada a proposta, o fluxo é automático em oito passos (gerar o documento → assinatura do profissional logado → escolher a via → enviar o token → paciente assina e devolve → CRM recebe → registra → **libera a execução**). **O `owner` pode desligar a exigência para processos específicos, assumindo o risco por escrito** | **Max, 2026-09-04** |
| D-V9 | Como a 03.8.b entrega "contrato assinado" se a assinatura por link é da 03.12 | **A 03.8.b entrega a estrutura, o estado `assinado` e a invariante**, com **assinatura presencial registrada** das duas partes; o canal por link/token (WhatsApp, SMS, e-mail, QR) chega na **03.12** e passa a alimentar o mesmo estado. Entrega a proteção jurídica desde já sem reordenar a Onda 4 inteira | **Max, 2026-09-04**, sobre recomendação do CODE |
| D-V10 | Como gerar o documento do contrato, já que `window.print()` exige uma pessoa diante da tela | **Documento canônico em HTML determinístico + hash de conteúdo, gerado no aceite da proposta**; o PDF se renderiza **só no momento de enviar ou arquivar**. O valor probatório vem do hash sobre o conteúdo canônico, não do formato do arquivo — e nenhuma dependência de PDF **volta** ao projeto (a 03.7 removeu a cadeia `jspdf`/`dompurify`) **[Correção de fato, 2026-09-04 — medida, não deduzida:** a cadeia `jspdf`/`dompurify`/`html2canvas` **ainda está no projeto hoje**, entrando de forma transitiva por `react-advanced-odontogram` (`npm ls jspdf` mostra a árvore), e pesa **754 KB** no chunk preguiçoso do odontograma. A 03.7 **não** a removeu — ela escondeu por governança a barra de exportação do aplicativo de demonstração, que é outra coisa. Quem a remove é a **03.7.a**, e o próprio plano dela diz isso no futuro: *"`npm audit` conferido depois da **saída** da cadeia"*. O CODE leu esse futuro como passado. **A decisão D-V10 não muda** — ela se sustenta pelo hash sobre conteúdo canônico, não por qual dependência está instalada. **[Cumprido em 2026-09-04 pela Subetapa 03.7.a: `npm ls jspdf dompurify html2canvas` devolve vazio, e 22 pacotes saíram do lock com 0 entrando.]****]** | **Max, 2026-09-04**, sobre recomendação do CODE |

> **A medição que decidiu a D-V3, e é a mais importante desta tabela.** A proposta original de Max era uma "tabela de associação" com uma coluna `tipo` (enum) mais uma coluna `id` apontando para uma das três tabelas. Isso é uma **referência polimórfica**, e ela **não pode ter `REFERENCES`**. As **10 chaves estrangeiras** que hoje apontam para `aba_catalog.servicos` e `aba_catalog.planos` foram medidas em 2026-09-04 e **todas as 10 são compostas por `account_id`**, resultado do achado da Subetapa 02.15 (a integridade referencial ignora RLS por especificação, então `account_id` na filha isola a linha mas não impede que ela **aponte** para linha de outra conta). A defesa permanente contra a 11ª chave desprotegida é `public.fks_sem_isolamento_de_conta()`, e ela filtra `WHERE con.contype = 'f'` (`039_auditoria_isolamento_de_conta.sql:55`) — ou seja, **uma coluna polimórfica não seria sinalizada como desprotegida: ela seria invisível para a auditoria**. O efeito prático: nada impediria a linha de um contrato de apontar para o pacote de outra clínica, ou para o plano de outro paciente, e nenhum teste ficaria vermelho.

### Dois níveis de portão de segurança na Etapa 03

A exigência de portão onde a subetapa toca `aba_health`, o núcleo de permissão ou endpoint público — os três lugares onde este projeto já teve achado real — é cumprida em dois níveis, para não transformar 22 subetapas em 22 benches.

| Nível | Gatilho | O que é |
|---|---|---|
| **P-sub** (portão de subetapa) | subetapa que toca `aba_health`, o núcleo de permissão ou endpoint público | casos de **ataque** novos e permanentes na suíte (não caminho feliz), `get_advisors` sem achado novo, conferência de privilégio de coluna, e — em endpoint público — a prova do freio por token. Roda dentro da própria subetapa, sem bench isolado. |
| **Portão completo** | Subetapas 03.9, 03.15 e 03.23 | os 7 passos da pendência vigiada "Portão de segurança adversarial obrigatório" (definição normativa em `docs/00_PLANO_E_CRITERIOS.md` → Pendências vigiadas — não duplicada aqui): bench isolado, ataque deliberado, registro em `handoffs/instrucoes.md`, achados viram itens `[Goal]`, execução até 100% verde ou teto, relatório com parecer explícito, e **o CODE nunca executa o merge** (`CLAUDE.md` §13). |

**Qualidade fixa de toda subetapa da Etapa 03, não repetida item a item** (herdada da 02.0 e das correções de 01.8/02.15): `.select()` sempre com colunas explícitas em tabela com narrowing — `select('*')` devolve `42501`, que parece falha de RLS; todo conteúdo vindo do banco renderizado escapado; nenhuma checagem de permissão duplicada no client; toda função nova com `REVOKE EXECUTE FROM PUBLIC` **e** `FROM anon`; toda chave estrangeira multi-inquilino composta por `account_id` (o teste de `039_auditoria_isolamento_de_conta.sql` falha sozinho se a próxima nascer desprotegida).

### Subetapa 03.0 — Leitura de Referências e Revisão do Plano da Etapa 03 [Plan] [LLM: Opus] · Status: ✅ CONCLUÍDA
Objetivo: instância da convenção `0X.0` para a Etapa 03. Reler `HANDOFF_UPGRADE.md`, `handoffs/HANDOFF_PLANO_MVP.md`, o backlog de versionamento e as pendências vigiadas; confirmar que o escopo a abrir ainda faz sentido frente ao estado real do produto; produzir o Plano de Ação das Etapas 1 a 4 do roteiro de sete.
Conclusão: Plano de Ação apresentado a Max e aprovado; as seis decisões D1–D6 respondidas ou perguntadas explicitamente; subetapas redigidas neste documento antes do primeiro commit de código.
Qualidade: nenhuma subetapa `03.n` aberta sem Conclusão/Qualidade/Evidência declaradas primeiro; nenhuma afirmação do handoff herdada sem confronto com o repositório (`CLAUDE.md` §11).
Evidência: esta seção da Etapa 03, publicada antes de qualquer código.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet na primeira; Opus na segunda.
Se esgotar: parar e emitir relatório curto.
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Opus, `[Plan]`, uma tentativa). Nenhuma implementação. **Cinco afirmações do handoff foram confrontadas com o repositório antes de virarem plano, e duas não sobreviveram:** (1) a tarefa 1 da Etapa 1 **já estava feita** — `design/benchmark/HANDOFF_PLANO_MVP.md` não existe mais e os três ponteiros já apontam para `handoffs/`, resolvidos no commit `4951cf8`; (2) **a armadilha 5 do handoff está errada na premissa** — `db/migrations/009_aba_scheduling.sql:259` já traz `nao_compareceu` no `CHECK` de status, que **é** o `faltou` traduzido pela convenção do `CLAUDE.md` §2, então a taxa de falta é calculável hoje e o que falta é `sala_de_espera` e o KPI (corrigido na 03.2, para não ser herdado como verdade); (3) **D3 não tinha a fundação que o handoff supunha** — `access.modules` é catálogo global sem `account_id`, `licensing` guarda só teto de assentos, e `access.can()` faz `IF v_role = 'owner' THEN RETURN TRUE` **antes** de consultar `module_permissions` (`003_core_access.sql:162`), de modo que uma trava de plano ali seria invisível justamente para quem contrata o plano; (4) **D1 e D3 mexem na mesma função** (`access.can()` resolve a conta com `WHERE user_id = auth.uid()`, sem parâmetro de conta), o que fundiu as duas numa subetapa só; (5) **o odontograma não partia do zero** — a migration `025` já persiste marcações em `aba_health.evolucoes.marcacoes`/`mapa_tipo` e `crm/src/features/health/mapas.ts:126` já tem a grade FDI de 32 dentes, tendo escolhido coluna em `evolucoes` de propósito para herdar RLS, log, privilégio de coluna e trava de evolução assinada. Os achados (3), (4) e (5) foram levados a Max como decisão; ele respondeu D1, D3, D5 e a adoção do `TOOL_Odontogram01`. Plano aprovado por Max em 2026-09-03, com a instrução de executá-lo em branch próprio.

### Subetapa 03.0.a — Revisão do plano da Etapa 03 à luz da pesquisa `analise-ice` [Plan] [LLM: Opus] · Status: ✅ CONCLUÍDA
Objetivo: fechar a pausa da Etapa 03 e reconciliar o plano com o que a pesquisa `analise-ice` mediu. Quatro entregas: (a) fundir `main` no branch `etapa-03/plano-mvp-odontologico`, resolvendo o conflito previsto em `handoffs/instrucoes.md` **sem apagar entrada nenhuma**; (b) responder as decisões D-I3 a D-I6 com recomendação em cada uma; (c) reescrever as Subetapas 03.6, 03.7 e 03.8 — Objetivo, Conclusão, Qualidade, Evidência, teto de tentativas, escalonamento de LLM e CHANGELOG; (d) aplicar as mudanças em `docs/` e no `CLAUDE.md` §2. **Nenhuma linha de código** — sufixo `.a` no precedente de 03.13.a, 03.16.a e 03.21.a, inserindo sem renumerar.
Conclusão: plano da Etapa 03 coerente com o parecer, com as subetapas reabertas numeradas ao lado das originais e nenhum Status apagado; `CLAUDE.md` §2 com o décimo schema; e a corrente obrigatória redeclarada `03.6.a → 03.7.a → 03.8 → 03.8.a`.
Qualidade: **nenhuma afirmação herdada do handoff sem confronto com o repositório** (`CLAUDE.md` §11) — é a mesma régua que a 03.0 aplicou e que derrubou duas premissas. Nenhum Status antigo apagado e nenhuma decisão anterior reescrita: decisão superada ganha o registro ao lado, com data e motivo (`CLAUDE.md` §8 e §10). Item fora do MVP se **reporta**, não se implementa (`CLAUDE.md` §15) — os itens 34-49 entram em escopo só pela resposta de Max a D-I5. O CODE **não funde** nada em `main` (`CLAUDE.md` §13).
Evidência: o merge conferido por contagem (`82 + 75 − 72 = 85` entradas, `comm` contra os dois lados sem perda, `uniq -d` sem duplicata, `grep -c '^# INSTRUÇÕES'` devolvendo `1`); a lista de arquivos alterados submetida a Max **antes** de qualquer edição; e as quatro decisões respondidas por ele.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Opus do início ao fim — é revisão de plano, e a 03.0 mostrou que o custo de errar aqui é herdado por todas as subetapas seguintes.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **nenhum** — é planejamento puro e não muda nada para quem usa o produto. Mesma exceção declarada da 03.2, registrada e não omitida.
Status: ✅ CONCLUÍDA — executada em 2026-09-04 (Opus, `[Plan]`, uma tentativa). Plano apresentado a Max e **aprovado por inteiro**, inclusive D-I3, D-I4, D-I5 (com a contagem 26 → 30) e D-I6, e a edição do `CLAUDE.md` §2. **Três achados de medição, nenhum deles suposto:**
- **O handoff de retomada subcontou as ocorrências de `aba_budget`.** Ele listou quatro lugares e concluiu "nenhum outro"; o `grep` sobre o repositório inteiro achou **três a mais**, e um importa: `design/benchmark/RELATORIO.md:337` aponta o item 34 para `` `aba_budget` / Subetapa 03.8`` e é referência viva de D-I5. Os outros dois estão em `design/benchmark/00_PLANO_DE_ACAO_ICE.md` e são registro histórico, que **não** se corrige.
- **A contagem "24" está errada em mais lugares do que o handoff previu.** Além do `README.md`, o cabeçalho de `docs/00a_PLANO_ETAPA_03.md` diz "as **24** subetapas da Etapa 03" enquanto a própria tabela abaixo dele já tinha **26 linhas** — medido por `grep -c '^| 03\.'`. Com as quatro inserções desta revisão são **30**.
- **O material bruto da pesquisa continua em disco**, o que torna D-I6 uma opção real e não uma intenção: `~/.claude/jobs/analise-ice/` com **95 MB** (`site.json`, `imagens.json`, `img/`, `videos/`), medido em 2026-09-04. Como o diretório vive **fora do controle de versão** — a mesma classe de perda que `handoffs/instrucoes.md` §5 já registrou com a skill `/watch` —, a disponibilidade virou pendência vigiada em vez de ficar como lembrança.

### Subetapa 03.1 — Evolution GO como módulo pago [Manual] [LLM: Opus] · Status: ⏸️ ADIADA
Objetivo / Conclusão / Qualidade / Evidência: a definir quando o primeiro cliente contratar o canal — não abrir esta subetapa sem demanda real paga.
Status: ⏸️ ADIADA — decisão registrada na Subetapa 03.0 (2026-09-03). `CLAUDE.md` §15 mantém o Evolution GO fora do v01, não há demanda paga, e o `HANDOFF_UPGRADE.md` já recomendava reavaliar se ela continuava sendo a primeira da Etapa 03. O número **não é reaproveitado** — renumerar quebraria referências existentes, e a convenção do fim da Etapa 02 manda inserir sem renumerar. O trabalho do MVP odontológico começa na 03.2.

---

## ETAPA 03 · ETAPA 1 DO ROTEIRO — REESTABELECER A UNIFORMIDADE

### Subetapa 03.2 — Uniformidade do repositório [Plan] [LLM: Sonnet] · Status: ✅ CONCLUÍDA
Objetivo: pôr o material do benchmark nos lugares onde as sessões futuras vão procurá-lo, e corrigir o diagnóstico errado que a 03.0 mediu. Três frentes: (a) distribuir as 30 diretrizes de `design/benchmark/DIRETRIZES_FORA_DO_BENCHMARK.md` aos destinos já sugeridos — 10 para `docs/02_MODELO_DE_DADOS.md` e `docs/01_ARQUITETURA.md`, 10 para `docs/05_COMPLIANCE_E_ETICA.md`, 4 para este documento, 2 para `handoffs/instrucoes.md` (as armadilhas A5 e A6, que são medidas e não hipóteses), 1 para `CLAUDE.md` §14 e 5 para o backlog comercial; (b) registrar o benchmark neste documento como etapa de transição concluída; (c) corrigir em `handoffs/HANDOFF_PLANO_MVP.md` e em `design/benchmark/RELATORIO.md` §5(c) a afirmação de que falta `faltou` no enum de status, citando a medição.
Conclusão: nenhuma das 30 diretrizes sem destino aplicado ou sem motivo registrado para não ter destino; a correção do enum escrita nos dois documentos com a linha da migration citada; a tarefa 1 do handoff registrada como já resolvida em `4951cf8`, não refeita.
Qualidade: nenhuma entrada antiga apagada — o que ficar obsoleto se marca `[OBSOLETA — <motivo>]` (`CLAUDE.md` §10). **A diretriz P4 (nomear o CRM Sindcom como fonte de porte no `CLAUDE.md` §14) exige autorização explícita de Max** pelo cabeçalho do próprio `CLAUDE.md`: sem o sim, a diretriz é registrada e o arquivo **não** é editado. Diagnóstico errado em documento vivo é pior que nenhum diagnóstico — ele é herdado por sessões futuras como verdade (`CLAUDE.md` §11).
Evidência: `grep` mostrando cada uma das 30 diretrizes rastreável ao arquivo de destino, e o texto novo dos dois documentos corrigidos.
Esforço máximo do /goal: 2 tentativas
Escalonamento de LLM: Sonnet nas duas.
Se esgotar: parar e emitir relatório curto (problema + causas + alternativas).
CHANGELOG: **nenhum** — é documentação e não muda nada para quem usa o produto.
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Sonnet/Opus, `[Plan]`, uma tentativa). **As 30 diretrizes distribuídas, 30/30 com destino aplicado** (conferido por varredura, saída `diretrizes sem destino: 0 de 30`): A1/A3/A8/A9/A10 → `docs/02_MODELO_DE_DADOS.md` §11 (novo); A2/A7 + o efeito de arquitetura de C4 → `docs/01_ARQUITETURA.md` §8 (novo); C1–C10 + A6 → `docs/05_COMPLIANCE_E_ETICA.md` §5 (novo); A5/A6 + uma lição de método → `handoffs/instrucoes.md` §6; P1/P2/P4 → Pendências vigiadas deste documento; P3/E1–E6 → `docs/07_BACKLOG_COMERCIAL.md` (**arquivo novo** — o destino "backlog comercial" que as diretrizes pediam não existia como lugar, e cinco delas ficariam órfãs sem ele; referenciado no `README.md` para não nascer perdido). **C1 fechou de quebra uma pendência aberta desde a fundação** — `docs/05` §4 deixava "política exata de retenção a confirmar por jurisdição", e a jurisdição é federal com prazo de 20 anos (Lei 13.787/2018, Art. 6º). **A tarefa 1 do handoff não foi refeita:** `design/benchmark/HANDOFF_PLANO_MVP.md` já não existia e os três ponteiros já apontavam para `handoffs/`, resolvidos no commit `4951cf8` — medido por `ls` e `grep` antes de agir. **O diagnóstico errado foi corrigido nos três lugares onde estava escrito** e em nenhum deles apagado: `handoffs/HANDOFF_PLANO_MVP.md` (armadilha 5, agora com o motivo do erro registrado como aprendizado de método), `design/benchmark/RELATORIO.md` §5(c) (item 4 reescrito) e `design/benchmark/DIRETRIZES_FORA_DO_BENCHMARK.md` (diretriz A4 marcada `[OBSOLETA]`, conforme `CLAUDE.md` §10). **`CLAUDE.md` não foi editado na execução da subetapa** — a diretriz P4 exige autorização explícita de Max, e ela virou pendência vigiada com o risco declarado. **[Atualização de 2026-09-03, mesma data: Max autorizou]** — o §14 passou a nomear o CRM Sindcom ao lado do Maximus, delimitado ao domínio de comunicação externa por token, e a pendência foi fechada. A Subetapa 03.10 está desbloqueada.

---

## ETAPA 03 · ETAPA 2 DO ROTEIRO — OS 30 ITENS DO MVP

A lista canônica é `design/benchmark/fontes/MVP.xlsx`, transcrita em `design/benchmark/RELATORIO.md` §5(c). **Não reabrir a lista** — revisada por Max em 2026-09-02. Item inviável se **reporta**, não se remove.

**A lista foi reaberta uma vez, e o registro fica aqui.** Em 2026-09-03, ao pedir o dossiê do paciente (Subetapa 03.16.a), Max acrescentou dois itens ao MVP: **25 — Resumo do paciente** (aba inicial consolidando dados sociais, alergias, riscos e planos vigentes) e **26 — Prescrições de medicamento** (registro clínico novo em `aba_health`). São **26 itens**, portanto. **[A lista foi reaberta uma segunda vez em 2026-09-04, pela decisão D-I5.]** Max acrescentou quatro itens vindos da pesquisa `analise-ice`: **34 — Opções de tratamento concorrentes**, **35 — Requisitos por procedimento**, **36 — Recusa implícita e re-consentimento** e **41 — Vigência de tabela de preço**. Os números são os da sequência única de `RELATORIO.md` §5.4 e **não foram renumerados**. **São 30 itens.** Nenhum deles cria subetapa nova. A regra "não reabrir" continua valendo para o CODE — quem reabre é Max, com data e motivo escritos, e é isso que distingue uma decisão de um desvio de escopo (`CLAUDE.md` §15).

Cinco ondas. A ordem não é livre: a corrente de dependência **catálogo → odontograma → orçamento → contrato/financeiro** governa a onda 2, e o item 24 entra entre a onda 2 e a onda 4 por decisão de Max (D1), para que a reauditoria de políticas de RLS fique limitada a três subetapas e tudo que vier depois nasça com a semântica multiconta final.

### ONDA 1 — fundação que todas as demais herdam

### Subetapa 03.3 — Divisão por rota + fontes auto-hospedadas [Goal] [Manual] [LLM: Sonnet] · Status: ✅ CONCLUÍDA
Objetivo: `React.lazy` + `<Suspense>` nas 16 rotas de `crm/src/app/router.tsx` (hoje não há nenhum, medido na 03.0), e auto-hospedagem das cinco faces realmente usadas (IBM Plex Sans 400/500/600 e Mono 400/500), com a serifa servida só quando a conta a escolher.
Conclusão: JS inicial abaixo de **180 KB gzip**, o teto declarado em `design/ux/06_ORCAMENTO_DE_PESO.md` §4 (linha de base medida: 284 KB num único chunk); um chunk por rota; nenhuma requisição a `fonts.googleapis.com` no caminho crítico.
Qualidade: **medição antes e depois pelo método do §5 daquele documento, nunca estimativa** — é a subetapa que existe para produzir número. Não é acabamento: é **pré-requisito** do odontograma (diretriz P2), porque `react-advanced-odontogram` pesa 426 KB gzip e não pode entrar no bundle inicial. Feita agora, toda tela das ondas seguintes nasce preguiçosa; feita depois, vira retrabalho em 20 rotas. O argumento de LGPD da fonte remota (LG München I, 3 O 17493/20 — carregar Google Fonts transmite o IP do visitante) registrado em `docs/05_COMPLIANCE_E_ETICA.md`, não só no dossiê de UX.
Evidência: saída do `vite build` com a lista de chunks e o gzip de cada um, antes e depois, mais a aba de rede mostrando a ausência da requisição de terceiro.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+0.1** — o produto abre mais rápido, e isso é visível para quem usa.
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Sonnet, `[Goal]`, uma tentativa). **Linha de base medida antes de tocar em código:** 290.553 B gzip num único chunk (idêntico ao dossiê), CSS 6.101 B. **Depois:** chunk de entrada em **160.202 B gzip** (`vite build` reporta 160,36 KB; `gzip -c` do método do §5 do dossiê devolve 160.202 B) — dentro do teto de 180 KB —, mais 20 chunks por rota/vendor, o maior deles (`SalesKanbanPage`) com 17.443 B gzip. As 16 rotas usam `React.lazy()` + `<Suspense>`, com fronteira dentro do `AppShell` (mantém sidebar/header pintados na troca) e fronteiras próprias para `/login` e `/convite`, que ficam fora do shell. **Um achado da própria medição corrigiu o dossiê:** IBM Plex Sans no Google Fonts v23 é fonte variável (`fvar` confirmado lendo o cabeçalho do woff2), então as nove faces auto-hospedadas (Sans 400/500/600 num arquivo só + Mono 400/500 sempre + Serif 400/500/600 sob demanda) pesam **135.712 B**, não os ~286 KB que o dossiê projetava contando a Sans três vezes. **Outro achado da medição corrigiu a primeira versão desta própria subetapa:** `/login` tinha ficado eager por suposição ("é a porta de entrada"); a atribuição por pacote mostrou que ela sozinha arrastava `zod`+`react-hook-form`+`@hookform/resolvers` (232 KB brutos) para o chunk de entrada, cobrado em toda carga de página — preguiçá-la também foi o que tirou o número de 186,70 KB para 160,36 KB. `fonts.googleapis.com`/`fonts.gstatic.com` não aparecem em nenhum artefato de `dist/` (`grep` sobre HTML, CSS e todos os chunks JS, zero ocorrência fora de um comentário de código) — o `<link rel="stylesheet">` remoto saiu do `index.html` e entrou um `<link rel="preload">` local. Argumento de LGPD (LG München I) registrado em `docs/05_COMPLIANCE_E_ETICA.md` §6 (seção nova). Duas lições de método em `handoffs/instrucoes.md` §5.

### Subetapa 03.4 — Agendamento: espera, marcadores e cadeiras [Goal] [Manual] [LLM: Sonnet] · Status: ✅ CONCLUÍDA
Objetivo: itens 4, 9 e 11. Acrescentar `sala_de_espera` ao `CHECK` de status de `aba_scheduling.agendamentos`; criar marcador colorido no próprio agendamento (a cor existe hoje em `profissionais` e `recursos`, nunca no agendamento) com relatório por marcador; e expor na UI o controle de cadeiras e a ocupação da agenda, que já existem no banco (`recursos` + `horarios_recursos`) sem tela.
Conclusão: o estado novo é aceito pelo banco e visível na agenda; o relatório por marcador devolve contagem real; a taxa de falta é calculada sobre `nao_compareceu`, que **já existe** desde a migration `009` e dispensa migration nova.
Qualidade: estado novo em `CHECK` obriga a varrer quem filtra por lista literal de estado — `grep` por `status !==`, por `'agendado'` e pelos demais literais antes de fechar. O custo dessa omissão já foi medido neste projeto: o agendador da 02.10 zerou o KPI "Vencido" do Financeiro porque a fatura saía do contador exatamente quando virava pendência (`handoffs/instrucoes.md` §5). Quem escreve o estado é quem lembra dele; quem lê é quem esquece.
Evidência: query do `CHECK` aplicado + print da agenda com marcador, do relatório por marcador e da taxa de falta calculada.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+0.1**
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Sonnet, `[Goal]`, uma tentativa). Migration `040_agenda_sala_de_espera_marcadores.sql` aplicada em produção contra 600 agendamentos reais: `sala_de_espera` no `CHECK` (buscado por catálogo, não suposto — o Postgres reescreve `IN (...)` como `= ANY (ARRAY[...])`), tabela `marcadores` nova com FK composta por `account_id` desde a origem, "cadeiras" reaproveitando `recursos`/`horarios_recursos` já existentes sem tabela nova. Seis provas por SQL real em `BEGIN...ROLLBACK` (zero resíduo): CHECK aceita/recusa, sobreposição continua tratando `sala_de_espera` como ocupando agenda, marcador associado, **FK composta recusa marcador de outra conta**, `ON DELETE SET NULL (marcador_id)` preserva `account_id`. Auditoria `fks_sem_isolamento_de_conta()` zero linhas. Achado corrigido pela varredura de literais: o "Check-in" do Balcão conflava confirmação prévia com chegada física (`agendado→confirmado`); agora é `→sala_de_espera`, com lista real de espera na tela. Detalhe completo em `docs/00_PLANO_E_CRITERIOS.md`.

### Subetapa 03.5 — Ações dos usuários + consentimento de imagem [Goal] [Manual] [LLM: Opus] · P-sub · Status: ✅ CONCLUÍDA
Objetivo: itens 8 e 6, os dois mais baratos de maior valor comercial da rodada, porque o dado já está no banco. Item 8: relatório "Ações dos usuários" sobre `aba_health.log_acesso`, **visível apenas ao `owner`**. Item 6: consentimento de imagem visível na ficha, travando publicação — `aba_health.consentimentos` já existe e nunca teve tela.
Conclusão: o `owner` lê o relatório; `admin`, `agent` e `viewer` recebem conjunto vazio pela RLS, nunca por `if` de papel no front; a trava de consentimento aparece onde a foto é publicada.
Qualidade: **P-sub obrigatório** — toca `aba_health`. O relatório é agregado sobre tabela com RLS por linha, então precisa **declarar o alcance de quem pergunta** antes de contar (`aba_health.pode_acessar(NULL,'leitura')`): contador de ausência sobre tabela com RLS por linha **erra para cima**, e sem alcance clínico a tela mostra "sem alcance clínico", nunca um número (`handoffs/instrucoes.md` §5). Nenhuma leitura clínica por `select` direto — só pelas funções `ler_*()` que gravam o log na mesma transação. O Art. 14, III do Código de Ética Odontológica torna a trava de imagem requisito, não recurso. **Ver também a pendência vigiada "Consentimento de imagem trava exibição, não envio" no recorte abaixo** — relevante ao desenhar esta tela, mesmo sem mudar a decisão de Max de mantê-la como está.
Evidência: print do relatório como `owner` e como `admin` (conjunto vazio) + contagem de `log_acesso` antes e depois + casos de ataque novos e permanentes na suíte.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Opus do início ao fim — mesmo tratamento de 01.4/02.9.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+0.1**
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Sonnet — Opus indisponível na sessão, registrado por transparência —, `[Goal]`, uma tentativa). **Achado que reduziu o escopo real:** o item 6 (consentimento de imagem) já estava pronto desde a 02.9 (`ConsentimentosTab.tsx` + trava em `pode_acessar_anexo()`, migration `014`) — nenhum código escrito para ele. **Trabalho real: item 8.** Migration `041` tira `admin` de `log_acesso_select` (era `admin+` desde a 013, agora só `owner` — decisão de produto do próprio Objetivo). Relatório "Ações dos usuários" reescrito com agregação por usuário e nome resolvido, janela de 500 eventos declarada. Quatro casos de ataque novos e permanentes em `05_aba_health.spec.ts`; suíte de `aba_health` 24/24 e `aba_scheduling` 6/6. `get_advisors` sem achado novo. Detalhe completo em `docs/00_PLANO_E_CRITERIOS.md`.

### ONDA 2 — a corrente clínico-comercial (bloco A), em ordem obrigatória

### Subetapa 03.6 — Catálogo: faces, unidade e semente SIGTAP [Goal] [Manual] [LLM: Sonnet] · Status: ✅ CONCLUÍDA
Objetivo: itens 3 e 22 — o **primeiro elo** da corrente. Acrescentar a `aba_catalog.servicos` a marca `aceita_faces`, a `unidade_lancamento` (`dente`/`sextante`/`arcada`/`sessao`/`elemento`), a `quantidade_maxima` por unidade e o `codigo_sigtap`; e semear o catálogo com os 84 procedimentos da Atenção Básica de `design/benchmark/fontes/procedimentos.txt`, com o código SIGTAP de cada um.
Conclusão: procedimento marcado como "aceita faces", com unidade e teto declarados, e a semente entrando numa conta nova sem apagar catálogo existente.
Qualidade: a `quantidade_maxima` é **validação de banco, não rótulo** — 32 por dente, 6 por sextante, 2 por arcada ao longo dos 64 procedimentos da tabela nacional (`RELATORIO.md` §5.2): um orçamento com 33 restaurações no mesmo dente é recusado pelo banco, não só pela tela. A semente é opcional e idempotente — a conta que já cadastrou o catálogo dela não é sobrescrita. Código nacional padronizado é o que torna o catálogo interoperável e prepara TISS/TUSS sem retrabalho. Sem este elo o odontograma não sabe quais procedimentos aceitam face.
Evidência: query mostrando serviço com faces + unidade + código SIGTAP, e a recusa do teto por quantidade.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Sonnet, `[Goal]`, uma tentativa). **Correção de fonte:** `procedimentos.txt` (citado no Objetivo) não tem código nenhum; a fonte real é `SIGTAP.xlsx` (64 linhas com código, unidade e quantidade máxima), já nomeada em `RELATORIO.md` linha 22. Migration `042` aplicada em produção e teste: `aceita_faces`, `unidade_lancamento`, `quantidade_maxima`, `codigo_sigtap` em `aba_catalog.servicos` + 3 CHECKs + função de semente opcional/idempotente. Provado com SQL real em `BEGIN...ROLLBACK`: semente 64/0 na primeira chamada, 0/64 na segunda (idempotência); CHECKs recusando dado inválido; permissão recusando usuário sem conta. Enforcement do teto contra orçamento fica para a 03.8 (docs/02 §11.2 já previa isso). Suíte de `aba_catalog` 7/7. Detalhe completo em `docs/00_PLANO_E_CRITERIOS.md`.

**🔁 Revisada pela Subetapa 03.6.a (2026-09-04).** O Status acima **continua válido e não se apaga** — o que a 03.6 entregou está correto. A pesquisa `analise-ice` mostrou que ele é insuficiente em dois pontos: `aceita_faces` é booleano onde a regra do código sabe **quantas** faces aceita e **se é anterior ou posterior**; e o código carrega **requisitos** (termo de consentimento, termo informado, achado diagnóstico obrigatório) que o nosso catálogo não tem. Detalhe em `design/benchmark/RELATORIO_DE_IMPACTO_ICE.md` §2.2.

### Subetapa 03.6.a — Catálogo: regra do código e requisitos por procedimento [Goal] [Manual] [LLM: Sonnet] · Status: ✅ CONCLUÍDA
Objetivo: item 35 (entrou no MVP por D-I5) e a metade que faltou do item 3. Acrescentar a `aba_catalog.servicos` a **regra de forma do código** — quantas faces aceita (mínimo e máximo), se vale em dente anterior, posterior ou ambos, e a área aplicável — e os **três requisitos por código**: exige termo de consentimento, exige consentimento informado, exige achado diagnóstico vinculado. Colunas **aditivas**, sem tocar nas 64 linhas da semente SIGTAP nem em `unidade_lancamento`/`quantidade_maxima`, que a 03.6 entregou e continuam válidas.
Conclusão: um procedimento marcado como "restauração de 2 faces em dente posterior" faz o **banco** recusar a linha com 3 faces ou em dente anterior; e um procedimento que exige consentimento fica declarado assim no catálogo, pronto para a trava que a 03.8 cobra.
Qualidade: **`aceita_faces` não se apaga** — ou vira derivada da regra nova, ou fica marcada `[OBSOLETA — <motivo>]` (`CLAUDE.md` §10), e **quem filtrava por ela é revisado um a um** (armadilha da 02.15: estado novo num CHECK exige revisar quem filtrava pelo estado antigo). A semente é **reexecutada em `BEGIN...ROLLBACK`** para provar que continua idempotente com as colunas novas. Regra de forma é **validação de banco, não rótulo** — mesmo argumento que a 03.6 usou para `quantidade_maxima`; o efeito contra o plano acontece na 03.8, e é lá que se prova. **Sem P-sub:** `aba_catalog` não é `aba_health`, não é núcleo de permissão e não é endpoint público.
Evidência: query mostrando serviço com regra de forma + os três requisitos; a semente reexecutada devolvendo `{inseridos: 0, ja_existentes: 64}`; CHECKs recusando faces fora do intervalo e arco inválido; `crm/tests/rls/02_aba_catalog.spec.ts` 7/7 sem regressão; `get_advisors` sem achado novo.
Esforço máximo do /goal: 3 tentativas
Escalonamento de LLM: Sonnet nas 2 primeiras; Opus na última.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+0.1** — a clínica ganha campos numa tela que já existe; a capacidade nova só aparece na 03.8, que é `+1.0`.
Status: ✅ CONCLUÍDA — executada em 2026-09-04 (Opus, `[Goal]`, uma tentativa). Migration `043_catalogo_regra_do_codigo.sql` aplicada nos dois projetos (produção e testes). **Três medições feitas ANTES de escrever a migration, cada uma contra a fonte e não contra a descrição da subetapa:**
  1. **"A área aplicável" do Objetivo já existia — é `unidade_lancamento`, da 03.6, e NÃO virou coluna nova.** A fonte (`design/benchmark/fontes/ice.md` §3.3, citando o fornecedor: *"narrows the available codes or **areas** based on code rules"*) e a evidência `capturas/ice/site_18` mostram que o campo se chama **`Tooth/Area`** e é a seleção do **sítio** do procedimento — dente, quadrante, arcada, boca. Isso é exatamente `dente`/`sextante`/`arcada`/`sessao`/`elemento`. Criar uma `area_aplicavel` ao lado seria a mesma informação com dois nomes, divergindo na primeira tela que escrevesse só num deles. Em vez de duplicar, a coluna da 03.6 foi **amarrada** à regra nova pelo CHECK `servicos_faces_exigem_dente` (face é superfície de dente). Quarta vez seguida que confrontar o Objetivo com a fonte derruba uma premissa — 03.5, 03.6, 03.8 e agora 03.6.a.
  2. **O intervalo de faces é 1 a 5, medido no repositório.** `design/benchmark/gerar_dentes_svg.mjs` (fundação da 03.7.a) declara as faces de cada dente — `mesial, distal, vestibular, lingual, incisal|oclusal`. Cinco, com o centro trocando de nome entre anterior e posterior. Não foi número de memória.
  3. **`regiao_dentaria`, e não `arco`.** A Evidência desta subetapa escrevia "arco inválido"; em odontologia arco/arcada é a arcada superior ou inferior — conceito diferente de anterior × posterior —, e `arcada` já é valor de `unidade_lancamento` no mesmo schema. Usar a palavra do documento derivado repetiria a colisão que "convênio → plano" já custou (`handoffs/instrucoes.md` §5).
**Entregue:** seis colunas aditivas em `aba_catalog.servicos` — `faces_minimo`, `faces_maximo`, `regiao_dentaria`, `exige_consentimento_tratamento`, `exige_consentimento_informado`, `exige_achado_diagnostico` — mais três CHECKs (`servicos_faces_intervalo`, `servicos_faces_exigem_dente`, `servicos_regiao_dentaria_check`), o gatilho `derivar_aceita_faces()` e a semente republicada. **Nada foi apagado:** o CHECK `servicos_aceita_faces_exige_unidade` da 042 continua vivo e, medido, continua sendo ELE que dispara quando faces são declaradas sem unidade nenhuma.
**`aceita_faces` virou DERIVADA, não obsoleta** (a primeira das duas saídas que a Qualidade autorizava): gatilho `BEFORE INSERT OR UPDATE` a mantém igual a `faces_maximo IS NOT NULL`. **Os quatro consumidores foram revisados um a um** (armadilha da 02.15): o CHECK da 042 (fica), a função de semente (**republicada — sem isso uma conta que semeasse depois de 043 receberia as três restaurações com `aceita_faces = FALSE`, regressão silenciosa e verde**), `catalog/api.ts` (para de enviar a coluna, continua lendo) e `ServicosTab.tsx` (a caixa de seleção virou o intervalo de faces; os dois selos que LEEM a coluna ficaram intactos). Varredura por `aceita_faces|aceitaFaces` em `*.ts`/`*.tsx`/`*.sql`/`*.mjs` não achou nenhum quinto.
**Provado com SQL real em `BEGIN...ROLLBACK`** contra o projeto de teste: semente reexecutada com as colunas novas devolvendo `{inseridos:64, ja_existentes:0}` e depois `{inseridos:0, ja_existentes:64}`; a query da matriz mostrando as três restaurações com regra de forma (`1–5` faces, região `ambas`/`anterior`/`posterior`) e os três requisitos; **7 CHECKs recusando com SQLSTATE `23514` lido, não presumido** (faces 6, faces 0, intervalo invertido, intervalo pela metade, faces em código por arcada, faces sem unidade, região `lateral`); a combinação válida aceita; `aceita_faces` ignorando o valor enviado nos dois sentidos, no INSERT e no UPDATE; permissão recusando usuário sem conta (`42501`); zero resíduo após o `ROLLBACK`.
**O harness de prova deu falso verde na primeira execução e foi corrigido antes de valer como evidência:** sem `SAVEPOINT`, a primeira violação abortou a transação e os seis CHECKs seguintes devolveram `25P02` ("transaction is aborted") — que o `catch` contava como recusa. 6 de 7 estavam verdes sem terem sido testados. Corrigido com savepoint por tentativa e exigência do SQLSTATE `23514`. É a armadilha da 02.15 (`handoffs/instrucoes.md` §6) acontecendo de novo, agora do lado do teste.
**Backfill dos 64 já semeados:** só as três "restauração", com `regiao_dentaria` lida da **própria descrição oficial** do código ("dente permanente anterior"/"posterior") e faces `1–5`, que é o intervalo anatômico inteiro — o SIGTAP **não declara contagem de face para nenhum código**, e `1–5` significa "aceita face, e a fonte não estreita", nunca uma afirmação clínica nossa. Os três requisitos nascem `FALSE` nos 64: inventar quais procedimentos do SIGTAP são "de risco significativo" seria codificar de memória sobre matéria clínico-jurídica. Guarda de passivo na migration **para** (não converte em silêncio) se achar linha com `aceita_faces` e unidade diferente de `dente` — medido zero nos dois bancos.
**Sem P-sub, como o plano previa.** Suíte de RLS **inteira** rodada (lição da 02.2 — CHECK/trigger novo em tabela compartilhada exige a suíte toda, e `servicos` é fixture de scheduling/finance/dashboard): **195/196, 1 skipped pré-existente, 19/19 arquivos verdes**; `02_aba_catalog.spec.ts` **7/7 sem regressão**. Uma primeira execução acusou timeout em `07_aba_automations` (20 s); reexecutado isolado em 1,3 s e a suíte inteira reexecutada 100% verde — intermitência de carga, não regressão (o módulo não toca `aba_catalog`). `get_advisors` **sem achado novo** — nenhuma menção às duas funções de `aba_catalog`, confirmando o `REVOKE FROM PUBLIC`/`FROM anon`. `tsc --noEmit` verde, `vite build` verde, chunk de entrada **160,42 KB gzip** (inalterado — o código é da rota `/catalogo`, lazy desde a 03.3), precache 1.128,62 KiB sob o teto de 1.400 KiB.
**Duas pendências reportadas, nenhuma delas desta subetapa** (ver "Pendências vigiadas"): (a) `npm run build` termina vermelho na guarda `escopar_css_odontograma.mjs --verificar`, **medido também na árvore limpa em `65cedde`** — a causa é fim de linha (`core.autocrlf = true`, sem `.gitattributes`): o script compara em memória com LF contra o arquivo em disco com CRLF, e nunca passa num checkout Windows. A **03.7.a remove o script e o CSS** (restrição 5), então não se conserta aqui. (b) A cópia da semente em produção difere da do repositório por **um comentário de 2 linhas** que não foi transcrito na aplicação por MCP; comparação de catálogo entre os dois bancos: CHECKs, colunas, gatilho e `derivar_aceita_faces()` com md5 **idêntico**, e o corpo da semente **idêntico linha a linha desconsiderando comentários** (118 linhas, mesmo md5). Converge na próxima reaplicação, que é idempotente.

### Subetapa 03.6.b — Vocabulário do domínio: procedimento, pacote, plano e nível [Goal] [Manual] [LLM: Opus] · bench isolado · Status: ✅ CONCLUÍDA
Objetivo: decisões **D-V1** e **D-V2** de Max (2026-09-04). Desfazer a sobrecarga da palavra "plano" — que hoje tem **quatro donos** dentro do mesmo produto — e devolver a "serviço" o papel de guarda-chuva. **Renome puro, sem mudança de comportamento nenhuma.** No banco: `aba_catalog.servicos → procedimentos`, `variantes_servico → variantes_procedimento`, `planos → pacotes`, `itens_plano → itens_pacote`; `aba_finance.planos_cliente → pacotes_cliente`, `saldos_plano → saldos_pacote`, `extrato_plano → extrato_pacote`, `vender_plano() → vender_pacote()`, `expirar_planos() → expirar_pacotes()`, `planos_vencendo_em() → pacotes_vencendo_em()`, `contratos.plano_id → pacote_id`; `aba_scheduling.agendamento_servicos → agendamento_procedimentos`, `agendamentos.plano_cliente_id → pacote_cliente_id`; toda coluna `servico_id → procedimento_id` (9 tabelas em 3 schemas) e todo índice/constraint com o nome antigo. No CHECK de `aba_finance.pagamentos.forma_pagamento`, o valor `'plano'` passa a **`'saldo_pacote'`** — decisão do CODE, com o motivo registrado: aquele valor nunca foi forma de pagamento nem pagador, é **liquidação contra saldo pré-pago**, e chamá-lo de `'pacote'` reabriria a leitura errada ("pagou com o pacote"). Nos documentos vivos, "plano" no sentido comercial do produto passa a **nível**.
Conclusão: nenhuma tabela, coluna, função, índice ou rótulo de tela usa a palavra "plano" para outra coisa que não o **planejamento clínico**; a suíte inteira continua verde; o site continua funcionando igual.
Qualidade:
  - **Renome, nunca reescrita.** `ALTER ... RENAME`, que preserva OID, dado, índice, policy, gatilho e chave estrangeira — jamais recriação de tabela com cópia de dado. As **10 chaves estrangeiras compostas por `account_id`** que apontam para `servicos`/`planos` (medidas em 2026-09-04) sobrevivem por construção, e `public.fks_sem_isolamento_de_conta()` tem de continuar devolvendo **zero linhas** depois do renome — é o contrato que a Subetapa 02.15 deixou.
  - **Migration dirigida por catálogo, não por lista escrita à mão** — a lição da 02.15: lista manual protege o que alguém lembrou, varredura protege o que ninguém viu. O que não puder ser dirigido por catálogo — o valor `'plano'` do CHECK de `forma_pagamento`, com **2 linhas em produção** — é `UPDATE` explícito com a contagem conferida antes e depois.
  - **Bench isolado, e o merge é ordem de Max** (`CLAUDE.md` §13). É o maior refactor desde a fundação: **459 ocorrências de "plano" e 595 de "servico"** medidas em `db/migrations`, `crm/src`, `crm/tests` e `crm/scripts` em 2026-09-04.
  - **O histórico não se renomeia.** `design/benchmark/*`, os dois relatórios de portão adversarial e as entradas antigas de `handoffs/instrucoes.md` são registro do que foi medido numa data, e ficam **intactos** (`CLAUDE.md` §10) — cada um ganha **uma nota de vocabulário no topo** dizendo o que a palavra significava então. O renome alcança só o que é vivo: `CLAUDE.md`, `docs/00`, `docs/00a`, `docs/02`, a seção corrente de `instrucoes.md`, código, testes e seeds.
  - **A janela é agora, e é medida.** Produção tem **4 pacotes de catálogo, 4 itens, 8 vendidos, 12 saldos, 2 contratos com pacote e 2 pagamentos com forma `'plano'`** (2026-09-04). Depois da 03.8 a superfície multiplica, porque `aba_treatment` passa a escrever a palavra "plano" — no sentido certo — em código novo.
  - **Nenhuma capacidade nova, nenhuma tabela nova.** `itens_contrato` e a view `ofertas` são da 03.8.b; aqui só se troca vocabulário. Misturar as duas coisas tornaria impossível dizer, diante de um teste vermelho, se a causa foi o renome ou a estrutura nova.
Evidência: `fks_sem_isolamento_de_conta()` devolvendo zero linhas **antes e depois**; contagem de linhas por tabela **idêntica** antes e depois (o renome não pode perder dado); a suíte de RLS inteira verde no mesmo número da 03.6.a (195/196, 1 skipped); `grep` provando que nenhum identificador antigo sobreviveu no código vivo, e que nenhum identificador novo vazou para o histórico; `tsc --noEmit` e `vite build` verdes; conferência no navegador em Catálogo, Agenda e Financeiro; `get_advisors` sem achado novo.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Opus do início ao fim — o renome atravessa três schemas e toca dado de produção.
Se esgotar: parar e emitir relatório curto. **Não entregar renome pela metade:** meio renome deixa os dois vocabulários vivos ao mesmo tempo, que é pior que nenhum.
CHANGELOG: **+0.1** — para quem usa, muda o nome de uma aba e de alguns rótulos; nenhuma capacidade nova.
Status: ✅ CONCLUÍDA — executada em 2026-09-04 (Opus, `[Goal]`, uma tentativa) no branch `bench/03.6.b-vocabulario`, **com merge ordenado por Max no mesmo dia** (commit `51c42d9`, `CLAUDE.md` §13).
**Publicação, e um erro de sequência que fica registrado.** O renome do banco e a publicação do build são **um passo só**: entre um e outro, o site em produção consulta tabelas que não existem mais. Houve essa janela — o banco foi renomeado antes de o build subir, e Catálogo, Agenda e Financeiro ficaram quebrados até a publicação. O deploy foi executado por Max (o script foi recusado ao CODE pelo classificador de permissão): **49 arquivos enviados, 49 com TLS pleno, 0 divergências de integridade**, e HTTP 200 em `/`, `/configuracoes` e `/prontuario`, com `id="root"` e referência a `/assets/index-*.js` presentes. **A regra que fica, e vale para toda migration de renome:** publicar o build **antes** de renomear, ou tratar os dois como uma janela de manutenção declarada. Registrada em `handoffs/instrucoes.md` §6.
**Aplicação em produção, e a prova que faltava no bench.** No bench as tabelas de teste estavam vazias, então "contagem idêntica antes e depois" não tinha o que provar. Em produção há dado real, e a medição foi feita nas duas pontas: **13 contagens, 13 idênticas** — 600 agendamento_procedimentos, 12 saldos_pacote, 8 pacotes_cliente, 10 contratos, 6 procedimentos, 5 variantes, 4 pacotes, 4 itens, 4 regras de comissão, 8 lançamentos, 0 extrato. Os **2 pagamentos** com `forma_pagamento = 'plano'` viraram `'saldo_pacote'`, e a contagem do valor antigo caiu a **zero**. `fks_sem_isolamento_de_conta()` em **zero** depois. O job 5 do `pg_cron` passou a `SELECT aba_finance.expirar_pacotes();`. `get_advisors` **sem achado novo**.
**A aplicação manual foi conferida por catálogo, não por confiança.** Produção não tem conexão direta por desenho — `provisionar_banco.mjs` recusa apontar para lá —, então a `044` foi aplicada por MCP com o SQL transcrito. Para pegar erro de transcrição, produção e testes foram comparados por md5: **funções (10), tabelas e constraints de `aba_catalog` — os três md5 idênticos**. De quebra, isso **fechou a pendência aberta pela 03.6.a**: a cópia da semente em produção divergia do repositório por um comentário de 2 linhas, e a `044` reescreveu a função com o texto do arquivo.
**Entregue.** Migration `044_vocabulario_pacote_procedimento.sql`, sete seções, aplicada no projeto de teste e **rodada duas vezes seguidas** para provar idempotência. **8 tabelas** (`servicos → procedimentos`, `variantes_servico → variantes_procedimento`, `planos → pacotes`, `itens_plano → itens_pacote`, `planos_cliente → pacotes_cliente`, `saldos_plano → saldos_pacote`, `extrato_plano → extrato_pacote`, `agendamento_servicos → agendamento_procedimentos`), **16 colunas** em 3 schemas, **todas as constraints e índices**, **9 funções**, **1 job de `pg_cron`** e **1 valor de CHECK** (`forma_pagamento 'plano' → 'saldo_pacote'`). No código: **17 arquivos** por script de identificadores, mais os rótulos de tela e os tipos exportados à mão, e dois arquivos renomeados (`ServicosTab.tsx → ProcedimentosTab.tsx`, `PlanosTab.tsx → PacotesTab.tsx`).
**Colunas, constraints e índices foram dirigidos por catálogo, não por lista escrita à mão** (lição da 02.15). As **funções foram geradas** por `crm/scripts/gerar_renome_044.mjs`, que lê `pg_get_functiondef` do banco de teste e aplica substituições explícitas: transcrever nove corpos à mão numa subetapa que promete "renome sem mudança de comportamento" seria a forma mais provável de introduzir um defeito. A saída é **commitada** — quem revisa lê o SQL que vai rodar.
**Três coisas que o renome quebraria em silêncio, e que só apareceram por serem procuradas:**
  1. **Corpo de função plpgsql é TEXTO** e não segue `ALTER ... RENAME`. Nove funções — inclusive `public.resgatar_convite`, do núcleo — continuariam citando tabelas inexistentes, e o erro só apareceria quando alguém as chamasse.
  2. **`cron.job.command` também é texto.** O job 5 chama `expirar_planos()`; sem a seção 5 da migration o agendador passaria a chamar função inexistente, e o sintoma seria **ausência de comportamento** — pacote que nunca expira —, que não gera erro, não aparece em teste e não acusa em advisor.
  3. **Renomear função a torna NOVA para o Postgres, e função nova nasce executável por `PUBLIC`.** Este não foi previsto: foi **medido pela suíte**, que ficou vermelha em `11_adversarial_superficie.spec.ts` com *"aba_finance.expirar_pacotes foi executável por authenticated"*. `expirar_planos()` era exclusiva de `service_role` por ser rotina de agendador; a versão renomeada nasceu aberta. Mesmo efeito por outra causa em `estornar_sessao`, que não mudou de nome mas mudou de **parâmetro** — e o Postgres recusa isso em `CREATE OR REPLACE` (`cannot change name of input parameter`), obrigando `DROP`+`CREATE`. **Um teste adversarial escrito em outra subetapa pagou por esta**, que é o argumento mais forte que existe para eles serem permanentes.
**Um achado incidental, que não é desta subetapa e foi corrigido aqui.** A verificação nova (f) — escrita para impedir que um renome futuro afrouxe função de novo — encontrou `aba_people.desvincular_perfil_da_conta_antiga` **já executável por `PUBLIC` e por `anon` em produção**, criada pela `038` (Subetapa 02.15) sem o `REVOKE` que todas as outras funções de módulo têm. **Severidade medida, não presumida:** é função de **gatilho** (`RETURNS TRIGGER`, presa a `public.profiles`), o PostgREST não expõe função de gatilho e o Postgres recusa chamá-la fora de um gatilho — **não é exploit vivo**, é um privilégio que nunca deveria ter existido. Corrigida aqui pela regra que o projeto já pagou para aprender: hardening novo se varre contra o catálogo inteiro, não se aplica só onde nasceu.
**Uma armadilha que a lista branca evitou por pouco.** O corpo de `semear_procedimentos_sigtap()` traz as 64 linhas da tabela nacional, e uma delas é `('07.01.07.008-0', 'Plano inclinado', ...)` — um aparelho odontológico. Substituição cega de prosa teria gravado **"Pacote inclinado"** num catálogo de códigos de faturamento real, verde e sem sintoma. O gerador só troca prosa numa lista branca de funções cujo assunto é comprovadamente o pacote.
**`features/health` foi preservado de propósito** — lá "plano" já significa o planejamento clínico ("a carta de plano diverge da de status", `odontograma.ts:83`), que é exatamente o sentido que esta subetapa existe para proteger. E **"serviço" continua sendo o guarda-chuva** (D-V1): variável local e agregação de tela que dizem "serviço" não foram tocadas; o que mudou foi identificador de banco, tipo exportado e rótulo de tela do catálogo.
**Evidência.** `fks_sem_isolamento_de_conta()` devolvendo **zero** antes e depois; **0 tabelas, 0 colunas, 0 constraints, 0 índices e 0 funções** com nome antigo no banco (medido por catálogo, não por `grep`); **8 tabelas novas** e **3 funções novas** presentes; `grep` provando que nenhum identificador antigo sobreviveu no código vivo; suíte de RLS **inteira 195/196, 19/19 arquivos verdes** (1 skipped pré-existente, o mesmo número da 03.6.a); `tsc --noEmit` verde; `vite build` verde com o chunk de entrada em **160,42 KB gzip — idêntico** ao da 03.6.a; precache 1.129 KiB sob o teto de 1.400 KiB; a migration rodada **duas vezes** sem efeito na segunda.
**A própria migration se recusa a terminar torta:** a seção 7 faz **seis perguntas** — tabela antiga, coluna antiga, função citando nome antigo no corpo, job de cron chamando função renomeada, chave estrangeira sem `account_id`, e função de módulo executável por `PUBLIC` — e qualquer uma respondida errado derruba a transação inteira. Meio renome deixa os dois vocabulários vivos ao mesmo tempo, que é pior que nenhum.
**O histórico não foi renomeado** (`CLAUDE.md` §10). `design/benchmark/RELATORIO.md`, `RELATORIO_DE_IMPACTO_ICE.md` e `handoffs/HANDOFF_RETOMADA_ETAPA_03.md` ganharam **uma nota de vocabulário no topo** dizendo o que cada palavra significava então e o que significa agora; o texto deles ficou intacto. Nos planos, o mesmo critério: Objetivo e Status de subetapa já executada são registro e não se reescrevem; o texto prospectivo já nasce com o vocabulário novo.
**Pendência alheia, inalterada:** `npm run build` continua vermelho no último elo (`escopar_css_odontograma.mjs --verificar`, fim de linha CRLF × LF), medido também na árvore limpa antes desta subetapa. A 03.7.a remove o script.

### Subetapa 03.7 — Odontograma [Goal] [Manual] [LLM: Opus] · P-sub · Status: ✅ CONCLUÍDA
Objetivo: item 2 — odontograma com dentição permanente, decídua e **mista**, e estados *a realizar / executado / existente*, adotando `react-advanced-odontogram` 2.4.0 (MIT, `github.com/ZoliQua/React-Odontogram-Modul`) por decisão de Max de 2026-09-03.
Conclusão: o profissional marca dente e face pela UI, a marcação persiste, reaparece na sessão seguinte, e o orçamento da 03.8 consegue ler dente e face daí.
Qualidade — **três restrições que a escolha da biblioteca torna obrigatórias, não opções de implementação:**
  1. **Só atrás de `React.lazy`** na rota do prontuário clínico, nunca no carregamento inicial. O núcleo pesa **426 KB gzip medidos** contra os 284 KB do bundle inteiro do Vitrine — 1,5× o app. A 03.3 é pré-requisito conferido por medição do bundle **antes** de instalar a dependência (`design/benchmark/fontes/REPOS.md` §1; diretriz P2). **A 03.3 já entrega o chunk de entrada em 160.202 B gzip, com 20 KB de folga até o teto de 180 KB — a folga inteira desta subetapa.**
  2. **Tradução `pt-BR` completa é trabalho desta subetapa, não pendência.** Os 11 idiomas de `src/i18n/translations.ts` são `ar, de, en, es, fr, hu, it, pl, ru, sk, zh` — não há português, e o vocabulário odontológico é técnico.
  3. **O estado clínico grava em `aba_health.evolucoes.marcacoes`**, o modelo que a migration `025` já criou — **nunca em tabela clínica nova**. A `025` escolheu coluna em `evolucoes` de propósito, para herdar RLS por `pode_acessar()`, log de escrita por trigger, privilégio de coluna (as colunas não recebem `GRANT SELECT`, saindo só por `ler_evolucoes()`) e a trava de evolução assinada. Tabela própria reabriria quatro superfícies no schema de maior risco jurídico do produto, que `CLAUDE.md` §5 declara sem exceção por nenhum motivo. A ponte entre o modelo de dados próprio do componente e essa coluna é **adaptador**, não schema novo.
O `src/fhir/` do componente (HL7 FHIR R4, `iso3950`, ICDAS) vem de brinde e é exatamente o vocabulário que a certificação SBIS/CFM pede — registrar como ativo, sem abrir escopo (item 33 é `+1.0`).
Evidência: bundle antes e depois provando que a rota inicial não cresceu; marcação gravada e relida por `ler_evolucoes()` com a linha correspondente em `log_acesso`; casos de ataque novos e permanentes na suíte.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim — toca `aba_health` e introduz dependência pesada.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
Status: ✅ CONCLUÍDA — executada em 2026-09-03 (Opus do início ao fim, `[Goal]`, uma tentativa). **Nenhuma migration** — a restrição 3 foi cumprida ao pé da letra: grava em `aba_health.evolucoes.marcacoes`/`mapa_tipo`, criadas pela `025`, sem tabela, política ou função nova.
**As três restrições:** (1) preguiça em dois níveis — chunk de entrada **160.212 → 160.263 B gzip (+51 B)**, odontograma isolado em **414.738 B gzip**, e a rede provando no navegador que ele só viaja ao abrir a aba; (2) **a restrição da tradução estava desatualizada** — a 2.4.0 tem 12 idiomas com `pt-br` e **907/907 chaves** de vocabulário odontológico brasileiro correto, restando três strings em inglês que a biblioteca não permite sobrescrever (sem API de override), declaradas como resíduo; (3) adaptador com projeção legível por dente (com `faces`, que a 03.8 consome) + item sentinela com o payload podado, invisível por construção a todo leitor anterior.
**Estados:** `existente`, `a_realizar` e `executado` — este último **derivado, nunca digitado**. Dentição mista veio de graça: a biblioteca modela decídua como substrato das mesmas 32 posições FDI, e a hipótese de estender o catálogo para 51–85 foi derrubada pela medição antes de virar código.
**Cinco achados da medição, três com custo real evitado:** o CSS da biblioteca sequestraria `--card`/`--muted`/`--accent` do app (escopado offline com PostCSS); o precache do PWA desfazia a divisão por rota, **4.404 → 1.124 KiB**, com guarda que quebra o build acima de 1.400 KiB; o singleton de módulo vazava a boca de um paciente para o seguinte, e `importStatus({})` não limpa `globals` — inclusive `edentulous`, que é achado clínico; a suíte ficou **27/27 verde com a arcada em 0px de largura**, porque media a nossa projeção e não o desenho; e a barra do aplicativo de demonstração foi escondida **por governança** — os botões de exportar baixavam o prontuário em um clique, que é o que a **03.13** vai construir com token e segunda prova de identidade.
**Mudança de tela pedida por Max durante a execução:** os quatro mapas viraram **abas do mesmo bloco** de Anamnese/Evoluções/Anexos/Consentimentos, em largura cheia — `.chart-column` passou de 458px para 726px e o layout nativo da biblioteca cabe sem reescrita de grade.
**P-sub:** 6 casos de ataque novos e permanentes (**30/30** em `05_aba_health.spec.ts`), privilégio de coluna medido (`marcacoes`/`mapa_tipo` sem `SELECT` para `authenticated` e `anon`), `get_advisors` sem achado novo, e `npm audit` sem nenhuma vulnerabilidade vinda da cadeia nova. **Evidência em navegador real: 37/37**, com ciclo clínico completo (abrir → marcar → salvar → recarregar → trocar de paciente) e limpeza por diferença de conjunto. Detalhe completo em `docs/00_PLANO_E_CRITERIOS.md`.

**🔁 Revisada pela Subetapa 03.7.a (2026-09-04).** O Status acima **continua válido e não se apaga** — as três restrições foram cumpridas, as cinco medições valem, e o que a 03.7 aprendeu é o que a 03.7.a herda como método. O que mudou é a **peça**, por decisão D-I1 de Max: o clique de `react-advanced-odontogram` é no **dente**, nunca na face, e o único popover é exclusivo de toque — medido no sourcemap do pacote, não suposto. Mais três achados de modelo (A2 face de achado × face de trabalho, A3 dentição como estado por dente, A4 três estados contra cinco). Detalhe em `design/benchmark/RELATORIO_DE_IMPACTO_ICE.md` §2.1. **O trabalho da 03.7 está arquivado, não apagado:** branch `arquivo/odontograma-tool01` e tag `odontograma-tool01`, ambos em `7f15dc5`, empurrados para `origin`.

### Subetapa 03.7.a — Odontograma autoral (substitui a implementação da 03.7) [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: decisão **D-I1** de Max (2026-09-03). Trocar `react-advanced-odontogram` por um odontograma **autoral em SVG paramétrico**, com **face clicável** e **pop-up por dente**, partindo de `design/benchmark/gerar_dentes_svg.mjs` — 3 dentes (incisivo 11, pré-molar 14, molar 16) com 5 faces + coroa + 1 a 3 raízes cada, **2,6-2,9 KB por dente**, validados por `provar_dentes_svg.mjs` em navegador real com **18/18 asserções verdes** e 24 cliques devolvendo região e dente corretos — e estendendo às **52 posições** (32 permanentes + 20 decíduas) por espelhamento e escala. E corrigir os três achados de modelo que a pesquisa expôs:
  - **A2 — a face do achado não é a face do trabalho.** `crm/src/features/health/odontograma.ts:96` soma `caries` + `fillingSurfaces`, que é **onde há doença**, e o comentário da linha 93, escrito na própria 03.7, diz *"É este conjunto que a 03.8 cobra por face"*. A face que se orça é onde o profissional vai **trabalhar** — pode coincidir, pode ser maior (restauração MOD sobre cárie só na oclusal) ou pode não existir como achado (selante em face hígida). É mudança de **projeção, não de schema**: a coluna é `jsonb`.
  - **A3 — a dentição vira estado por dente**: `erupcionado` / `nao_erupcionado` / `ausente` / `removido` / `supranumerario` / `substituido`, derivado da idade e editável dente a dente. As quatro maneiras de dizer "não está na boca" têm consequências **diferentes** sobre quais códigos são aceitos.
  - **A4 — três estados viram cinco**: `proposto` · `planejado` · `em_execucao` · `executado` · `nao_mais_necessario`, com **`executado` deixando de ser derivado e passando a fato afirmado**, com data e autor.
Conclusão: o profissional clica **na face**, o pop-up abre com o que está registrado e o que está planejado naquele dente; a marcação persiste em `aba_health.evolucoes.marcacoes`, reaparece na sessão seguinte, e a 03.8 lê **a face do trabalho**. `react-advanced-odontogram` sai de `crm/package.json` e o app continua verde.
Qualidade — **seis restrições, não opções de implementação:**
  1. **O SVG é contrato de nomes, nunca geometria.** Max declarou que provavelmente vai redesenhar os SVG: o componente depende só dos `id` e dos `data-face`/`data-regiao`, e quem trocar o desenho respeita os identificadores sem o componente perceber. **Validador que roda no build e falha se um dente perder uma região nomeada** — ele toma o lugar de `escopar_css_odontograma.mjs --verificar` na cadeia de `"build"` (`crm/package.json:8`, hoje `tsc --noEmit && vite build && conferir_precache.mjs && escopar_css_odontograma.mjs --verificar`). Sem ele, um redesenho silencioso quebra o orçamento e ninguém vê.
  2. **`executado` deixa de ser inferência.** A trava de finalização de contrato da 03.8.a se apoia nele, e trava financeira não se apoia em dedução entre duas evoluções. A mudança de semântica é registrada e os consumidores do estado antigo são revisados (armadilha da 02.15).
  3. **Continua gravando em `aba_health.evolucoes.marcacoes`** (migration `025`), **nunca em tabela clínica nova** — `CLAUDE.md` §5, e a `025` escolheu coluna em `evolucoes` de propósito para herdar RLS, log, privilégio de coluna e trava de evolução assinada. Com componente próprio o envelope **fica mais simples**: o item sentinela com o payload verbatim, a poda e a forma pristina **deixam de existir**, e a projeção legível passa a ser o dado inteiro.
  4. **Medir antes e depois**, pelo método do §5 de `design/ux/06_ORCAMENTO_DE_PESO.md`. Linha de base medida na 03.7: chunk isolado **414.738 B gzip + 10.459 B de CSS**, precache em **1.124 KiB** sob teto de **1.400 KiB**. A queda é esperada e provavelmente grande — **e é hipótese até ser medida** (`CLAUDE.md` §11); a 03.3 já mostrou que a projeção erra.
  5. **Nada se apaga do que ficou.** **Saem** `crm/scripts/escopar_css_odontograma.mjs` e `odontograma-escopado.css` (o objeto que domavam saiu), a forma pristina, o reset por `chaveSessao` e os `globIgnores` específicos do pacote. **Ficam** `crm/scripts/conferir_precache.mjs` e o teto de 1.400 KiB (protegem a **próxima** dependência pesada, seja qual for), os 6 casos de ataque de `crm/tests/rls/05_aba_health.spec.ts` (testam o banco, não o componente) e as entradas de `handoffs/instrucoes.md` §5 (`CLAUDE.md` §10). O branch `arquivo/odontograma-tool01` e a tag homônima **não se apagam** — Max os pediu para consultar ao longo do tempo.
  6. **P-sub:** os 6 casos de ataque existentes revalidados **mais** casos novos para o envelope sem sentinela; privilégio de coluna de `marcacoes`/`mapa_tipo` **remedido**, não presumido; `get_advisors` sem achado novo; `npm audit` conferido **depois** da saída da cadeia `jspdf`/`dompurify`.
Evidência: `crm/scripts/evidencia_odontograma.mjs` reescrito para o componente autoral, **mantendo o ciclo clínico que ele já exercita** — abrir sessão → marcar → salvar → conferir no banco → recarregar e as marcações voltarem → abrir outro paciente e a boca estar limpa — e trocando só as asserções que mediam a biblioteca; **asserção de largura da arcada e contagem de dentes no DOM preservadas**, porque a suíte da 03.7 ficou 27/27 verde com a arcada em 0px de largura e essa é a lição que não pode se perder; prova de clique **por face** devolvendo dente + região; saída do validador de SVG; bundle e precache antes e depois; `npm ls react-advanced-odontogram` vazio.
Esforço máximo do /goal: **6 tentativas** (era 5 na 03.7 — a subetapa cresceu e a biblioteca sai junto; decisão D-I3 de Max, 2026-09-04)
Escalonamento de LLM: Opus do início ao fim — toca `aba_health` e substitui a peça central da tela clínica.
Se esgotar: parar e emitir relatório curto. **Não partir a subetapa ao meio durante a execução:** remover a biblioteca e entregar metade do substituto deixa `/prontuario` quebrado.
CHANGELOG: **+1.0**
Status: ✅ CONCLUÍDA — executada em 2026-09-04 (Opus do início ao fim, `[Goal]`, uma tentativa das 6). **Nenhuma migration** — a restrição 3 foi cumprida ao pé da letra: continua gravando em `aba_health.evolucoes.marcacoes`/`mapa_tipo` (migration `025`), sem tabela, política ou função nova. A subetapa **não foi partida ao meio**: a biblioteca saiu e o substituto entrou no mesmo commit.

**As seis restrições:**
1. **O SVG é contrato de nomes.** 26 desenhos paramétricos em `crm/src/features/health/dentes/` (gerados por `scripts/gerar_dentes_svg.mjs`, **commitados**, instanciados nas 52 posições por espelhamento). O componente resolve o clique por `data-face`/`data-regiao` do alvo do evento — nunca por coordenada, nunca por `id` (26 arquivos × 52 posições dariam `id` repetido, que é HTML inválido). `scripts/validar_dentes_svg.mjs` **entrou na cadeia de `"build"` no lugar de `escopar_css_odontograma.mjs --verificar`** e cobra o contrato, não os bytes: 26 desenhos, 130 faces, 26 coroas. Teste negativo executado — renomear `data-face="mesial"` derruba o build com a mensagem certa.
2. **`executado` deixou de ser inferência.** Cinco estados por trabalho (`proposto` → `planejado` → `em_execucao` → `executado` | `nao_mais_necessario`), e `executado` grava `executadoEm` + `executadoPor` no ato de afirmar — e os apaga se o estado voltar atrás, porque data sobrevivente de trabalho que voltou para `planejado` afirmaria execução que não houve. Provado em navegador real e no banco.
3. **Grava em `aba_health.evolucoes.marcacoes`.** O envelope ficou **mais simples**, como o plano previa: o item sentinela `estado_nativo`, a poda e a forma pristina **deixaram de existir**, e a projeção legível passou a ser o dado inteiro. `marcacoesParaGravar()` na `ProntuarioPage` caiu para uma linha.
4. **Medido antes e depois** (§5 de `design/ux/06_ORCAMENTO_DE_PESO.md`): chunk do odontograma **414.741 → 6.174 B gzip (−98,5%)**, CSS dele **10.459 → 995 B (−90,5%)**, `dist/` inteiro **4.676.983 → 1.340.513 B (−71,3%)**, chunk de entrada **160.262 → 160.208 B**, `ProntuarioPage` **+1,13 KB gzip** (o catálogo das 52 posições agora vive no chunk ansioso). **`npm ls react-advanced-odontogram` vazio**, e **22 pacotes saíram do lock com 0 entrando** — a cadeia `jspdf`/`dompurify`/`html2canvas`/`canvg`/`core-js` inteira. **O precache SUBIU: 1.129,08 → 1.157,33 KiB (+28,25 KiB)**, contrariando a previsão de `RELATORIO_DE_IMPACTO_ICE.md` §2.1 — os `globIgnores` que mantinham o odontograma fora do precache saíram com a biblioteca (restrição 5), e os 52 KiB do autoral passaram a ser precacheados como qualquer tela. Continua sob o teto de 1.400 KiB, com 242 KiB de folga.
5. **Nada se apagou do que ficou.** Saíram `escopar_css_odontograma.mjs`, `odontograma-escopado.css`, `odontograma-integracao.css`, a forma pristina, o reset por `chaveSessao` e os seis `globIgnores` do pacote. Ficaram `conferir_precache.mjs` com o teto de 1.400 KiB, os casos de ataque de `05_aba_health.spec.ts` e as entradas de `handoffs/instrucoes.md` §5. `arquivo/odontograma-tool01` e a tag homônima **intocados**.
6. **P-sub: 33/33** em `05_aba_health.spec.ts` (eram 30/30 — os 6 ataques revalidados com o envelope novo, mais 3 casos que só o envelope novo torna possível errar). Privilégio de coluna **remedido no catálogo**, não presumido: `authenticated` tem `INSERT`/`UPDATE` e **nenhum `SELECT`** em `marcacoes`/`mapa_tipo`; `anon` não aparece na tabela de privilégios. `get_advisors` **sem achado novo** (os 13 são todos anteriores e já registrados). `npm audit` depois da saída da cadeia: **7 achados, todos pré-existentes e nenhum vindo dela** — 6 são de dependência de desenvolvimento (`vite`/`vitest`/`esbuild`/`vite-plugin-pwa`) e o sétimo (`fast-uri`, via `@hookform/resolvers`→`ajv`) **não chega ao bundle**, medido por varredura do `dist/`.

**Os três achados de modelo, corrigidos e provados:**
- **A2** — `faces` passou a carregar a face do **trabalho**, em vocabulário português (`mesial`/`distal`/`vestibular`/`lingual`/`oclusal`/`incisal`, o de `db/migrations/043`); a face do **achado** vive em `achados[].faces`. Provado pela UI real: cárie na oclusal + restauração MOD grava `faces: ["mesial","distal","oclusal"]` e `achados[0].faces: ["oclusal"]`.
- **A3** — dentição virou estado por posição, com as 52 posições sempre na tela e as 6 opções derivadas da idade do paciente (`aba_people.pessoas.data_nascimento`) e editáveis dente a dente. **Derivação nunca é gravada** — só o afirmado vai para o banco.
- **A4** — três estados viraram cinco, com `existente` sobrevivendo para o dente que tem achado e nenhum trabalho em aberto.

**Evidência em navegador real: 49/49**, com o ciclo clínico completo (abrir sessão → clicar na face → achado → trabalho → salvar → conferir no banco → recarregar e o desenho voltar pintado → trocar de paciente e a boca estar limpa), clique por face devolvendo dente + região, e limpeza por diferença de conjunto. **`npm run build` está VERDE** — a pendência que o arrastava desde antes da 03.6.a foi embora com o script que a causava.

**Três defeitos que a execução encontrou, todos medidos antes de escritos como causa** (`CLAUDE.md` §11), com entrada própria em `handoffs/instrucoes.md` §5: (a) a evidência escrevia dentro de sessão pré-existente de outra pessoa na conta de demonstração pública — limpeza por diferença de conjunto protege contra *apagar* o que não é seu e não contra *escrever* nele; a linha contaminada foi **restaurada** ao valor medido no início da sessão, não excluída; (b) `innerText` devolve o texto renderizado, e `text-transform: uppercase` deixou uma asserção vermelha sem defeito atrás dela; (c) a arcada permanente inferior caiu **abaixo da dobra** com 47/48 verdes — contagem no DOM e largura não somam "está visível", e a asserção nova mede o `bottom` das quatro linhas.

### Subetapa 03.8 — Plano de tratamento: a entidade clínica [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 1 na sua **metade clínica**, mais os itens **34** e **36** e o consumo do **35** (os três entraram no MVP por D-I5). Schema novo **`aba_treatment`**, chave de módulo **`treatment`**, label **"Planos de tratamento"** (D-I2, Max, 2026-09-03) — **"Orçamento" continua sendo a palavra da interface**, onde o paciente e a recepção a esperam. **A entidade não é uma lista de itens com preço: é uma matriz.** Linha = **fase clínica** (Emergency · Systemic · Acute · Disease Control · Definitive · Maintenance, configuráveis; o ordenamento é clínico, não comercial). Coluna = **opção de tratamento concorrente** (A, B, …, número livre). **O diagnóstico atravessa as colunas e o procedimento mora dentro de uma delas**, de modo que duas alternativas para a mesma cárie fiquem lado a lado. Diagnóstico ainda não fasado fica numa **fila de trabalho** à parte. Cada procedimento carrega código, dente, **faces do trabalho** (nunca as do achado), fase, opção, diagnóstico vinculado (opcional) e **estado** (`proposto → planejado → em_execucao → executado | nao_mais_necessario`). **Sem preço, sem contrato, sem fatura** — isso é a 03.8.a.
Conclusão: o profissional monta duas opções de tratamento para o mesmo diagnóstico a partir do odontograma; **selecionar N faces cria N linhas, uma por dente** — nunca uma linha com N dentes; consentir a Opção A move os procedimentos dela de `proposto` para `planejado` **e marca os da Opção B para o mesmo diagnóstico como recusados**; e só `proposto` se apaga — o resto some do plano e **permanece na lista geral**, porque o histórico do que se propôs é o que protege a clínica depois.
Qualidade:
  - **Dente e face são dado de saúde.** A leitura da linha respeita o regime de `aba_health`, e é por isso que a subetapa tem **P-sub** mesmo com o schema novo sendo `aba_treatment` e não uma tabela clínica. Toda chave estrangeira multi-inquilino composta por `account_id` — `db/migrations/039_auditoria_isolamento_de_conta.sql` falha sozinha se a próxima nascer desprotegida.
  - **A trava de requisito vive no banco, não na tela:** procedimento cujo código exige consentimento (03.6.a) e não o tem **não sai de `proposto`**. É o mesmo argumento que a 03.6 usou para `quantidade_maxima` — e é aqui que a `quantidade_maxima` também passa a ter efeito, como `docs/02_MODELO_DE_DADOS.md` §11.2 já declarava ("validação surtindo efeito na 03.8").
  - **Re-consentimento com gatilho explícito:** mudar **dente** ou mudar **código** exige termo novo; mudar **face**, **fase**, **opção** ou **diagnóstico vinculado** não exige. A regra mora em dado, não num `if` espalhado pela tela.
  - **Ciclo de estado terminal e garantido no `WHERE` do `UPDATE`**, no padrão que a 02.4 usou para `ativa → ganha | perdida`.
  - **Schema novo nasce com o hardening completo na própria migration**, não em patches depois (`handoffs/instrucoes.md` §5, lições da 01.2 e 01.3): `ENABLE ROW LEVEL SECURITY` explícito em cada tabela — o event trigger da plataforma só cobre `public` —, exposição ao PostgREST por `ALTER ROLE authenticator`, `GRANT` amplo **antes** do `REVOKE`/`GRANT` por coluna, `REVOKE EXECUTE ... FROM PUBLIC` **e** `FROM anon` em toda função, `ALTER DEFAULT PRIVILEGES` na mesma migration que cria o schema, e trigger que cruza schema morando no schema que **depende**.
  - **`aba_treatment` entra em `CLAUDE.md` §2 e em `access.modules`** — décimo schema de módulo. O §2 já foi atualizado na 03.0.a; `access.modules` é trabalho desta migration.
Evidência: query mostrando a matriz completa — duas opções sob o mesmo diagnóstico, em duas fases — montada a partir do odontograma; a recusa do teto por unidade da 03.6; a recusa de sair de `proposto` sem o consentimento que o código exige; a recusa implícita da Opção B registrada; casos de ataque novos e permanentes em `crm/tests/rls/19_aba_treatment.spec.ts`; privilégio de coluna medido; `get_advisors` sem achado novo.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
Status: ✅ CONCLUÍDA — executada em 2026-09-04 (Opus do início ao fim, `[Goal]`, uma tentativa das 5). **Migrations novas:** `045_aba_treatment.sql` (o schema inteiro) e `046_expose_aba_treatment_schema.sql` (exposição ao PostgREST). **Escopo decidido por Max nesta sessão: SÓ BANCO** — a Evidência do bloco é integralmente de banco, e a tela da matriz (fase na linha, opção na coluna) vem junto com o preço, na 03.8.a.

**Divergência de documento resolvida a favor do `CLAUDE.md`:** o Objetivo acima diz que o rótulo do módulo é "Planos de tratamento", texto escrito na 03.0.a. A decisão **D-V1, de 2026-09-04**, revisou o rótulo para **"Planos"**, e o `CLAUDE.md` §2 registra a revisão com autorização explícita de Max. `access.modules` recebeu **"Planos"** — regra permanente e mais nova vence texto de bloco. O Objetivo não se reescreve; a divergência fica registrada aqui.

**A matriz, como o desenho de `docs/02` §12 pedia:** `fases` (linha, seis semeadas por conta e configuráveis), `opcoes` (coluna, número livre), `diagnosticos` (**filhos do PLANO**, para atravessarem as colunas) e `procedimentos_plano` (a célula). **A fila de trabalho é DERIVADA** — diagnóstico sem procedimento nenhum, um `NOT EXISTS` — e não ganhou coluna própria: coluna a mais ali seria a mesma informação com dois donos, que é o defeito que a 03.6.a já pagou com `area_aplicavel` × `unidade_lancamento`. **`dente` é uma coluna de texto, nunca array**, e é a forma da tabela que garante "selecionar N faces cria N linhas, uma por dente". **Sem preço, sem contrato, sem fatura.**

**O regime de acesso foi portado, não reescrito** (`CLAUDE.md` §14): `aba_treatment.pode_planejar(cliente_id, acao)` é a conjunção de `aba_health.pode_acessar()` (o alcance clínico inteiro, fail-closed) com `access.can('treatment', ação)`. **Medido antes de decidir:** `access.module_permissions` tem UMA linha em produção e tudo corre pelo padrão; pôr `treatment` no ramo fechado de `default_permission` (onde `health` está) exigiria dois interruptores ligados à mão e faria uma concessão nominal de prontuário deixar de abrir o plano. A severidade já vem do primeiro fator.

**As travas que passaram a morder, todas do lado do banco:** forma do código da 03.6.a (unidade por dente, intervalo de faces, região dentária, face em código que não aceita face), **`quantidade_maxima` da 03.6** (trigger `AFTER`, porque em `BEFORE INSERT` o `count` mede N−1 e deixa passar sempre a última), a trava de consentimento (**as duas bandeiras com efeito próprio**), a trava de achado diagnóstico, o ciclo de estado com terminais garantidos, o re-consentimento com gatilho explícito (mudar **dente** ou **código** derruba o termo e devolve a linha para `proposto`; mudar face, fase, opção ou diagnóstico não), e a recusa implícita da opção concorrente **só para o mesmo diagnóstico**.

**`aba_health` cedeu duas coisas, as duas por adição:** `UNIQUE (id, account_id)` em `consentimentos` (a `035` a pusera em 25 tabelas-pai e essa ficara de fora por não ter FK apontando para ela) e o valor **`procedimento_informado`** no CHECK de `tipo`. O segundo não é conveniência: sem ele, `exige_consentimento_tratamento` e `exige_consentimento_informado` seriam satisfeitas pelo mesmo termo, e uma das duas colunas da 03.6.a não mudaria nada. `crm/src/features/health/api.ts` ganhou o quarto tipo, para a recepção poder coletar o termo que a trava exige — regra que existe no banco e é inalcançável pela tela é a pior das duas metades.

**Hardening completo na própria migration**, com **oito verificações que RECUSAM a migration** se algo não bater: RLS ligada em toda tabela, policy em toda tabela, zero FK de `aba_treatment` sem `account_id`, nenhuma função executável por `PUBLIC`/`anon`, `anon` sem alcance de tabela, ninguém com `TRUNCATE`, módulo no catálogo com o rótulo certo e as seis fases em toda conta.

**P-sub: 25/25** em `crm/tests/rls/19_aba_treatment.spec.ts` (fronteira, regra e histórico), mais **60/60** nas suítes vizinhas (`05_aba_health`, `16_configuracoes`, `17_adversarial_isolamento_fk`, `02_aba_catalog`) provando que a migration não quebrou nada. **Evidência da ponte: 18/18** em `crm/scripts/evidencia_plano_tratamento.mjs` — o plano montado **a partir do odontograma**, com a matriz impressa, a fila de trabalho derivada, as quatro recusas e o privilégio medido no catálogo. **`get_advisors` sem achado novo** (as mesmas 13 linhas anteriores, nenhuma citando `aba_treatment`).

**Aplicada primeiro no banco de TESTES e reaplicada lá duas vezes seguidas antes de ir para produção** — idempotência provada, não presumida. Em produção entrou pelo MCP em três partes (`045_aba_treatment_1_tabelas`, `_2_rls`, `_3_regras_modulo_semente`) mais a `046`; o arquivo no repositório é **um só**, e `crm/scripts/provisionar_banco.mjs` prova que `db/migrations/` sozinho reconstrói o banco.

**Quatro defeitos encontrados na execução, todos medidos antes de escritos como causa** (`CLAUDE.md` §11), com entrada própria em `handoffs/instrucoes.md` §5: (a) `public.fks_sem_isolamento_de_conta()` — a guarda que a suíte cobra em "zero linhas" — tem a lista de schemas **cravada no corpo**, e o schema novo nasceria invisível para ela; (b) a policy de `DELETE` nasceu **morta**, porque `aba_health.pode_acessar` não conhece o verbo `exclusao` e nunca vai conhecer; (c) `consentir_opcao` gravava recusa com autor nulo quando chamada sem sessão, e a CHECK a barrava com uma mensagem que não explicava nada; (d) mudar `access.modules` em produção acendeu, no site publicado, um item de menu "Planos" apontando para rota inexistente — o `?? /${module_key}` de `nav.ts` é silencioso. Mais duas lições menores, registradas no cabeçalho da própria migration: FK composta exige a chave única **antes** do `CREATE TABLE`, e `DROP CONSTRAINT` + `ADD` deixa de ser idempotente no instante em que alguém passa a depender da restrição.

**[SUPERADO em 2026-09-04 — ver o fechamento abaixo. O registro não se apaga.]** **Reportado, não implementado** (`CLAUDE.md` §15): **a leitura de um plano não gera linha em `aba_health.log_acesso`.** O regime de leitura de `aba_health` é registrado porque toda leitura passa por `ler_evolucoes()`; `aba_treatment` lê por RLS direta, e Postgres não tem gatilho de `SELECT`. Registrar exigiria um caminho de leitura por RPC, que o plano desta subetapa não pede e a Evidência não cobra. Fica nas Pendências vigiadas, para decisão de Max junto com a 03.8.a.

**🔁 As duas divergências foram fechadas no mesmo dia, por ordem de Max (2026-09-04) — migration `047_planos_leitura_registrada.sql`.**

**(1) O rótulo é "Plano", no singular** — *"pois optamos por padronizar dessa forma em todo o sistema"*. A `045` gravara "Planos"; a `047` corrige, e o `CLAUDE.md` §2 registra as duas revisões sem apagar nenhuma. O singular é a forma dos cinco termos do vocabulário (`docs/02` §13.1).

**(2) A leitura de plano passou a deixar rastro, como a de prontuário.** Max recusou a pendência que a 03.8 tinha reportado: *"tudo o que se referir ao schema `aba_health` deve ser resolvido de forma segura e sem deixar para depois"*. Está certo — o regime de `aba_health` tem **duas metades**, e a 03.8 tinha entregue só a primeira: a política diz QUEM pode ler, e a função de leitura diz QUE ALGUÉM LEU. Política autoriza e não registra (`instrucoes.md` §6). O porte agora é literal, em três peças:
  - **Revogação de `SELECT` por coluna** em `planos`, `diagnosticos` e `procedimentos_plano`. Sai o que é dado de saúde — `dente`, `faces` e todo texto livre (`titulo`, `observacao`, `descricao`); fica o metadado que a aplicação precisa para listar, juntar e atualizar. **Lista de permissão, não de negação:** coluna nova nasce ilegível, e a falha certa é a tela pedir permissão, nunca a coluna vazar por esquecimento.
  - **`aba_treatment.ler_planos(cliente_id)`** — `SECURITY DEFINER`, porta única do conteúdo clínico, que **loga antes de devolver**, reafirma a fronteira de conta no filtro (`SECURITY DEFINER` não passa por RLS) e devolve **uma linha por plano** com a matriz em `jsonb` — opções, diagnósticos (com a fila de trabalho já calculada) e células. Uma leitura, uma linha de log; a matriz achatada geraria N linhas para a mesma abertura de tela. Autorização negada devolve **conjunto vazio e não loga** — nada foi lido, então não há o que registrar.
  - **Gatilho de escrita** nas quatro tabelas de dado de paciente, porque a migration `070` do Maximus existe exatamente por o log clínico dele cobrir a leitura e não cobrir a escrita. Portar metade seria reintroduzir num schema novo o defeito que ela corrigiu num antigo.

`aba_health.log_acesso.tipo_registro` ganhou o valor **`plano`** (por adição; os dois consumidores exibem o valor como texto e foram conferidos, não supostos). **P-sub: 31/31** (eram 25 — seis casos novos), **72/72** nas vizinhas, **evidência 23/23**. `aba_treatment.fases` fica fora do log pelo mesmo motivo que `formularios_anamnese` ficou em `aba_health`: é catálogo da conta, não dado de paciente, e `log_acesso.cliente_id` é `NOT NULL`.

**A migration foi recusada pela própria verificação na primeira execução, e o achado é o mais caro desta rodada:** a primeira versão fazia `REVOKE SELECT (coluna)` e **as oito colunas continuaram legíveis** — privilégio de TABELA cobre todas as colunas, e revogar por coluna com o `GRANT` de tabela ainda de pé não dá erro, não protege nada e não aparece em lugar nenhum. A lição já estava escrita no cabeçalho da `013`, herdada da `053` do Maximus, e foi relida **depois** de o defeito ser reproduzido. Entrada própria em `handoffs/instrucoes.md` §5.

**O que a versão anterior desta subetapa registrou, e não se apaga** (`CLAUDE.md` §8 e §10):

**⏸️ Aberta e pausada em 2026-09-03, antes da primeira linha de código** (ver o bloco de pausa no topo desta Etapa). Nada foi implementado e nada foi commitado; a subetapa seguia **não iniciada** e por isso sem marcador de Status no título. O que ficou apurado, para a sessão que a retomasse não pagar de novo o reconhecimento:

- **Decisão de Max (2026-09-03):** o schema novo é **`aba_budget`**, chave de módulo **`budget`**, label "Orçamentos" — o décimo schema de módulo, a ser acrescentado à lista do `CLAUDE.md` §2 e a `access.modules`. Sujeita a revisão se a pesquisa `analise-ice` mudar o desenho da entidade.
  - **[Revista em 2026-09-03 pela decisão D-I2 — a decisão acima não se apaga.]** A ressalva se realizou: a pesquisa mostrou que a entidade real é o **plano de tratamento** e que o orçamento é uma **projeção financeira** dela, não o objeto. Max decidiu **`aba_treatment`**, chave `treatment`, label **"Planos de tratamento"**. Motivo registrado: chamar de orçamento a coisa que o dentista usa para decidir o tratamento faz o produto parecer um sistema de vendas com odontograma acoplado. **"Orçamento" continua sendo a palavra da interface** — o schema não aparece na tela.
- **Decisão preliminar de Max sobre o PDF:** vista de impressão + `window.print()` (sem dependência nova, sem custo de bundle nem de precache), a reconfirmar depois das referências do ICE.
  - **[Reconfirmada pela pesquisa em 2026-09-03.]** O ICE também não usa biblioteca de PDF no fluxo de consentimento: gera um **documento a partir de um modelo**, e o modelo decide o que aparece. `window.print()` continua sendo o motor certo; **o que muda é o que ele imprime** — ver a 03.8.a.
- **As quatro dependências foram conferidas no repositório e todas sobreviveram:** `Marcacao.faces` (`crm/src/features/health/mapas.ts:246`), `projetarMarcacoes()` (`crm/src/features/health/odontograma.ts:196`), as colunas da migration `042` com os tetos 32/`dente`, 6/`sextante`, 2/`arcada`, e as seis operações de `aba_finance` em `011_aba_finance_operations.sql` (linhas 49/153/214/298/329/362).
  - **[Uma delas mudou de sentido em 2026-09-03, e a conferência não pegava isso.]** `projetarMarcacoes()` e `facesDoDente()` continuam existindo e funcionando — mas `facesDoDente()` (`odontograma.ts:96`) devolve a face do **achado**, e o plano precisa da face do **trabalho** (achado A2). A dependência era verdadeira e o consumo dela seria errado: é o tipo de defeito que não dá erro, e o corrige a 03.7.a.
- **Achado de premissa, o sexto da Etapa 03** — registrado em `handoffs/instrucoes.md` §5 e em `docs/02_MODELO_DE_DADOS.md` §11.1: o `plano` de `plano · procedimento · dente · faces · valor` **não é** `aba_catalog.planos`.
  - **[Confirmado pela fonte em 2026-09-03, e ampliado.]** A palavra certa era mesmo **convênio**. Mas a leitura registrada — `variantes_servico` como tabela de preço **na linha** — é só metade: no ICE **a linha não guarda preço**, guarda código, dente, faces e status, e o preço é **resolvido por escada** no momento do lançamento. Correção completa na 03.8.a e em `docs/02_MODELO_DE_DADOS.md` §11.2.

### Subetapa 03.8.a — Orçamento: o preço que se resolve, e a tabela que tem vigência [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 1 na sua **metade financeira**, mais o item **41** (entrou no MVP por D-I5). Partida da antiga 03.8.a por **D-V6** — o contrato saiu daqui e virou a 03.8.b. Duas peças:
  1. **Tabela de preço como entidade com vigência** — rascunho editável, **compromisso com data**, tarifa comprometida **imutável**. Reajuste é **tabela nova**, nunca `UPDATE`: senão o passado do financeiro se reescreve, e isso não tem conserto retroativo. É a razão de o item 41 ter entrado agora e não depois.
  2. **O preço se resolve, não se escolhe** — escada `Paciente > Tipo de profissional > Clínica > Grupo de clínicas > Prática`, percorrida no momento do lançamento, **sem ninguém escolher tabela na tela**. Prova ao vivo na fonte: a mesma consulta custa **$250** com um profissional comum e **$400** com um especialista (`design/benchmark/fontes/ice.md` §5.2).
  E o **orçamento** como o que a palavra passa a significar por decisão de Max de 2026-09-04: a **vista financeira do plano clínico** — o preço a ser pago pela execução do conjunto de procedimentos —, coisa distinta do **plano**, que é o planejamento em si.
Conclusão: a mesma consulta custa valores diferentes com profissionais diferentes sem ninguém escolher tabela; trocar o profissional **recalcula e avisa a diferença antes de confirmar**; e reajustar preço **não muda nenhum valor já acordado**.
Qualidade:
  - **A linha guarda o valor resolvido, com a tabela que o resolveu como proveniência** — não `variante_servico_id` como escolha. Isso **corrige a nota da 03.8 em `docs/02_MODELO_DE_DADOS.md` §11.2**: guardar a tabela na linha obriga alguém a escolhê-la na tela, que é exatamente o que a escada existe para evitar. Congelar o valor no momento do acordo **e dizer de onde ele veio** é diferente, e é o correto.
  - **A escada não fica órfã com o convênio adiado (D-V5).** Ela foi justificada em parte por convênio, mas a prova mais forte dela na fonte é **tipo de profissional** — $250 × $400 —, que nada tem a ver com operadora. O degrau `Paciente` continua útil como preço pessoal (cortesia, acordo pontual), e é ele que receberá o convênio quando o convênio existir.
  - **Só `admin` mexe no dinheiro** (decisão de Max, 2026-09-04): parcelas, descontos, juros, mora e promoções são da recepção, **nunca do profissional**. Não é detalhe de tela — é padrão de qualidade do trabalho clínico, porque tira do dentista a negociação de preço na cadeira. A trava é de banco, não de interface.
  - **A régua de `aba_finance` continua valendo, e é a mesma da 02.8:** tudo passa pelas seis operações já provadas (`db/migrations/011_aba_finance_operations.sql`, linhas 49/153/214/298/329/362 — com os nomes já renomeados pela 03.6.b), **nunca por `INSERT` direto** nas tabelas mantidas por gatilho.
  - **P-sub:** o orçamento projeta dado clínico (dente, face, procedimento) numa vista que a recepção lê. O que a recepção pode ver do plano é fronteira de `aba_health`, não de conveniência.
Evidência: escada provada com dois profissionais e a mesma consulta devolvendo valores diferentes **sem escolha na tela**; recálculo ao trocar o profissional com o aviso da diferença **antes** de confirmar; tabela comprometida recusando `UPDATE` de tarifa; reajuste por tabela nova **não alterando** nenhum valor já acordado; profissional recusado ao tentar alterar desconto/parcela; casos de ataque novos e permanentes; `get_advisors` sem achado novo.
Esforço máximo do /goal: 4 tentativas
Escalonamento de LLM: Opus do início ao fim.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
Status: ✅ CONCLUÍDA — executada em 2026-09-05 (Opus do início ao fim, `[Goal]`, uma tentativa das 4). **Migration nova:** `048_orcamento_preco_resolvido.sql` (arquivo único, 1.891 linhas). **Escopo decidido por Max na abertura da sessão, à pergunta obrigatória: BANCO + TELA** — a Evidência do bloco fala em "sem escolha na tela" e em "avisar a diferença ANTES de confirmar", e as duas frases são de interface. É esta subetapa que dá porta ao módulo `treatment`, que a 03.8 deixou de propósito na matriz de permissões e fora do menu.

**A escada, e a razão de a assinatura ser o contrato.** `aba_finance.resolver_preco(procedimento, cliente, profissional, data)` percorre `Paciente > Tipo de profissional > Clínica > Grupo > Prática` até achar uma tabela **comprometida e vigente** com tarifa para o código, e cai em `aba_catalog.procedimentos.preco_base` (degrau `catalogo`) quando nenhuma alcança — sem esse fundo, toda conta nasceria com orçamento vazio no dia da implantação. **Ela não tem parâmetro de tabela de preço**, e é essa ausência que cumpre "sem escolha na tela": não existe caminho por onde a interface passe uma tabela, então não existe tela que possa pedir para escolher. A verificação (g) da migration lê a assinatura no catálogo e **recusa a migration** se alguém acrescentar esse parâmetro um dia. Desempate dentro do mesmo degrau é determinístico (`vigente_de` mais recente, depois `comprometida_em`, depois `id`) — sem ordem total, o preço mudaria entre duas execuções da mesma consulta, que é a pior classe de defeito num número que vira contrato.

**O degrau que a fonte prova exigiu um catálogo novo.** A prova mais forte da escada não é convênio (adiado por D-V5) — é tipo de profissional: R$ 250 com um clínico geral e R$ 400 com um especialista (`fontes/ice.md` §5.2). `aba_scheduling.profissionais.especialidade` **não servia**: é TEXT livre, sem tela e sem catálogo, e nenhuma chave estrangeira se pendura num texto que cada pessoa digita como quer. Nasceu `aba_scheduling.tipos_profissional` (catálogo por conta, dois tipos semeados, escrita por `admin` porque quem cria o tipo decide quanto custa a consulta de quem for marcado com ele) e `profissionais.tipo_profissional_id`, chave composta. `especialidade` fica intacta: "Endodontia" é especialidade, "Especialista" é tipo, e é o tipo que move o preço.

**Reajuste é tabela nova, e a imutabilidade não tem atalho.** `tabelas_preco` tem três estados — `rascunho` (editável) → `comprometida` (com data, autor e **tarifa imutável**) → `encerrada` (continua existindo, porque é a proveniência de todo valor congelado). Gatilhos recusam alterar **e apagar** tarifa comprometida, mudar o degrau ou a vigência de uma tabela comprometida, e apagar a tabela. `reajustar_tabela_preco()` cria tabela nova em rascunho com as tarifas copiadas e `substitui_id` apontando para a origem; `comprometer_tabela_preco()` da nova encerra a anterior. **A prova de que a trava não tem caminho pela aplicação é a limpeza da própria suíte: ela precisa de `DISABLE TRIGGER` por conexão de dono — nem `service_role` apaga uma tarifa comprometida.**

**A linha guarda o valor resolvido COM a proveniência.** `itens_orcamento` congela `valor_resolvido`, o `degrau` que o resolveu e a `tabela_preco_id` de origem — não como escolha de tela (que é o que a nota da 03.8 em `docs/02` §11.2 propunha e o ICE derrubou), mas como registro de onde o número veio. Um ano depois, "por que este procedimento saiu por R$ 250?" tem resposta no próprio banco.

**O aviso antes de confirmar é uma função `STABLE`.** `simular_troca_de_profissional()` devolve, item a item, valor atual, valor novo, os dois degraus e a diferença, **sem gravar nada**; `trocar_profissional_do_orcamento()` aplica e devolve a mesma diferença. As duas resolvem pela **mesma** `resolver_preco()` com os mesmos argumentos — duas contas separadas, uma para mostrar e outra para gravar, divergiriam no primeiro dia em que alguém mexesse numa delas.

**Só `admin` mexe em dinheiro, e a trava alcança o que já existia.** Medido antes de escrito: as policies de `contratos`, `parcelas_contrato` e `pagamentos` autorizam `UPDATE` a partir de `agent` — o papel do profissional. Trocar o papel mínimo da policy fecharia a tabela inteira, e a pergunta aqui não é "quais linhas", é "quais colunas", que a RLS não responde (`instrucoes.md` §5). A trava é gatilho comparando OLD × NEW, com a lista de colunas em `TG_ARGV`, e vale em **`orcamentos`, `contratos` e `parcelas_contrato`** — a regra de Max é sobre o produto, não sobre o código escrito hoje. Conferido que a tela do Financeiro não regride: `useVenderPacote` cria contrato com os valores padrão, que não disparam a trava, e as suítes `03`/`04` seguem verdes.

**P-sub — a decisão mais importante da subetapa, e ela é de modelagem.** O orçamento projeta dado clínico para a recepção ler, e a saída cômoda seria guardar `dente` e `faces` em `itens_orcamento`. **Não se guarda.** A migration `047` acabara de revogar exatamente essas colunas em `aba_treatment`; copiá-las para `aba_finance` daria a qualquer papel com `finance.read` leitura de dado de saúde **sem alcance clínico e sem rastro**, uma migration depois — a fronteira não seria rompida, seria contornada por um caminho novo aberto por quem a conhecia. O item aponta para `procedimento_plano_id`, e o detalhe sai por **`aba_finance.ler_orcamentos()`**, que decide pelo alcance do chamador: com alcance, devolve dente e face e **registra em `aba_health.log_acesso`** (uma linha por plano aberto); sem alcance, devolve o mesmo orçamento com os mesmos valores, `dente` e `faces` nulos e **nenhum log** — nada clínico foi lido, e log a mais seria log mentindo. O retorno traz `com_detalhe_clinico`, e **a tela diz** que está incompleta em vez de mostrar coluna vazia. Coluna que não existe é proteção mais forte que coluna revogada, e a verificação (h) recusa a migration se alguma delas aparecer.

**A régua de `aba_finance` cobrada por catálogo, não por promessa.** Esta subetapa não escreve uma linha em `faturas`, `itens_fatura`, `pagamentos`, `pacotes_cliente`, `saldos_pacote` ou `extrato_pacote` — a cobrança que nasce da aprovação é da **03.8.b**, junto do contrato. A verificação (k) lê o **corpo** de cada função nova em `pg_get_functiondef` e recusa a migration se alguma passar a escrever nelas: um comentário dizendo "não escrevemos em faturas" não impede ninguém de escrever.

**Doze verificações que recusam a migration**, entre elas a assinatura de `resolver_preco` (g), a ausência de coluna clínica no orçamento (h), a régua de `aba_finance` (k) e — a lição da 03.8 — a conferência de que `public.fks_sem_isolamento_de_conta()` **alcança** os três schemas tocados, sem a qual o "zero linhas" da auditoria seria cumprido por não olhar.

**A tela.** `treatment` saiu de `MODULOS_SEM_TELA` (a lista fica, vazia, porque o mecanismo continua necessário), ganhou `/plano` e `/plano/:clienteId`, ícone próprio e página preguiçosa (**4,66 kB gzip**). A matriz mostra fase na linha, opção na coluna, a fila de diagnósticos não fasados embaixo e o orçamento da opção ao lado, com o degrau de cada valor. Trocar o profissional abre o diálogo da diferença **antes** de aplicar. As condições comerciais aparecem desabilitadas para quem não é `admin`, com o motivo escrito — controle cinza sem motivo declarado é indistinguível de defeito. A administração das tabelas de preço é a 11ª seção de **Configurações**.

**P-sub: 19/19** em `crm/tests/rls/20_orcamento.spec.ts` (fronteira clínica, alçada financeira, imutabilidade e escada), **248/248 na suíte inteira** (1 pulado: o ataque A06 do webhook, congelado por decisão de Max desde 2026-08-21) e **evidência 33/33** em `crm/scripts/evidencia_orcamento.mjs`, que percorre a corrente completa do preço com sessão real simulada por `request.jwt.claims` — o caminho da aplicação, sem gastar o endpoint de token. `npm run build` verde, precache em 1.190 KiB (teto 1.400). **`get_advisors` sem achado novo:** os mesmos 13 lints anteriores, nenhum citando as tabelas ou funções desta subetapa.

**Aplicada primeiro no banco de TESTES e reaplicada lá antes de ir para produção** — idempotência provada, não presumida. Em produção entrou pelo MCP em quatro partes (`048_orcamento_1_tipos_e_tabelas_de_preco`, `_2_operacoes_e_escada`, `_3_entidade_e_alcada_financeira`, `_4_operacoes_rls_e_verificacoes`), com as verificações da §11 passando lá também; o arquivo no repositório é **um só**.

**Quatro defeitos encontrados na execução, os quatro medidos antes de escritos como causa** (`CLAUDE.md` §11), com entrada própria em `handoffs/instrucoes.md` §5: (a) `NUMERIC(12,2)` serializa zero como `0.00`, e a trava de alçada comparava número como **texto** — todo orçamento novo parecia nascer com desconto, e `montar_orcamento` foi recusado para o `agent` na primeira execução da evidência; (b) encerrar uma vigência "na véspera" quebra quando a vigência começou **hoje**, cenário banal (comprometer, ver o erro, reajustar no mesmo dia) que o `GREATEST` com piso no próprio início resolve — e que fez nascer a operação `encerrar_tabela_preco`, que faltava; (c) o `UPDATE ... FROM resolver_preco(i.coluna, ...)` não compila, porque a cláusula `FROM` de um `UPDATE` não enxerga a tabela-alvo; (d) **aplicar migration por MCP transcrevendo poda os comentários**, e produção ficou com a mesma lógica e outro texto — ver o parágrafo abaixo.

**A divergência produção × repositório foi medida e fechada, e o método é o registro que fica.** `md5(pg_get_functiondef())` divergia nas 17 funções novas. O teste que separou "lógica diferente" de "texto diferente" custou uma consulta: comparar os corpos **sem os comentários** nos dois lados devolveu o mesmo hash (`e8ac5bbc52f34aaad1a78e2299c5b9db`), provando que nenhuma lógica diferia. Os corpos foram reemitidos com o texto exato do arquivo (`048_orcamento_5a`/`5b`), e a comparação final ficou **idêntica** — restava apenas `CRLF`, artefato do checkout Windows presente no banco de TESTES e ausente em produção, que é quem carrega a forma canônica que o git versiona: `replace(def, chr(13), '')` no teste devolve exatamente o hash de produção, `03ea6c2206ef3138fb2dc7a6b4ddcf03`.

**Duas decisões de escopo declaradas, para não virarem surpresa na 03.8.b:** (1) **aprovar o orçamento não gera fatura** — congela o acordo com autor e data, e a cobrança que nasce da aprovação (a inversão brasileira) é do **contrato**, na 03.8.b; (2) os degraus `Clínica` e `Grupo de clínicas` nascem **sem discriminador**, porque multiunidade é a **03.9** e não existe hoje tabela de unidade para uma chave apontar — inventá-la aqui seria a mesma informação com dois donos, defeito que a 03.6.a já pagou. Os dois degraus já distinguem pela precedência, e a 03.9 acrescenta a coluna por adição, sem reescrever a escada.

**Reportado, não implementado** (`CLAUDE.md` §15): a verificação visual da tela em navegador não foi feita nesta sessão — ela exige credencial de sessão, e digitá-la aqui a exporia no registro da conversa. Build, typecheck e a suíte cobrem o contrato de dados; o que falta é a conferência de que a peça aparece na tela, que é exatamente a lição da 03.7.a ("a suíte ficou verde com a peça central invisível"). Fica como o primeiro passo depois da publicação, com a sessão que Max já tem no navegador.

### Subetapa 03.8.b — Contrato: itens heterogêneos, assinatura e a trava dupla [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: decisões **D-V3**, **D-V4**, **D-V8**, **D-V9** e **D-V10** de Max (2026-09-04). O contrato deixa de ser uma vaga única e passa a ser **a estrutura documental dentro da qual se registra tudo o que foi pactuado**. Três peças:
  1. **`aba_finance.itens_contrato` com arco exclusivo** — `procedimento_id`, `pacote_id` e `plano_id`, cada uma chave estrangeira **composta por `account_id`**, com `CHECK (num_nonnulls(...) = 1)` garantindo que exatamente uma está preenchida. N itens de cada tipo por contrato. **Nunca referência polimórfica** — o motivo medido está na nota da tabela D-V. Mais o **gatilho que impede o plano de um paciente de entrar no contrato de outro**: `plano.cliente_id = contrato.cliente_id`, porque a chave composta protege entre clínicas e não entre pacientes da mesma clínica.
  2. **A view `aba_catalog.ofertas`** — o "cardápio": `UNION` de procedimentos, pacotes e planos com uma coluna `tipo`, e o plano aparecendo **só para o paciente dono dele**. É o que a tela consulta para montar o contrato. View, e não tabela, porque cardápio não guarda dado: guardar duplicaria preço e envelheceria.
  3. **A trava dupla de finalização** — o contrato não se finaliza enquanto não forem verdadeiras **ao mesmo tempo**: (a) o paciente pagou **tudo** e (b) o profissional executou em **todas as faces de todos os dentes planejados**.
  E a **inversão brasileira**, instrução de Max de 2026-09-03, que continua valendo: a cobrança se solta na **aprovação**, antes de qualquer execução. No ICE a cobrança nasce da execução; quem esperar o dentista marcar a face trabalhada para faturar **não fatura nunca**.
Conclusão: um contrato carrega, junto, um plano personalizado, um pacote padrão e um procedimento avulso; a cadeia `odontograma → plano → contrato → fatura` fecha por query; o aceite da proposta gera o **documento canônico com hash** e o contrato só sai de `rascunho` com as **duas assinaturas presenciais registradas**; nenhum tratamento se executa sem contrato assinado; e o contrato **continua aberto** enquanto faltar dinheiro **ou** faltar trabalho.
Qualidade:
  - **Aditivo não existe (D-V4).** Acréscimo de serviço ou de tempo é contrato novo, ligado ao anterior pelo `cliente_id` compartilhado. Não inventar tabela de aditivo, não inventar versionamento de contrato — é versionamento futuro declarado.
  - **O documento é canônico, não é um PDF (D-V10).** No aceite nasce um HTML **determinístico** — mesma entrada, mesmo byte — com **hash de conteúdo** guardado junto. É o hash que dá valor probatório; o PDF é só um formato de entrega, renderizado no envio ou no arquivamento. **Nenhuma dependência de PDF volta ao projeto** — a 03.7 removeu a cadeia `jspdf`/`dompurify` e ela não retorna aqui. **[Correção de fato, 2026-09-04 — medida, não deduzida:** a cadeia `jspdf`/`dompurify`/`html2canvas` **ainda está no projeto hoje**, entrando de forma transitiva por `react-advanced-odontogram` (`npm ls jspdf` mostra a árvore), e pesa **754 KB** no chunk preguiçoso do odontograma. A 03.7 **não** a removeu — ela escondeu por governança a barra de exportação do aplicativo de demonstração, que é outra coisa. Quem a remove é a **03.7.a**, e o próprio plano dela diz isso no futuro: *"`npm audit` conferido depois da **saída** da cadeia"*. O CODE leu esse futuro como passado. **A decisão D-V10 não muda** — ela se sustenta pelo hash sobre conteúdo canônico, não por qual dependência está instalada. **[Cumprido em 2026-09-04 pela Subetapa 03.7.a: `npm ls jspdf dompurify html2canvas` devolve vazio, e 22 pacotes saíram do lock com 0 entrando.]****]**
  - **A assinatura desta subetapa é presencial e registrada (D-V9):** as duas partes assinam, o contrato passa a `assinado`, e o estado é o mesmo que a **03.12** vai alimentar pelo canal por link. A 03.8.b não constrói canal externo nenhum — quem constrói é a 03.10 (token) e a 03.12 (link multicanal).
  - **A exigência de contrato assinado é padrão e desligável só pelo `owner` (D-V8)**, por processo, com o risco assumido e **registrado** — chave desligada sem registro de quem desligou e quando é pior que chave nenhuma. A invariante é de banco: sem contrato em estado `assinado`, o serviço não se executa.
  - **A armadilha, nomeada antes de acontecer: não declarar o contrato concluído no último pagamento.** É o simétrico exato do que a 02.10 já pagou (`handoffs/instrucoes.md` §5) — o agendador zerou o KPI "Vencido" porque a fatura saía do contador exatamente quando virava pendência. Aqui o contrato sairia da lista no momento em que o paciente termina de pagar **e ainda tem trabalho a receber**, que é o pior momento possível. **Os dois testes que provam isso são obrigatórios e permanentes:** pagar tudo com uma face por executar → contrato aberto; executar tudo com saldo devedor → contrato aberto. **Nenhuma das duas fontes responde sozinha** — o saldo mora em `aba_finance`, a cobertura de execução mora em `aba_health`; é invariante de dois schemas, e é por isso que esta subetapa tem P-sub.
  - **A cobertura de execução é por FACE, não por procedimento.** É o que torna o **item 37 (step set)**, fora do MVP por D-I5, aditivo depois em vez de reescrita.
  - **Autoria e sucessão (D-V7):** o plano só é editado pelo profissional que o criou (RBAC `agent` + profissional, mais IBAC pelo autor), com **coautor indicado pelo próprio autor**. Se o autor for marcado **inativo** pelo `owner` sem ter indicado ninguém, planos e contratos dele passam **automaticamente ao `owner`**. Atenção medida: `access.can()` devolve `TRUE` para `owner` **antes** de consultar qualquer tabela (`003_core_access.sql:162`) — a trava por autor precisa do mesmo mecanismo que a 03.9 vai construir para a trava de nível, e **não** de uma segunda invenção.
  - **A régua de `aba_finance` continua valendo:** nunca `INSERT` direto nas tabelas mantidas por gatilho.
Evidência: um contrato com os três tipos de item ao mesmo tempo, montado por query; **o mesmo contrato gerando duas vezes o mesmo hash** (determinismo do documento canônico) e hash diferente ao mudar um item; recusa de executar serviço com contrato em `rascunho`; recusa do `CHECK` quando zero ou duas colunas do arco vêm preenchidas; recusa do plano de outro paciente e do pacote de outra conta (`42501`/`23503`, SQLSTATE lido); `fks_sem_isolamento_de_conta()` com zero linhas depois das três chaves novas; **os dois testes da trava dupla, cada metade sozinha não fechando o contrato**; recusa de executar serviço sem contrato assinado, e o caminho do `owner` desligando a exigência **com registro**; casos de ataque novos e permanentes; privilégio de coluna medido; `get_advisors` sem achado novo.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**

> **Duas tensões declaradas aqui antes de virarem surpresa — e RESOLVIDAS por Max em 2026-09-04 (D-V9 e D-V10).** O texto do que estava em aberto fica inteiro (`CLAUDE.md` §10); o que mudou é que agora há resposta, e ela está ao pé de cada uma.
>
> **(a) O fluxo de oito passos da D-V8 atravessa três subetapas, e duas vêm depois desta.** Os passos 3 a 6 — escolher a via (WhatsApp/SMS/e-mail/QR), enviar **token**, o paciente assinar e devolver — são exatamente a **Subetapa 03.10** (infraestrutura de token externo) e a **Subetapa 03.12** (assinatura do paciente por link multicanal), que na ordem atual vêm **depois** da 03.8.b. Saídas possíveis, para Max decidir: (i) a 03.8.b entrega a estrutura, o estado `assinado` e a invariante, com a assinatura **presencial registrada**, e o canal por link chega na 03.12; (ii) mover 03.10 e 03.12 para antes da 03.8.b; ou (iii) a invariante nasce desligada e é ligada quando a 03.12 fechar. A recomendação do CODE é **(i)**, porque entrega a proteção jurídica desde já e não reordena a Onda 4 inteira.
>
> **→ Resolvida (D-V9): Max acatou a (i).** A 03.8.b entrega estrutura, estado `assinado` e invariante com **assinatura presencial registrada** das duas partes; o canal por link é da 03.12, que passa a alimentar o mesmo estado sem redesenhá-lo.
>
> **(b) "Gerar o PDF automaticamente" não cabe em `window.print()`.** A decisão vigente para o documento é **vista de impressão + `window.print()`** (Max, 2026-09-03, reconfirmada pela pesquisa), e a Subetapa 03.7 **removeu** a cadeia `jspdf`/`dompurify` do projeto. **[Correção de fato, 2026-09-04 — medida, não deduzida:** a cadeia `jspdf`/`dompurify`/`html2canvas` **ainda está no projeto hoje**, entrando de forma transitiva por `react-advanced-odontogram` (`npm ls jspdf` mostra a árvore), e pesa **754 KB** no chunk preguiçoso do odontograma. A 03.7 **não** a removeu — ela escondeu por governança a barra de exportação do aplicativo de demonstração, que é outra coisa. Quem a remove é a **03.7.a**, e o próprio plano dela diz isso no futuro: *"`npm audit` conferido depois da **saída** da cadeia"*. O CODE leu esse futuro como passado. **A decisão D-V10 não muda** — ela se sustenta pelo hash sobre conteúdo canônico, não por qual dependência está instalada. **[Cumprido em 2026-09-04 pela Subetapa 03.7.a: `npm ls jspdf dompurify html2canvas` devolve vazio, e 22 pacotes saíram do lock com 0 entrando.]****]** `window.print()` exige uma pessoa diante da tela; um gatilho automático no momento do "proposta aceita" precisaria de renderização **no servidor** — Edge Function com motor de PDF —, que é dependência nova e custo novo. Proposta do CODE: gerar no aceite o **documento canônico** (HTML determinístico + hash de conteúdo, que é o que dá valor probatório), e renderizar PDF só no momento de enviar ou de arquivar.
>
> **→ Resolvida (D-V10): Max acatou.** O documento canônico nasce no aceite, com hash; o PDF se renderiza só no envio ou no arquivamento. **Nenhuma dependência de PDF volta ao projeto.**

### ONDA 3 — o núcleo de permissão

### Subetapa 03.9 — Multiunidade + trava de plano por módulo [Manual] [LLM: Opus] · PORTÃO COMPLETO
Objetivo: item 24 e a decisão D3 na **mesma cirurgia**, porque a medição da 03.0 mostrou que as duas reescrevem `access.can()`. Multiunidade: remover `public.profiles.user_id UNIQUE`, passar a conta ativa por toda a camada de autorização, e login em dois estágios (e-mail e senha → o sistema detecta que o e-mail pertence a mais de um consultório → seleção de consultório). Trava de plano: camada nova que diz quais módulos a conta contratou, consultada **antes** do atalho de `owner`.
Conclusão: um usuário pertence a duas contas e, na conta ativa, não enxerga **nada** da outra; um módulo fora do plano contratado fica invisível **inclusive para o `owner`**; os 7 passos do portão executados em bench isolado, com parecer explícito.
Qualidade: **portar a lógica, traduzir os nomes — nunca reescrever a permissão do zero** (`CLAUDE.md` §14); 21 arquivos de migration tocam `profiles` ou `is_account_member`. As políticas de RLS escritas em 03.6–03.8 são **reauditadas uma a uma** para garantir que comparam com a conta **ativa**, e não com "qualquer conta do usuário" — que é vazamento entre clínicas. **A trava de plano não pode viver em `access.module_permissions`:** `access.can()` devolve `TRUE` para `owner` antes de consultar aquela tabela (`003_core_access.sql:162`), e o `owner` é justamente quem contrata o plano — o interruptor aceitaria o clique e não esconderia nada, repetindo em escala o defeito que a 02.12 encontrou no grid de módulos. `access.modules` é catálogo global sem `account_id` e `licensing` guarda só teto de assentos: nenhum dos dois responde "o que esta conta contratou". Guarda permanente no molde do achado F01-b da 02.15: função de auditoria por catálogo mais teste de suíte que falha se ela devolver qualquer linha — sem isso a correção vale só para hoje. **Qual item do MVP entra em qual nível (Bronze/Prata/Ouro/Diamante) é decisão comercial de Max**, preenchida em tabela depois, sem tocar em código — decisão dele de 2026-09-03: a matriz fica para o momento certo, e não atrasa esta subetapa. Consequência de implementação, portanto: **o mecanismo nasce com todos os níveis liberando todos os módulos**, e o corte comercial passa a existir quando a matriz for preenchida. O que precisa estar provado aqui não é o corte, é que ele **funciona quando existir** — inclusive contra o `owner`, que é o caso que a implementação ingênua erra.
Evidência: relatório do bench + parecer explícito. Vetor obrigatório: usuário com duas contas lendo dado da conta inativa — **o vazamento mais provável do MVP inteiro**.
Esforço máximo: sem teto de tentativas — é auditoria, não `/goal` de implementação.
Escalonamento de LLM: Opus do início ao fim — é a subetapa de maior risco da Etapa 03.
**O CODE entrega o parecer e para. Ordenar o merge é atribuição exclusiva de Max** (`CLAUDE.md` §13).
CHANGELOG: **+1.0**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — reforço, não redesenho.** A escada de preço do ICE usa **Clínica** e **Grupo de clínicas** como dois níveis distintos, o que confirma D1/D3 e dá à multiunidade um consumidor concreto além do isolamento: a 03.8.a resolve preço por esses níveis, e sem eles a escada fica truncada. Fonte: `design/benchmark/fontes/ice.md` §5.2 e `RELATORIO_DE_IMPACTO_ICE.md` §3.2.

### ONDA 4 — token e comunicação externa (bloco B): uma infraestrutura só

Os itens 7, 18, 19 e 23 parecem quatro recursos e são **um só mecanismo**. Construir uma vez, reusar quatro vezes. E há uma regra de negócio que Max fixou e que atravessa o bloco inteiro: **nunca anexar dado pessoal em e-mail** — o canal (e-mail, WhatsApp ou SMS) carrega **só o link**; o dado trafega pelo ambiente privado.

### Subetapa 03.10 — Infraestrutura de token externo [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: portar `sql/20_comunicacao_externa.sql` e `sql/21_remessas_recepcao.sql` do **CRM Sindcom**, onde o padrão está pronto e depurado — a concessão (`token`, `token_expira_em`, `token_revogado_em`, mais os carimbos de envio e de primeira/última remessa), a tabela de tentativas com motivo enumerado, o bucket privado e a Edge Function pública. Mesmo tratamento que `CLAUDE.md` §14 dá ao Maximus: portar a lógica, traduzir os nomes.
Conclusão: token válido serve; inexistente, expirado, revogado, já consumido e arquivo inválido são recusados, cada um com o motivo registrado na tabela de tentativas.
Qualidade — **três lições medidas que vêm junto com o porte:**
  1. **O freio conta por token, nunca pela entidade.** Travar o *laboratório* permitiria a um atacante bloquear o envio de exames de uma clínica inteira só errando token de propósito. Motivo enumerado: `token_inexistente` / `expirado` / `revogado` / `arquivo_invalido`. É DoS por desenho, evitado de origem.
  2. **Bucket com `public = false`, mais `file_size_limit` e `allowed_mime_types` no próprio Storage** — segunda camada, independente da validação da Edge Function. Se a checagem da função quebrar num refactor futuro, o Storage ainda recusa.
  3. **Policy ausente em `storage.objects` não nega: faz sumir.** Com RLS ligada e zero policies, o `authenticated` inteiro fica de fora e o erro é `"Object not found"` — que parece arquivo inexistente, não permissão negada. Quem construir a leitura da caixa de entrada sem saber disso perde horas caçando o arquivo errado.
A Edge Function é da mesma classe do `whatsapp-webhook`, que este projeto já sabe endurecer: autenticação **antes** de qualquer verificação de configuração (achado F03 da 02.15), `account_id` reafirmado em todo filtro (achado A06 da 01.8), CORS explícito.
Evidência: log da Edge Function com os cinco desfechos e o motivo registrado de cada recusa; prova de que o freio incide sobre o token e não sobre o remetente.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim — é endpoint público servindo dado clínico.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **nenhum** — infraestrutura sem superfície de usuário; o valor aparece em 03.11–03.14.

### Subetapa 03.11 — Caixa de entrada de exames [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 18 — o laboratório envia resultado por link rastreável, revogável e com expiração; o arquivo **espera o aceite do dentista** antes de entrar no prontuário. Máquina de estados `recebida → validada → importada → rejeitada`, com `ip_origem`, `user_agent`, `processada_em` e `processada_por`. Nenhum dos oito concorrentes do benchmark tem isso.
Conclusão: arquivo enviado por token cai na caixa de entrada, não no prontuário; só o aceite explícito o migra; o rejeitado não deixa resíduo legível.
Qualidade: consome a infraestrutura da 03.10, sem tabela de token própria. O laboratório é `aba_people.fornecedores`, que já existe. O aceite é o que impede arquivo de terceiro de cair direto no prontuário — é a trava, não uma etapa burocrática.
Evidência: ciclo completo com arquivo real de teste + contagem de `log_acesso` na importação.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Opus do início ao fim.
CHANGELOG: **+1.0**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — item 47, reportado e não planejado** (`CLAUDE.md` §15). No ICE **o tipo do arquivo governa o acesso**: o anexo herda a permissão do domínio a que pertence (financeiro, imagem, agenda, prontuário). Hoje o nosso bucket é clínico ou não é. Fica registrado como referência de desenho — entrar em escopo é decisão de Max.

### Subetapa 03.12 — Assinatura do paciente por link multicanal [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 19 — assinatura eletrônica simples do paciente por WhatsApp, e-mail, SMS **ou leitura de QR code**, com desenho no celular e estado `pendente → assinado`, sobre `aba_health.consentimentos`, que já existe e nunca teve tela. O concorrente faz por um canal só e cobra R$ 0,15 por documento.
Conclusão: o paciente assina pelo celular a partir de qualquer um dos quatro canais e o consentimento fica registrado com data, canal e token usado.
Qualidade: consome a infraestrutura da 03.10. **E alimenta o estado `assinado` que a 03.8.b já criou (D-V9) — não inventa um segundo.** A 03.8.b entrega contrato assinado presencialmente; esta subetapa acrescenta o canal remoto sobre a mesma máquina de estados, e é aqui que a assinatura do paciente deixa de exigir presença. Um link de assinatura sem expiração é um documento assinável para sempre por quem tiver o link — a expiração é requisito, não configuração. A camada ICP-Brasil com certificado A1 do profissional é o item 25 e fica `+1.0`; esta é a etapa barata antes dela, e a distinção precisa estar visível para não prometer o que não entrega.
Evidência: assinatura colhida pelos quatro canais em ambiente de teste + a recusa de um token expirado.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Opus do início ao fim.
CHANGELOG: **+1.0**

### Subetapa 03.13 — Exportação de prontuário [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 7 — exportação pelo dono da conta a qualquer momento **e pelo próprio paciente quando ele solicita**, sempre por token de expiração curta e com **segunda prova de identidade**.
Conclusão: o dono exporta; o paciente solicita e recebe, e nenhum dos dois caminhos entrega o documento sem a segunda prova.
Qualidade: **o ponto sensível não é gerar o PDF, é entregar** — um prontuário completo com imagens é o documento mais sensível do sistema, e quem pede prontuário por telefone pode não ser o paciente. Expiração curta, não os 90 dias do padrão do Sindcom. O Art. 18, I do Código de Ética Odontológica torna infração negar ao paciente o acesso ao prontuário ou deixar de fornecer cópia quando solicitada: a exportação é cumprimento de dever do cliente, não conveniência — e é argumento comercial que nenhum dos cinco brasileiros usa, contra os 30 meses de retenção que o líder de mercado declara em contrato frente aos 20 anos que a Lei 13.787/2018 Art. 6º exige.
Evidência: os dois caminhos exercidos + a recusa sem a segunda prova + linha em `log_acesso`.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Opus do início ao fim.
CHANGELOG: **+1.0**

### Subetapa 03.14 — Encaminhamento com contrarreferência [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 23 — encaminhamento como entidade com estado (`encaminhado → aceito → em atendimento → contrarreferenciado`), formulário nas duas pontas, pré-requisito clínico declarado, e trânsito sempre por token quando o especialista for externo. Lacuna que nenhum dos oito concorrentes modela — o que existe no mercado é encaminhamento como texto livre na evolução.
Conclusão: o ciclo completo percorrido, com o alerta de "aguardando contrarreferência" nascendo do uso do token, que é o que diz **com quem está a demanda**.
Qualidade: consome a infraestrutura da 03.10. Contrarreferência aceita duas vezes é recusada pela máquina de estados, não pela tela — transição pulada e estado revertido são vetores declarados do portão da 03.23. O pré-requisito clínico é campo declarado, não texto livre.
Evidência: ciclo completo + a recusa da segunda aceitação + o alerta aparecendo sozinho.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Opus do início ao fim.
CHANGELOG: **+1.0**

### Subetapa 03.15 — Portão adversarial da comunicação externa [Manual] [LLM: Opus] · PORTÃO COMPLETO
Objetivo: os 7 passos da pendência vigiada sobre a superfície do bloco B. Justificativa da decisão D2: até a 03.10 o produto tinha **um** endpoint público (o webhook da Meta, autenticado por HMAC); os itens 7, 18, 19 e 23 criam endpoints públicos que **recebem e servem dado clínico**. É a primeira vez que o produto expõe prontuário fora da sessão autenticada, e acumular isso para o fim seria testar tarde a maior mudança de perfil de risco do MVP.
Conclusão: mesmos critérios de 01.8 e 02.15, aplicados à superfície do bloco B.
Qualidade — vetores obrigatórios: token adivinhado; expirado aceito; revogado aceito; reuso após consumo; token de uma conta servindo dado de outra; enumeração por força bruta; freio incidindo sobre a entidade em vez de sobre o token; leitura de anexo sem concessão; URL assinada vazando; `allowed_mime_types` e `file_size_limit` provados como segunda camada real. **Ponto de partida herdado da 02.15:** perguntar primeiro *o que a camada de proteção não tem como responder*, antes de perguntar *se ela está certa* — foi assim que a ponta da chave estrangeira apareceu, e ela não estava em nenhuma lista de vetores previstos.
Evidência: relatório do bench + parecer explícito.
Esforço máximo: sem teto — é auditoria.
Escalonamento de LLM: Opus do início ao fim.
**Merge para `main` é ordem exclusiva de Max** (`CLAUDE.md` §13).
CHANGELOG: **+0.1**

### ONDA 5 — recursos de operação (bloco D)

### Subetapa 03.16 — Alertas clínicos derivados da anamnese [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: item 5 — alertas fixos no cabeçalho da ficha ("Hipertenso", "Risco de hemorragia"), derivados de `aba_health.respostas_anamnese`. **É segurança do paciente, não conveniência**, e nenhuma das três versões de UX previa o elemento.
Conclusão: a resposta de anamnese que caracteriza risco produz o alerta, e ele acompanha a ficha em toda tela clínica.
Qualidade: o alerta é derivado, nunca digitado — mudar a resposta muda o alerta. A regra de derivação vive em tabela do módulo, não em código, para a clínica poder ajustá-la sem deploy (a mesma pendência já aberta para o vocabulário dos mapas clínicos). Anamnese incompleta não gera alerta falso: a migration `034` já exige anamnese completa, e ausência registrada é informação, enquanto ausência não registrada é lacuna disfarçada de informação. A leitura passa pelas funções `ler_*()` com log.
Evidência: alerta nascendo e sumindo com a mudança da resposta + casos de ataque novos na suíte.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Opus do início ao fim.
CHANGELOG: **+1.0**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — esta subetapa REDESENHA.** O ICE não tem "alerta derivado da anamnese": tem **health facts** — condição, medicação e alergia como **entidades** com significância, nota datada e autor, alimentadas pela anamnese e visíveis na barra lateral em cores. É modelo melhor: o alerta deixa de ser uma consulta sobre respostas e vira dado de primeira classe, com histórico e responsabilidade. O redesenho detalhado é trabalho da própria subetapa, quando ela abrir. Fonte: `RELATORIO_DE_IMPACTO_ICE.md` §3.2.

### Subetapa 03.16.a — Dossiê do paciente: três famílias de abas com permissão por módulo [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: **pedido de Max de 2026-09-03**, registrado durante a execução da 03.7 e provisionado aqui em vez de executado na hora. A ficha do paciente deixa de ser só clínica e passa a reunir, no mesmo bloco de abas que a 03.7 unificou, **três famílias**: **Saúde** (Resumo, Anamnese, Mapa facial, Mapa corporal, Odontograma, Acupuntura, Evoluções, Anexos, Consentimentos, Prescrições), **Organizacional** (agendamentos do paciente, alertas ligados a ele, mensagens salvas, notas e observações) e **Financeiro** (contrato, plano, serviços, faturas, pagamentos, atrasos). Duas abas são novas de conteúdo: **Resumo**, a aba inicial que consolida dados sociais, alergias, riscos e planos vigentes; e **Prescrições**, registro de prescrição de medicamento.
Levantamento já feito (2026-09-03, `information_schema` do projeto de produção), para a subetapa não nascer sobre suposição: **12 das 14 abas são interface sobre dado que já existe** — `aba_scheduling.agendamentos`, `aba_messaging.conversas`/`mensagens`, `aba_people.pessoa_notas`/`pessoa_tags`, e as oito tabelas de `aba_finance` (`contratos`, `planos_cliente`, `faturas`, `itens_fatura`, `pagamentos`, `parcelas_contrato`, `saldos_plano`, `extrato_plano`). **Resumo** é derivação, sem tabela nova. **Prescrições não tem nenhuma tabela** e é a única peça que exige migration.
Conclusão: um recepcionista abre a ficha e vê as abas organizacionais e financeiras, e **não consegue ler prontuário nem prescrever — pela recusa do banco, não por aba escondida**; um dentista com alcance clínico vê as abas de saúde; o proprietário vê tudo, e a tela **diz** que ele vê tudo em vez de fingir um controle que não existe para ele. **O corte fino entre papéis — inclusive "dentista vê ou não vê o financeiro do paciente" — não é decidido aqui:** cada aba nasce com a permissão que o módulo dela já tem, e a distribuição definitiva é da **Subetapa 03.21.a** (decisão de Max de 2026-09-03), que varre o produto inteiro de uma vez. Prometer o corte fino nesta subetapa seria declarar uma Conclusão que o mecanismo vigente não sustenta.
Qualidade — **quatro travas, e a primeira é a que decide se a subetapa vale alguma coisa:**
  1. **Esconder aba não é controle de acesso — é cortina.** Cada aba precisa ser sustentada por recusa no banco: as de saúde já são, por `aba_health.pode_acessar()` + o regime de coluna e log; as financeiras passam por `access.can('finance', ação)`; as organizacionais, pelo módulo correspondente. O teste que prova isso não é abrir a tela: é chamar a consulta de cada aba **direto pela API**, com o papel que não deveria alcançá-la, e exigir conjunto vazio ou `42501`.
  2. **`access.can()` devolve TRUE para `owner` antes de consultar a tabela** (`003_core_access.sql:162`, medido duas vezes neste projeto). O proprietário enxerga toda aba, e isso é desenho, não defeito — mas a tela não pode oferecer a ele um interruptor que não faz nada, erro que a 02.12 já pagou uma vez no grid de módulos. Onde o atalho vale, a tela informa; não simula.
  3. **Prescrição de medicamento é entidade clínica nova em `aba_health`** — logo nasce com o regime completo do schema (RLS por `pode_acessar()`, log de leitura e de escrita, revogação de coluna, imutabilidade depois de assinada) e com **P-sub próprio**. E é responsabilidade profissional: prescrever exige o atributo profissional ativo, nunca papel administrativo.
  4. **A distribuição fina de acesso é da Subetapa 03.21.a, não desta.** A separação "dentista não vigia o financeiro" é nova em natureza — até aqui o produto separava por módulo e por alcance clínico, e nunca precisou esconder o financeiro de quem tem alcance clínico. Aqui as abas nascem com a permissão que o módulo já tem e **nenhum mecanismo de permissão é inventado**; a 03.21.a varre tudo e decide. O que esta subetapa não pode fazer é o oposto: entregar aba clínica alcançável por quem o banco não autoriza, contando com a 03.21.a para consertar depois.
Dependência de ordem: **depois da 03.9**, sem exceção. A 03.9 reescreve `access.can()` para multiunidade e trava de plano; montar o dossiê antes obrigaria a reauditar aba por aba depois — exatamente o retrabalho que a decisão D1 de Max existiu para evitar. E depois da 03.8 e da 03.16, que produzem o conteúdo das abas de orçamento/financeiro e dos alertas do Resumo.
Decisão de Max (2026-09-03): **Resumo do paciente e Prescrições de medicamento entram como itens NOVOS do MVP** — a lista fechada do benchmark foi reaberta uma vez, por ele, para acrescentá-los, e o registro dessa reabertura está no cabeçalho da Etapa 2 do roteiro. As outras 12 abas do dossiê são reorganização de interface sobre dado que já existe (medido em 2026-09-03) e não ampliam escopo. **Prescrições é a única peça que exige tabela nova em `aba_health`**, e por isso nasce com o regime completo do schema — RLS por `pode_acessar()`, log de leitura e de escrita, revogação de coluna e imutabilidade depois de assinada — mais P-sub próprio.
Evidência: para cada uma das três famílias, a consulta da aba disparada direto pela API com o papel que não deveria alcançá-la, devolvendo vazio ou `42501` — com controle positivo provando que o papel certo continua alcançando; mais os casos de ataque novos e permanentes na suíte e o ciclo de prescrição (criar → assinar → tentar reescrever) se a decisão de escopo a incluir.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim — toca `aba_health` e o núcleo de permissão.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — o item 25 ganha desenho.** O "Resumo do paciente" é exatamente os **health facts** da 03.16 mais o **resumo de evolução configurável** do ICE, que declara no próprio modelo quais respostas aparecem no resumo — em vez de o resumo ser código. Fonte: `RELATORIO_DE_IMPACTO_ICE.md` §3.2.

### Subetapa 03.17 — Painel como lista de tarefas + estados vazios + exportação [Goal] [Manual] [LLM: Sonnet]
Objetivo: itens 12 e 17 — painel como lista de tarefas acionáveis (confirmar / reagendar / receber), **não de gráficos**, e estados vazios instrutivos mais exportação para Excel.
Conclusão: cada pendência do painel tem o seu botão e leva à ação; nenhuma tela vazia sem instrução do que fazer.
Qualidade: por D5, esta subetapa entrega **o mecanismo e o conteúdo** — quais pendências aparecem, com que regra, em que ordem —, e a **forma** fica concentrada na 03.22. O painel do líder de mercado não tem um único gráfico na aba principal, e o dossiê de UX chegou ao mesmo desenho por caminho independente. Todo contador sobre `aba_health` declara o alcance antes de contar (regra da 03.5).
Evidência: print do painel com pendências reais e clicáveis + um estado vazio de cada família.
Esforço máximo do /goal: 4 tentativas · Escalonamento: Sonnet nas 3 primeiras; Opus na última.
CHANGELOG: **+0.1**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — mecanismo, não item.** O item 39 (*warnings*) é a forma genérica do que esta subetapa faria card a card: um relatório que devolve linha vira mensagem acionável, com texto diferente no painel da clínica e no do paciente, contagem, link para a lista e opção de exigir reconhecimento. **Ficou FORA do MVP por D-I5** e está registrado aqui como desenho a adotar **se couber** no teto desta subetapa — não é escopo acrescentado.

### Subetapa 03.18 — Régua de cobrança, campanhas, template e cota [Goal] [Manual] [LLM: Sonnet]
Objetivo: itens 13, 16, 14 e 15. Régua de cobrança como **linha do tempo** (verde antes → amarelo no vencimento → vermelho depois), cada ponto pendurando uma regra — a melhor peça de UX do corpus do benchmark; campanhas por receita pronta (aniversário, retorno, pós-operatório) com **contagem de alcance antes do envio**; editor de template com variáveis como fichas coloridas no texto, prévia ao vivo e contador; e cota de mensagem declarada no plano em vez de créditos opacos.
Conclusão: a régua dispara pelas regras configuradas nos pontos certos; a campanha mostra quantas pessoas serão atingidas antes de enviar; a cota aparece ao lado do estado da conexão do WhatsApp.
Qualidade: a régua vive em `aba_automations` e lê `aba_finance` — nenhum estado financeiro é recalculado nela. A contagem de alcance é obrigatória **antes** do envio, não relatório depois: é o que impede disparo em massa por engano. A cota declarada resolve a precificação da janela de 24h e é o modelo Weave; nunca cobrar o WhatsApp à parte (decisão de precificação do `RELATORIO.md` §6). A superfície da Meta segue congelada por decisão de Max — o que depender de envio real fica registrado como não exercido, com o motivo, no padrão honesto que a 02.10 já usa.
Evidência: régua percorrendo os três estados + contagem de alcance conferida contra query + prévia do template.
Esforço máximo do /goal: 5 tentativas · Escalonamento: Sonnet nas 3 primeiras; Opus nas 2 últimas.
CHANGELOG: **+1.0**

### Subetapa 03.19 — Link público de agendamento [Goal] [Manual] [LLM: Sonnet] · P-sub
Objetivo: item 10 — link público de agendamento, entrando como **solicitação a confirmar**, nunca direto na agenda. Os cinco concorrentes brasileiros têm.
Conclusão: a solicitação nasce pendente, aparece para a recepção confirmar, e só então vira agendamento sujeito às regras de expediente e sobreposição que já existem.
Qualidade: **endpoint público → P-sub obrigatório**, com o mesmo freio por token da 03.10 — a solicitação não pode ser instrumento de enchimento da agenda alheia. A verificação de expediente e a restrição de exclusão por intervalo continuam sendo do banco; a solicitação não as contorna por vir de fora. Nenhum dado de outra conta é inferível pelo link.
Evidência: solicitação criada de fora, confirmada pela recepção, e a recusa por força bruta de token.
Esforço máximo do /goal: 4 tentativas · Escalonamento: Sonnet nas 3 primeiras; Opus na última.
CHANGELOG: **+1.0**

### Subetapa 03.20 — Estoque: alertas e validade [Goal] [Manual] [LLM: Sonnet]
Objetivo: item 20, no recorte que o `RELATORIO.md` §5.2 recomenda — **alertas e validade primeiro**, porque é o que sustenta o argumento de conformidade sanitária. Itens com `validade` e `quantidade_minima`, e vencimento de serviço de terceiro (calibragem, manutenção, contrato), com job de alerta.
Conclusão: o alerta de validade, de estoque mínimo e de serviço vencendo aparece no painel de tarefas da 03.17 antes de virar problema.
Qualidade: **é conformidade sanitária, não gestão** — é o que evita a autuação no dia da fiscalização, e essa é a tese que reposiciona o módulo. Separar **item de catálogo** de **lote em estoque** (um para muitos, diretriz A8): achatar os dois obriga a repetir o mesmo produto por fabricante. `aba_people.fornecedores` já existe e é o vínculo. Entrada/saída, lote e o kanban de prótese ficam `+1.0`, e a tela declara isso em vez de fingir que já faz. Todo job novo de `pg_cron` nasce com `account_id` no `WHERE` — vetor herdado da 02.15.
Evidência: alerta disparando para os três casos + query do job conferindo o `account_id`.
Esforço máximo do /goal: 4 tentativas · Escalonamento: Sonnet nas 3 primeiras; Opus na última.
CHANGELOG: **+1.0**

### Subetapa 03.21 — Tabela de métricas por CRM-filho [Goal] [Manual] [LLM: Sonnet]
Objetivo: item 21 — tabela de métricas agregando na origem, pronta para a plataforma **Aurora** consolidar quando existir. Métricas: faturamento; clientes ativos; profissionais; procedimentos por categoria; taxa de falta; taxa de falta na primeira consulta, separada; ocupação da agenda; conversão de orçamento; inadimplência; mensagens enviadas.
Conclusão: a série temporal existe e permite correlação local (o uso imediato, e o melhor argumento de venda da tabela), sem nenhuma linha de dado pessoal.
Qualidade — **três travas:** (1) a tabela guarda **contagem e categoria, nunca linha de dado**. Não é anonimização, que é reversível e dá trabalho provar — é **agregação na origem**, estruturalmente incapaz de vazar dado personalíssimo em vez de apenas proibida de fazê-lo, e é essa distinção que sustenta a frase do Termo de Uso; (2) a cláusula do Termo de Uso nomeia **quais** métricas, não "métricas de uso"; (3) métrica agregada de clínica ainda é segredo de negócio dela — o consolidado entre CRMs-filhos é seguro, mas o dado individual por clínica passa pela mesma régua do resto (`access.can()`, nunca "quem tem a URL do painel"). **Sobre a posição na fila:** o argumento de que este item "não dá para adiar" é sobre não adiar **para depois da venda** — retroajustar coleta em N instâncias vendidas é migração coordenada em N bancos. Nada está vendido e o item sai dentro do MVP; adiantá-lo mediria o que ainda não existe (taxa de falta depende da 03.4, conversão de orçamento depende da 03.8).
Evidência: série temporal com as dez métricas + prova de que nenhuma coluna aceita identificador de pessoa.
Esforço máximo do /goal: 4 tentativas · Escalonamento: Sonnet nas 3 primeiras; Opus na última.
CHANGELOG: **+1.0**

---

## ETAPA 03 · ETAPA 3 DO ROTEIRO — ESCOLHER E IMPLANTAR A UX

### Subetapa 03.21.a — Análise e distribuição de acessos: quem vê o quê, em tudo [Goal] [Manual] [LLM: Opus] · P-sub
Objetivo: **decisão de Max de 2026-09-03** — a subetapa que faltava. Fazer, com o MVP praticamente fechado, uma varredura geral de **tudo o que o CRM oferece** (módulo, rota, aba, painel, relatório, RPC, job e bucket) contra **quem alcança cada coisa**, em duas direções: (a) **conferir** o que foi distribuído nas Etapas 01 e 02, item a item, e (b) **distribuir** o acesso às funcionalidades e serviços que a Etapa 03 acrescentou — orçamento, odontograma, prescrições, resumo do paciente, exames, encaminhamento, assinatura por link, exportação de prontuário, métricas, estoque e o dossiê da 03.16.a.
Por que aqui, e não antes nem depois: **antes** não haveria o que distribuir — metade dos recursos ainda não existiria, e a matriz nasceria desatualizada; **depois da 03.22** seria pior ainda, porque a UX teria desenhado telas para papéis que não as verão. Entre 03.21 e 03.22 é o único ponto em que o conjunto está completo e a maquiagem ainda não foi aplicada. E não substitui o portão da **03.23**: aqui se pergunta *"o acesso declarado é o acesso real?"*; lá se pergunta *"o que um atacante consegue apesar disso?"*.
Conclusão: uma **matriz publicada** de papel × recurso, cobrindo os quatro papéis (`owner`/`admin`/`agent`/`viewer`) mais o atributo profissional e as concessões nominais, em que **cada célula "não alcança" foi medida**, não presumida; e toda divergência entre o declarado e o medido, corrigida ou reportada com motivo.
Qualidade — **cinco travas, e a primeira decide se a subetapa vale alguma coisa:**
  1. **Esconder não é negar.** Item de menu oculto, aba ausente e botão desabilitado são conveniência; a garantia é o banco recusar. Cada célula "não alcança" da matriz é provada chamando a consulta **direto pela API**, com o papel que não deveria alcançá-la, e exigindo `42501`, conjunto vazio por RLS ou `access.can` falso. Tela não é evidência de permissão.
  2. **Ler o SQLSTATE, não só a falha** (lição da 02.15): `23514`/`23502`/`42703` significam "o payload estava errado", não "o sistema barrou". Recusa incidental contada como isolamento **subnotifica** — e subnotificação numa matriz de acesso é pior que nenhuma matriz, porque vira verdade herdada.
  3. **`access.can()` devolve TRUE para `owner` antes de consultar a tabela** (`003_core_access.sql:162`, medido três vezes neste projeto). A matriz **declara** esse atalho em vez de fingir granularidade que o proprietário não tem — e nenhuma tela oferece a ele interruptor que não faz nada, erro que a 02.12 já pagou.
  4. **Varredura dirigida por CATÁLOGO, nunca por lista escrita à mão.** Cruzar `information_schema`/`pg_proc`/`pg_policies` com o roteador do front: o recurso que ninguém lembrou é exatamente o que fica aberto — foi assim que a 01.8 achou `account_invitations.token_hash` e a 02.15 achou as 73 chaves.
  5. **Guarda permanente**, no molde de `039_auditoria_isolamento_de_conta.sql`: uma função de auditoria mais um teste de suíte que **falha quando nasce recurso novo sem linha na matriz**. Sem isso a análise vale para o dia em que foi feita, e a próxima subetapa reabre tudo.
Escopo declarado que esta subetapa absorve: a pergunta "**um dentista precisa ver a situação financeira do paciente?**", levantada por Max em 2026-09-03 ao pedir o dossiê da 03.16.a, e que é de natureza nova no produto — até aqui a separação era por módulo (`access.can`) e por alcance clínico (`aba_health.pode_acessar`), e nunca foi preciso esconder o financeiro de quem tem alcance clínico. A 03.16.a monta as abas com a permissão que o módulo já tiver; **o corte fino é decidido e implantado aqui**.
Evidência: a matriz completa como documento versionado; a saída da varredura por catálogo mostrando zero recurso sem linha; e, para cada família, o par ataque/controle — o papel que não alcança recebendo recusa medida, e o papel que alcança continuando a alcançar (o controle negativo é o que impede "fechei tudo" de passar por "está seguro").
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Opus do início ao fim — toca o núcleo de permissão e `aba_health`.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+0.1**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — três coisas a levar e uma a recusar.** **Levar:** (1) a permissão de relatório **intersecta** as de domínio — relatório de auditoria exige a permissão de atividade, relatório financeiro exige a de financeiro; a nossa 03.5 chegou nisso por decisão de produto, e aqui é regra geral que vale varrer em toda tela agregada; (2) validação é **par de grupos** ("exige validação" × "pode validar"), não atributo de pessoa — item 42, futuro; (3) **o papel na consulta é ortogonal à permissão** — principal, assistente, higienista e dentista rotulam quem faz o quê **no atendimento** e não dão acesso a nada, e hoje nós conflamos as duas coisas no atributo profissional. **Recusar:** trocar a hierarquia pelo modelo aditivo do ICE — `is_account_member` + `access.can()` é `CLAUDE.md` §14 puro, portado do Maximus e provado em produção; o que se leva é a **pergunta** que o modelo aditivo faz bem ("o que exatamente esta pessoa pode fazer?"), não o mecanismo. Fonte: `RELATORIO_DE_IMPACTO_ICE.md` §3.3.

### Subetapa 03.22 — Implantação da UX Versão 03 [Goal] [Manual] [LLM: Sonnet]
Objetivo: implantar a **Versão 03** (`design/ux/versoes/`, decidida por Max em 2026-09-03) mais os cinco acréscimos do `RELATORIO.md` §7 — tela de orçamento, odontograma como padrão de tela novo, alertas clínicos no cabeçalho da ficha, consentimento de imagem visível e indicador de cota de mensagem. É a maquiagem final, aplicada depois do MVP porque há interface funcionando em 16 rotas para recebê-la.
Conclusão: as rotas na forma da Versão 03, com a forma final dos itens 12, 13 e 17, cujo mecanismo e conteúdo saíram em 03.17 e 03.18.
Qualidade: o orçamento de peso de `design/ux/06_ORCAMENTO_DE_PESO.md` §4 continua respeitado depois da conversão — nenhuma biblioteca de gráfico (SVG inline já resolve, e é a decisão de arquitetura mais valiosa desta frente), nenhuma de calendário, de animação ou date picker. A identidade ratificada em `docs/04` §5 não é negociada: raio 8px, separação por borda de 1px e não por sombra, `IBM Plex Mono` como vocabulário de metadado. A paleta de comandos da Versão 02 continua backlog, não caminho descartado.
Evidência: capturas das rotas convertidas ao lado das telas de `design/ux/versoes/telas/` + bundle dentro do teto.
Esforço máximo do /goal: 5 tentativas
Escalonamento de LLM: Sonnet nas 3 primeiras; Opus nas 2 últimas.
Se esgotar: parar e emitir relatório curto.
CHANGELOG: **+1.0**
**Acréscimo da pesquisa `analise-ice` (2026-09-04) — +1 ao parecer.** O mesmo módulo aparece em **dois tamanhos**: painel compacto no dashboard e área cheia na tela própria. É composição, não schema. **E uma advertência de direção:** não copiar a estética do ICE — o produto é visualmente arcaico e sobrecarregado; o interesse dele é estrutura, campo de formulário, caminho feliz e caminho alternativo.

---

## ETAPA 03 · ETAPA 4 DO ROTEIRO — TESTE ADVERSARIAL DO MVP

### Subetapa 03.23 — Portão de segurança adversarial do MVP [Manual] [LLM: Opus] · PORTÃO COMPLETO
Objetivo: terceiro portão completo desta Etapa e quarto do projeto, no molde de 01.8, 02.15 e 03.15 — os 7 passos sobre o MVP inteiro, agora com a superfície que os portões anteriores não cobriram.
Conclusão: os 7 passos executados em bench isolado; relatório final com parecer explícito.
Qualidade — os sete vetores que Max nomeou: **segurança de dados** (RLS de toda tabela nova, `aba_health` com regime próprio, injeção em toda RPC nova); **token e link** (todos os vetores da 03.15, reexercidos sobre a superfície final); **bucket e Storage** (leitura de anexo sem concessão, URL assinada vazando, `allowed_mime_types` e `file_size_limit` como segunda camada); **núcleo de permissão** (usuário com duas contas lendo dado da conta inativa, e módulo fora do plano visível ao `owner`); **automação** (todo job novo de `pg_cron` conferido quanto ao `account_id` no `WHERE`); **navegabilidade e caminho infeliz** (rota fora do caminho feliz, estado impossível, XSS armazenado contra a UI real); **kanban e máquina de estados** (transição pulada, estado revertido, contrarreferência aceita duas vezes). Todo achado — explorável ou não — vira entrada em `handoffs/instrucoes.md`; toda falha real vira item `[Goal]` com Conclusão, Qualidade e Esforço máximo declarados.
Evidência: relatório do bench + parecer explícito.
Esforço máximo: sem teto — é auditoria.
Escalonamento de LLM: Opus do início ao fim.
**O CODE nunca executa o merge por conta própria, mesmo com parecer 100% favorável** (`CLAUDE.md` §13).
CHANGELOG: **+0.1**

(… 03.n conforme necessário. Subetapa nova de escopo entra **antes da 03.23**, para que o portão adversarial continue sendo o último passo da Etapa. O sufixo de LETRA — `03.16.a`, `03.21.a` — é **o padrão único** para inserir subetapa sem renumerar as seguintes, que já estarão referenciadas em commits e em entradas de `handoffs/instrucoes.md`. Sufixo numérico não se usa: `03.21.1` seria lido como "a primeira parte da 03.21", e não é isso — é uma subetapa irmã, inteira. Ratificado por Max em 2026-09-03, depois de a `03.21.a` ter nascido numerada e ser corrigida.)

---

## Pendências vigiadas — recorte da Etapa 03

Lista completa e permanente em `docs/00_PLANO_E_CRITERIOS.md` → "Pendências vigiadas". Aqui só as que travam, informam ou nasceram de uma subetapa `03.n`.

- [x] **Pesquisa `analise-ice` — o ICE Health System como fonte de referência, e o retrabalho que ela vai gerar** — gatilho: **imediato**, em sessão e branch próprios (`analise-ice`), com a Etapa 03 pausada na Subetapa 03.8 (2026-09-03, ordem de Max). Escopo restrito a `design/benchmark/`: site de suporte (`https://help.icehealthsystems.com/`, todas as páginas) e canal de 32 vídeos no YouTube, com atenção especial ao **odontograma** (SVG com face/raiz/região clicáveis e pop-up de detalhamento por dente) e ao **financeiro/orçamento**. Saídas: `design/benchmark/fontes/ice.md`, capturas em `design/benchmark/capturas/ice/`, resumo incorporado ao `RELATORIO.md` §5(c) — que **já está desatualizado**, pois lista 24 itens de MVP onde hoje são 26. **Risco de não fazer agora:** a 03.8 nasce com um desenho de orçamento que a referência mais rica do mercado desmente, e a 03.7 fica congelada num odontograma sem face clicável — os dois elos centrais do produto odontológico. **Risco assumido ao fazer:** o relatório de impacto provavelmente reabre subetapas já `✅ CONCLUÍDA` (03.7 em primeiro lugar) e redesenha a 03.8. **O merge de `analise-ice` em `main` é ordem exclusiva de Max** (`CLAUDE.md` §13).
  - **FECHADA em 2026-09-04.** A pesquisa foi executada (424 páginas e 32 vídeos), aprovada por Max, fundida em `main` por ordem dele e depois fundida no branch da Etapa 03 (commit `6f694a2`). **O risco assumido se materializou como previsto:** duas subetapas concluídas reabriram (03.6 e 03.7, revisadas pelas novas 03.6.a e 03.7.a) e a 03.8 foi redesenhada e partida em duas (03.8 e 03.8.a). **Nenhuma migration precisou ser revertida.** O parecer completo está em `design/benchmark/RELATORIO_DE_IMPACTO_ICE.md`; a reconciliação do plano foi a Subetapa 03.0.a.

- [ ] **As pesquisas dedicadas de periodontia (item 48) e ortodontia (item 49) — disponíveis sem nova coleta, e o material bruto vive fora do controle de versão** — gatilho: decisão de Max, quando os itens 48 e 49 saírem do futuro. **Registrada por D-I6 (2026-09-04): Max decidiu NÃO fazê-las agora**, e a razão é boa — os dois itens são futuro e o parecer não teria consumidor. **O motivo de a pendência existir mesmo assim é outro, e é medido:** o material bruto da coleta continua em `~/.claude/jobs/analise-ice/` com **95 MB** (`site.json`, `imagens.json`, `img/`, `videos/`), conferido em 2026-09-04, e 10 das 20 páginas do tema já foram lidas — mas esse diretório é arquivo de ferramenta, **fora do controle de versão do projeto**. É a mesma classe de perda silenciosa que `handoffs/instrucoes.md` §5 registrou com a skill `/watch`: nada no repositório aponta para ele, nada testa se ainda está lá, e o sintoma só aparece quando alguém tenta usá-lo. **Se o disco for limpo, a pesquisa volta a custar a coleta inteira** — 424 páginas e 32 vídeos, com o limite de taxa de legenda do YouTube junto. **Conferir a existência do diretório antes de prometer a pesquisa a custo baixo.**
- [ ] **A `047` (Subetapa 03.8) está em produção com os comentários podados** — gatilho: próxima subetapa que tocar `aba_treatment`. **Medido na 03.8.a, não suposto:** `aba_treatment.ler_planos` tem **3.278 bytes** no banco de testes e **2.741** em produção, diferença grande demais para ser só `CRLF` — é a mesma poda de comentários que a 03.8.a cometeu na `048` e corrigiu (`instrucoes.md` §5). **A lógica não foi conferida além do tamanho**, e essa é a parte que importa: enquanto a comparação por hash sem comentário não for feita nas funções da `045`/`047`, ninguém sabe se a diferença é só texto. **Não é bloqueio** — as 31 provas do P-sub da 03.8 rodam contra o banco de testes e o regime clínico está provado nos dois —, mas é dívida de fidelidade entre repositório e produção, e a correção é reemitir os corpos, como a 03.8.a fez.
- [x] **FECHADA em 2026-09-05, por ordem de Max — e o achado era maior do que a pendência dizia.** A pendência supunha "comentários podados"; a medição, função a função, mostrou **oito das nove** funções de `aba_treatment` idênticas em conteúdo (só comentário) e **uma diferente de verdade**: `aba_treatment.validar_procedimento_plano` — a função que carrega todas as travas clínicas da 03.8 — tinha em produção a mensagem `'Procedimento % nao existe no catalogo desta conta.'`, **sem os acentos** de "não" e "catálogo". A hipótese foi confrontada antes de virar diagnóstico (`CLAUDE.md` §11): aplicando a troca dessa única string ao corpo de produção, o hash normalizado virou exatamente o do banco de testes (`cb359a7c85146f7e637410936f559d01`), o que provou que essa era a **única** divergência de conteúdo no schema inteiro e que **nenhuma trava clínica estava diferente** — mesmo `23503`, mesma recusa. Migrations `049a`/`049b` reemitiram os nove corpos com o texto exato de `045`/`047`, e a conferência final devolveu **o mesmo hash nos dois bancos** (`2364ec8e6e19bf038936c5c5236c6cdb`), com os 11 gatilhos do schema ativos. **A lição que fica é maior que o caso:** a poda de transcrição para o MCP alcança **conteúdo**, não só comentário — foi a comparação por hash que a pegou, e é por isso que ela passou a ser obrigatória depois de toda aplicação por MCP (`instrucoes.md` §5).
- [ ] **Matriz Bronze/Prata/Ouro/Diamante — qual módulo e qual funcionalidade entra em cada nível** — gatilho: decisão de Max, no momento que ele julgar correto, e não antes. **Não é bloqueio da Etapa 03.** A Subetapa 03.9 constrói o *mecanismo* (a camada de plano consultada antes do atalho de `owner` em `access.can()`), que nasce com todos os níveis liberando tudo; preencher a matriz depois é dado, não código.
- [x] **FECHADA em 2026-09-03 — Max autorizou.** `CLAUDE.md` §14 passou a nomear o CRM Sindcom como fonte de porte para comunicação externa por token, com o mesmo estatuto do Maximus. **A Subetapa 03.10 está desbloqueada.**
- [x] **FECHADA em 2026-09-03 — Max decidiu: `Resumo do paciente` e `Prescrições de medicamento` entram como itens NOVOS do MVP** (itens 25 e 26). A lista fechada do benchmark foi reaberta por ele, com registro no cabeçalho da Etapa 2 do roteiro. As outras 12 abas do dossiê são interface sobre dado já existente e não ampliam escopo. Prescrições é a única peça com tabela nova em `aba_health`, e por isso leva o regime completo do schema e P-sub próprio. **A Subetapa 03.16.a está desbloqueada quanto a escopo.**
- [x] **ENDEREÇADA em 2026-09-03 — Max criou a Subetapa 03.21.a para isto.** A pergunta "um dentista precisa ver a situação financeira do paciente?" deixou de ser um item solto da 03.16.a e virou parte de uma varredura geral de acessos, entre a 03.21 e a 03.22: conferir tudo o que as Etapas 01 e 02 distribuíram e distribuir o que a Etapa 03 acrescentou. Continua **aberta como questão de produto** — o que fechou foi o lugar onde ela se responde.
- [ ] **Colisões com `CLAUDE.md` §15 encontradas no benchmark (diretriz P1)** — gatilho: decisão de Max sobre escopo. Duas: agente de IA 24h no WhatsApp e certificação SBIS/CFM (item 33, futuro). Reportadas, não planejadas; nenhuma das duas está em subetapa da Etapa 03. O odontograma da 03.7 traz HL7 FHIR R4 de graça, que é vocabulário útil se a certificação entrar um dia.
- [x] **FECHADA na Subetapa 03.0 — virou pré-requisito, não melhoria (diretriz P2).** A divisão por rota deixou de ser acabamento e virou pré-requisito da 03.7 (odontograma, 426 KB gzip). Institucionalizada como Subetapa 03.3, **executada em 2026-09-03**.
- [ ] **Consentimento de uso de imagem trava a exibição, não o envio — e trava para todos** — gatilho: prova de fogo com profissional real. Decisão de Max (2026-08-08): manter como está. Se a 03.5 confirmar o incômodo, a correção certa não é liberar geral: é separar "posso documentar" (consentimento de tratamento de dados) de "posso divulgar" (uso de imagem, para exportação e publicação). **Candidata a considerar ao desenhar a Subetapa 03.5**, sem que isso mude a decisão já tomada.
- Definição normativa do **Portão de segurança adversarial obrigatório** (os 7 passos que 03.9, 03.15 e 03.23 seguem) mora só em `docs/00_PLANO_E_CRITERIOS.md` → Pendências vigiadas — não duplicada aqui por ser longa e estável; abrir o docs/00 quando for executar um dos três portões completos.
